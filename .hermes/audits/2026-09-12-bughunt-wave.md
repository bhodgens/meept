# Bughunt Wave 2026-09-12 - meept, boundary cf527d44..HEAD (691d83f6)

REPORT ONLY - no fixes applied, no repo file edited by this pass
(the fixes came later; see .hermes/audits/2026-09-12-fix-wave.md).

## CORRECTIONS to this report (added after the fix wave, 2026-09-13)

Two claims in the body below were re-read against source and are WRONG. They are
struck here rather than edited in place so the record shows what the audit got
wrong.

1. F13 "270be60f removed the loopback seed ... binds ALL interfaces on an
   ephemeral port" - the wildcard-bind REACHABILITY claim is false.
   internal/comm/http/server.go:240-242 re-applies "127.0.0.1:8081" inside
   NewServer whenever Addr == "", and `git show 875c84b9:internal/comm/http/server.go`
   proves that guard was already present at the audit base. The daemon never
   handed "" to net.Listen, so this was never a remote-exposure bug.
   What survived: an empty Addr at the assignment site (daemon.go:1137 at the
   time) and a boot log that printed addr="" for a server actually bound to
   loopback. Corrected severity: LOW (misleading state + no invariant pin),
   not HIGH. The parent's own verification reproduced the MECHANISM
   (net.Listen("tcp","") is the wildcard) but not the REACHABILITY - the
   lesson is that both are required before a severity is assigned.
2. F27's sub-claim "v_fallback_trend counts a classifier_method value no
   production code writes" is false. dispatcher.go:1433 and :1447 write
   Method: "fallback", and both lines exist at the audit base 875c84b9. The
   view is populated; the auditor's grep evidence was wrong. The other half of
   F27 (the unreachable 'suppressed' bucket in harvest_outcomes.py) stands and
   was fixed.


## Scope and method

Repo: /Users/caimlas/git/meept. Boundary commit `cf527d44` is the last commit
covered by the 2026-09-10 wave record (.hermes/audits/2026-09-10-bughunt-wave.md),
so the wave under review is `cf527d44..HEAD`: 105+ commits, 507 files, 245 Go
files, 93 test files.

Shape: 7 parallel READ-ONLY auditors (disjoint scopes A-G) + parent pre-reads,
per the bughunt pipeline. Every CRITICAL/HIGH that this report calls
parent-verified was re-read by the parent at file:line at HEAD, and the four
load-bearing mechanisms were reproduced independently (encoding/json decode
probe under /tmp/evprobe; source reads of internal/task/step.go, the validator,
the HTTP listen path; a grep of ~/.hermes/sessions for the corpus rows).
Auditor raw output (full evidence text, all 100 findings): see the paths in
the appendix footer.

### Baseline gates (parent, at HEAD)
- `go build ./...` -> exit 0, clean.
- `go vet ./...` -> exit 0, clean.
- `go test -p 2 -count=1 ./...` -> exit 0, 102 packages ok, 0 FAIL lines.
  (Log: ~/.hermes/logs/process-results/proc_1d2810ef0ca7.json)
- The suite ran WITH the then-uncommitted classifier-lane WIP present; that WIP
  landed mid-review as 691d83f6. So the baseline is not "broken at start".

### Live-tree warning (read this before acting on line numbers)
Sibling sessions committed into this repo during the audit. HEAD moved
8b29dbdb (19:24) -> 875c84b9 (19:41) -> 691d83f6 (19:47, the classifier-lane
commit). Auditor citations were read at 875c84b9/691d83f6; the line numbers in
findings that touch internal/agent/llm_classifier.go refer to the post-691d83f6
tree (verified by the parent). Re-check any patch against the tree at the
moment you apply it.

## New finding on 691d83f6 (landed during this audit, parent-verified)

### C-0. The LLM classifier can now emit quickplan, but the async gate stays shut for that producer
- `internal/agent/llm_classifier.go:524` (ClassifyMulti) and `:637`
  (parseResponse) both set `requiresPlanning := intent == string(IntentPlan)`.
- `internal/agent/intent.go:209-210`: `case IntentQuickPlan: return requiresPlanning`.
- `internal/agent/handler.go:770` opens the async path only on
  `h.dispatcher.ShouldDispatchAsync(result) && result.Task != nil`, and
  `internal/agent/dispatcher.go:3302` reads `result.Intent.RequiresPlanning`.
- Result: an LLM-classified quickplan carries RequiresPlanning=false, so the
  gate that fac36b5e opened for the fallback/semantic producers stays shut for
  the producer 691d83f6 just made reachable. The lane is now emittable but its
  route is closed: the C1 shape (2026-09-10 record) returns for this producer.
- The same commit took the emittable lane list from 12 to 27
  (classifierLanes, llm_classifier.go:40-70). Each lane needs a resolvable
  agent; `IntentQuickPlan.DefaultAgent()` returns "orchestrator", and there is
  no `config/agents/orchestrator/` directory. fac36b5e's commit message argues
  the missing spec is irrelevant on the async path because strategic.Plan
  resolves the planner spec - so the finding is the closed gate above, not the
  missing directory, but the two must be checked together.
- Fix shape: derive the flag from the lane (`IntentType(resp.Intent).RequiresPlanning()`)
  at both sites, and pin an LLM-quickplan async dispatch.



## CRITICAL (2)

### F1 [0] Approval paths write the PRE-review step state back to the store, un-terminalizing the step so the task can never finalize
- file:line: internal/agent/review_manager.go:207-211 (heuristic) / :521-523 (reviewer approval) / internal/agent/tactical.go:1486-1488 (needs_info)
- evidence: review_manager.go:509-523: `if err := rm.stepStore.SetState(step.ID, task.StepApproved)` ... `if !step.Validated { step.Validated = true; step.ValidationError = ""; if err := rm.stepStore.Update(step); ... }`; heuristic site :198-211 is identical; tactical.go:1479-1488: `ts.stepStore.SetState(step.ID, task.StepCompleted)` then `step.Validated = true; ... ts.stepStore.Update(step)`. StepStore.Update writes the whole row (internal/task/step.go:521-556) including `state = step.State`.
- why: SetState writes the DB only; the in-memory step is never refreshed, so Update(step) re-writes the STALE state over the terminal one. HandleReviewResult loads the step itself at :494 while the row is still 'reviewing' (set DB-only by ReviewStep at :96), so the row ends 'reviewing'; the heuristic site is called with tactical's step pointer whose state is 'scheduled' (tactical.go:945 getStepByJobID, dispatch-time SetState(StepScheduled) at :738; StepRunning is never set in production code). Either way the step leaves review NON-terminal, so task/step.go AreAllCompleted (:973) returns false, the completion block in tactical.go:1301-1345 is never reached, and the sync reply hangs to its ceiling. This is the exact failure mode the file already documents and fixed for the rejection path (review_manager.go:555-563: SetState(rejected) then Update(step) 'wrote reviewing back over rejected ... revisions never scheduled (2026-09-06 live-run finding)'); the wave reintroduced the pattern on the two approval paths it touched.
- claimed vs actual: d80b1295 / f127e765 / 6f462f55 claim to persist approval+validation ('Persist Validated=true/ValidationError="" ... so approval means completion'). They do persist the flag, but the same write reverts the state, so the approved step is non-terminal for the completion gate they were trying to satisfy.
- fix shape: Set the in-memory state before the write (the 2026-09-06 fix shape): `step.State = task.StepApproved; step.Validated = true; step.ValidationError = ""; rm.stepStore.Update(step)`, or drop the full-row Update and add a narrow `SetValidated(stepID, bool, errMsg)` store method. Add a test that drives ReviewStep -> approval and asserts persisted State==approved AND Validated==true.
- parent verification: VERIFIED by parent: review_manager.go:198-212 / :509-526 run SetState(approved) then stepStore.Update(step); internal/task/step.go:524 writes state = string(step.State), so the stale in-memory step overwrites the terminal state. Fix shape as stated.

### F2 [0] tactical.go persists the validation verdict BEFORE assigning it - the b31c3f6e fix is a no-op and the newly-live gate blocks completion
- file:line: internal/agent/tactical.go:1139-1142
- evidence: `// Persist Validated=true/ValidationError="" here.` / `if err := ts.stepStore.Update(step); err != nil { ts.logger.Error("Failed to persist step validation verdict", ...) }` / `step.Validated = true` / `step.ValidationError = ""`
- why: Update serializes step.Validated/validation_error by value at call time (internal/task/step.go:540-541 `step.Validated, nullableString(step.ValidationError)`) and the assignments happen three lines later, so the row keeps validated=false. ValidatorManager.ValidateStep does NOT set the flag itself (internal/validator/manager.go:67-93 only calls the validator). The task gate at tactical.go:1327 (`if ts.validatorManager != nil && s.State.IsSuccessfullyTerminal() && !s.Validated`) therefore still fires 'step completed but not validated', which the commit says it fixed - and because 58a2ff42 wires ValidatorManager for the first time (see next finding) the gate is now live, so this converts every validated step into a blocked completion.
- claimed vs actual: b31c3f6e states 'Validated was set only on the in-memory struct ... Persist Validated=true/ValidationError="" here'. Reality: the row is persisted with the pre-validation values; the described bug survives unchanged.
- fix shape: Move the Update call after `step.Validated = true; step.ValidationError = ""`, and pin it with a store re-read assertion in a test that runs the validator path.
- parent verification: VERIFIED by parent: tactical.go:1139 Update, :1142-1143 assignments. Found independently by scopes A and B.


## HIGH (24)

### F3 [0] ValidatorManager wired for the first time turns every non-approval completion path into a permanent task hang
- file:line: internal/daemon/components.go:2757-2763 + internal/agent/tactical.go:1189-1194, :1201-1206, :960
- evidence: components.go:2763 `c.ValidatorManager = validator.NewValidatorManagerWithLogger(...)`; tactical.go:1189-1194 `// Continue without review - mark as completed` / `SetState(step.ID, task.StepCompleted)` (no Validated); :1201-1206 same in the review-disabled branch; :960 pair-managed path.
- why: 58a2ff42's own message says ValidatorManager 'was constructed nowhere in the daemon', so the gate at tactical.go:1327 was inert for every pre-existing path. Now that it is wired, any step terminalized through these three paths has Validated=false while State is successfully terminal -> validationErrors non-empty -> `return fmt.Errorf("task validation incomplete: ...")` blocks finalization forever. A reviewer error (ReviewStep returns nil,err -> tactical.go:1190) is enough; the user sees the same i/o timeout the wave was chasing.
- claimed vs actual: Claim ('turning the existing ValidateStep gate on for the first time') is true - but the wave did not audit what the gate now rejects. Three parallel consumers of the same defect shape were left unguarded while only three others (heuristic/reviewer/needs_info) were patched.
- fix shape: Set the validated flag (with an explicit reason) on every terminalization path, or scope the gate to steps that actually entered a validation/review flow (e.g. only flag steps whose ValidatorManager had a validator for the tool hint).
- parent verification: VERIFIED by parent: internal/daemon/components.go:2763 constructs ValidatorManager; tactical.go:1192/:1203/:960 terminalize without Validated; the gate at tactical.go:1327-1340 then blocks finalization.

### F4 [0] Claim-vs-evidence backstop returns early and aborts the rest of OnJobCompleted, leaving the step non-terminal
- file:line: internal/agent/tactical.go:1040-1054
- evidence: `// ...mark it so downstream consumers can tell unverified narration from a validated result, and let the normal completion flow proceed.` / `return nil` (line 1054)
- why: The `return nil` exits OnJobCompleted before the validation gate (:1057), the step-completed publish (:1146), the review block (:1163-1206), context propagation, IncrementCompletedJobs and the whole task-finalization block (:1301-1440). Nothing sets the step terminal, so AreAllCompleted stays false and the task hangs in executing - a worse outcome than the OLD code, which fell through and completed the step (marked unverified at most). The comment claims the opposite of what the code does.
- claimed vs actual: 58a2ff42 claims 'mark it rather than let a fabricated report ride forward as ground truth' and 'let the normal completion flow proceed'. The annotation persists, but the completion flow does not proceed.
- fix shape: Drop the `return nil` (fall through after marking + Update), or restrict the early exit to skipping the fabricated result publish; never return before the step is terminalized.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F5 [0] The ralph cap fails the task before evaluating the final attempt's evidence
- file:line: internal/agent/ralph_loop.go:103-113
- evidence: `if iteration >= rl.config.MaxIterations { rl.logger.Warn("Max Ralph loop iterations reached, failing task", ...); rl.failTaskAtCap(taskID, "max ralph loop iterations reached without sufficient evidence"); return false, nil, false }` // Parse result to extract completion evidence (line 116, unreachable at the cap)
- why: TriggerReplan increments the counter (:237-247) and only then grants the next attempt a full execution budget; when that attempt reports back, CheckCompletion hits the cap branch FIRST and discards the result - evidence/checklist parsing at :116+ is skipped. So the reason string ('without sufficient evidence') is not evaluated for the attempt being killed, and a task that finally succeeded on attempt MaxIterations+1 is terminalized StateFailed. OLD behavior returned (true, nil, false) = complete; the wave flipped a false-complete into a false-failure for the boundary attempt.
- claimed vs actual: d8833a60 claims the cap now 'terminalizes as failed, not fake-complete' - true for the cap itself, but the implementation discards the final attempt's evidence instead of checking it first, and the test (ralph_loop_cap_test.go) never covers 'final attempt has adequate evidence'.
- fix shape: Evaluate the result/checklist first; call failTaskAtCap only when the evidence is still insufficient after the last granted attempt.
- parent verification: VERIFIED by parent: ralph_loop.go:102-114 returns before the result parse at :116.

### F6 [0] failTaskAtCap is not durable: the task can be flipped back to completed and the ralph counter reset
- file:line: internal/agent/ralph_loop.go:339-345 + internal/agent/tactical.go:1364-1371 + internal/agent/orchestrator.go:451-460
- evidence: ralph_loop.go:339-345 `func (rl *RalphLoop) failTaskAtCap(taskID, reason string) { t, err := rl.taskStore.GetByID(taskID); ...; t.State = task.StateFailed; ... }` (no IsTerminal guard); tactical.go:1364-1371 `if taskFailed { t.SetState(task.StateFailed) } else { t.SetState(task.StateCompleted) }` (no IsTerminal guard).
- why: The cap fires while other steps of the same task may still be in flight; when those steps go terminal, tactical's finalize block recounts failures and can set StateCompleted over the cap's StateFailed. The orchestrator then sees a terminal task, TaskOutcome fires, and `o.ralphLoop.Reset(taskID)` (orchestrator.go:457-460) zeroes the counter - the '3->1->2->1' loop the commit was written to kill can still occur, and the user can receive both 'task failed' and 'task completed'.
- claimed vs actual: d8833a60 claims 'the orchestrator's TaskOutcome-gated Reset then never fires and the counter stays armed'. Nothing enforces the terminal state, so that claim does not hold under concurrent step completion.
- fix shape: Add `if t.State.IsTerminal() { return }` to failTaskAtCap and to tactical's finalize block before the SetState.
- parent verification: VERIFIED by parent: ralph_loop.go:339-349 and tactical.go:1362-1367 have no IsTerminal guard.

### F7 [0] b31c3f6e's "persist the validation verdict" writes the row BEFORE the assignment, so validated=true is still never persisted
- file:line: internal/agent/tactical.go:1133-1144
- evidence: 1133 /* PERSIST the verdict (e2e run 9 brx2FG) ... Persist Validated=true/ValidationError="" here. */
1139 if err := ts.stepStore.Update(step); err != nil {
1140     ts.logger.Error("Failed to persist step validation verdict", "step_id", step.ID, "error", err)
1141 }
1142 step.Validated = true
1143 step.ValidationError = ""

stepStore.Update serializes the struct's CURRENT fields (internal/task/step.go:525 `validated = ?, ... validation_error = ?`), so line 1139 persists validated=false plus whatever validation_error was loaded (the DB column defaults FALSE — step.go:367). The assignment at 1142-1143 happens after the write and is never flushed by this handler.
- why: This is the exact bug b31c3f6e was written to fix: the task-level completion check re-reads rows via stepStore.ListByTaskID (tactical.go:1317) and blocks on `s.State.IsSuccessfullyTerminal() && !s.Validated` (1327) and on `s.ValidationError != ""` (1331), returning `fmt.Errorf("task validation incomplete: %s", ...)` (1340) before the task ever reaches StateCompleted. Every later consumer that re-reads the row sees validated=false. The hang is masked today only when a later review write happens to serialize the same in-memory struct (ReviewStep's heuristic Update at review_manager.go:209, HandleReviewResult's Update at 523, or tactical.go:1488 for needs_info). It is NOT masked on the ReviewStep-error path (tactical.go:1189-1194: log + SetState(StepCompleted), no Update), on a nil reviewManager, or on review-policy-disabled. OLD behavior was identical only because the assignment never happened at all; the new code adds a write that claims to fix it and cannot. The sibling fix at 6f462f55 got the order right (1485-1491: assign, then Update) — the two hunks contradict each other.
- claimed vs actual: Commit: "The gate's validation verdict was never persisted ... Persist Validated=true/ValidationError='' after a passing gate." Actual: the Update is issued before the two assignments, so the persisted verdict is still validated=false (and the stale validation_error) — the claimed fix is a no-op. A step that failed validation once, retried, and passed is persisted with validated=false AND the old validation_error string (written by the retry branch at 1082), which trips BOTH task-level conditions at 1327/1331.
- fix shape: Swap the order: `step.Validated = true; step.ValidationError = ""; if err := ts.stepStore.Update(step); err != nil { ... }` (identical to the needs_info fix at 1485-1491). Add a regression test that asserts the row read back via ListByTaskID/goal has Validated==true after a passing gate with the review path stubbed to no-op.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F8 [0] 58a2ff42's claim-vs-evidence gate can never fire: len(step.Evidence)==0 is false for every daemon step job (decode artifact)
- file:line: internal/agent/tactical.go:1040 (with 1013-1032); internal/daemon/components.go:7983
- evidence: tactical.go:1040
  `if len(execResult.Claims) > 0 && len(step.Evidence) == 0 && ts.claimsFileSideEffects(execResult.Claims) {`
tactical.go:1022-1023
  `} else if len(execResult.Evidence) > 0 {`
  `    step.Evidence = execResult.Evidence`
daemon components.go:7976-7984 (unconditional)
  `result := map[string]any{ "job_id": job.ID, "response": response, "status": "completed", "success": true,
      "evidence": []string{fmt.Sprintf("job %s completed by agent %s: %s", job.ID, job.AgentID, truncateEvidence(response))}, }`
Empirical probe (standalone program in /tmp/evtest, run because internal/ packages cannot be imported from outside the module):
  `raw = {"success":true,...,"evidence":["job j1 completed by agent coder: hello.txt"],"claims":["Created file hello.txt"]}`
  `err=json: cannot unmarshal string into .evidence.0 of type main.Evidence len(Evidence)=1 Evidence=[{Type: Subject: Value:}] Claims=["Created file hello.txt"]`
- why: The daemon's step-job result ALWAYS carries `evidence` as a []string of prose (components.go:7983), so `execResult.Evidence []models.Evidence` (tactical.go:995) decodes to a one-element slice holding a ZERO-VALUE Evidence, and the else-if at 1022 writes that artifact into step.Evidence (and persists it to the step's evidence column at 1025, so `[{}]` is now stored as the step's evidence). Therefore `len(step.Evidence) == 0` at 1040 is false for exactly the run-7 shape the gate was written for, and the gate is unreachable in production. This is the same decode artifact 2fcf2365 identified in review_manager.go — but the fix was applied to only ONE of the three affected predicates. Tests pass because tactical_claims_test.go pins the regex helper (claimsFileSideEffects) and never exercises the gate with a daemon-shaped envelope.
- claimed vs actual: Commit 58a2ff42: "a step whose parsed report claims file side-effects but carries NO tool-issued evidence is marked Validated=false with an explicit ValidationError ('unverified narration') instead of silently passing." Actual: the marking branch never runs for a daemon step job; the fabricated report silently passes (it is additionally laundered into Validated=true by the approval paths, finding 5).
- fix shape: Use the meaningful-evidence predicate at tactical.go:1040 (`!hasMeaningfulEvidence(step.Evidence)`), and stop assigning the decode artifact at 1022 (or filter zero-value entries there). Move hasMeaningfulEvidence to a shared helper so all three guards use one definition, and add a test that drives OnJobCompleted with the exact daemon envelope (evidence as []string + claims).
- parent verification: VERIFIED by parent: tactical.go:995 decodes evidence as []models.Evidence; internal/daemon/components.go:7983 emits []string. Independent probe (/tmp/evprobe) reproduced err='cannot unmarshal string into .evidence.0 of type main.Evidence' with len(Evidence)=1 holding a zero value, so len(step.Evidence)==0 is false.

### F9 [0] bb9d6609's "artifact claims with zero tool evidence" guard is dead for the same reason — only the 2fcf2365 guard was repaired
- file:line: internal/agent/review_manager.go:962 (vs the fixed guard at 945-947)
- evidence: review_manager.go:945-947 (repaired by 2fcf2365)
  `if !reviewHintIsConversational(step.ToolHint) &&`
  `    strings.Contains(result, "stopped after extended thinking") &&`
  `    !hasMeaningfulEvidence(step.Evidence) {`
review_manager.go:962 (unchanged by 2fcf2365)
  `if step.ToolHint != "" && !reviewHintIsConversational(step.ToolHint) && len(step.Evidence) == 0 {`
  `    if claimsArtifacts(result) {`
  `        rm.logger.Warn("Heuristic review: artifact claims with no tool evidence; refusing auto-approve", ...)`
- why: Identical defect to finding 2 on the heuristic-review predicate: step.Evidence is the zero-value artifact, so `len(step.Evidence) == 0` is false and the guard cannot refuse. Consequence chain: heuristicReviewPasses returns true → review_manager.go:198-212 sets StepApproved AND stamps `step.Validated = true` (d80b1295) → the trivial-task auto-approve path waves a zero-tool hallucination through as approved+validated. The commit's own regression narrative (run-7 coder with zero tool calls returning fabricated file_exists evidence) is therefore still live for <3-step tasks.
- claimed vs actual: Commit bb9d6609: "The heuristic now refuses auto-approve when an execution-hint step's report claims artifacts (file_exists / 'created …') while carrying no tool-produced evidence." Actual: the refusal is unreachable for job-driven steps (the only path where the report comes from the daemon envelope); the fix landed for the reasoning-only phrase but was not applied to the artifact-claim guard added in the same function.
- fix shape: Replace `len(step.Evidence) == 0` with `!hasMeaningfulEvidence(step.Evidence)` at review_manager.go:962; add a test that builds the step through the daemon envelope path (evidence []string) rather than constructing []models.Evidence directly.
- parent verification: VERIFIED by parent: review_manager.go:962 still uses len(step.Evidence) == 0 while :947 uses !hasMeaningfulEvidence.

### F10 [0] The newly-wired validator passes vacuously, so 'validated' is stamped for a step with zero verified evidence
- file:line: internal/validator/manager.go:67-92, internal/validator/filesystem.go:66-87, internal/agent/tactical.go:1058-1142
- evidence: internal/validator/filesystem.go:66-87
  `for _, ev := range step.Evidence { switch ev.Type { case models.EvidenceFileExists: ... case models.EvidenceFileHash: ... default: // Not a filesystem evidence type, skip`
  `    continue`
  `} }`
  `result.Valid = len(result.Errors) == 0`
internal/validator/manager.go:80-84
  `result := validator.Validate(ctx, step)`
  `if !result.Valid { ... return fmt.Errorf("validation failed: %s", ...) }`
  `return nil`
internal/agent/tactical.go:1058-1059 / 1142
  `if ts.validatorManager != nil { validationErr := ts.validatorManager.ValidateStep(ctx, step)` ... `step.Validated = true`
- why: "Valid" means "no entry of a recognized type failed", not "evidence exists": an empty Evidence list, the zero-value decode artifact (finding 2), or evidence of any non-file type (pending_change_created, api_response, shell_output, …) all reach `result.Valid = len(result.Errors) == 0` with zero errors → ValidateStep returns nil → tactical marks the step validated. So the run-7 case (zero tool calls, fabricated prose evidence) is scored as validated, and the record is indistinguishable from a step whose artifacts were really checked on disk. The daemon comment claims the constrained validator makes "a fabricated file_exists (run 7) fail the gate unless the artifact exists" — but run-7's fabrication was *narrated prose in a report*, and the validator never sees narrated prose at all (only step.Evidence, which is empty/artifact in that case).
- claimed vs actual: Commit 58a2ff42: "the existing ValidateStep gate on for the first time ... any file_exists/file_hash evidence in a code-hinted step must resolve on disk." Actual: the gate only re-checks evidence that is already present and typed; absence of evidence is treated as success, which is precisely the hallucination shape.
- fix shape: Make the validator (or the gate) fail-closed for side-effect-hinted steps: require at least one meaningful evidence entry when the step claims file work (combine with finding 5 by only stamping Validated when hasMeaningfulEvidence is true), or have ValidateStep return a distinct 'no evidence' verdict the gate treats as unverified rather than passing.
- parent verification: VERIFIED by parent: internal/validator/filesystem.go:66-86 sets result.Valid = len(result.Errors) == 0 with a `default: continue`; internal/validator/manager.go:75-84 returns nil. Zero-value evidence therefore passes.

### F11 [0] Any ReviewApproved verdict now stamps Validated=true — including policy auto-approvals and default-approve on unparseable reviewer output — and the same hunk clears the only unverified marker
- file:line: internal/agent/review_manager.go:512-526 (with 126-133, 241-245, 486-487); internal/agent/review.go:157-175
- evidence: review_manager.go:517-526 (f127e765)
  `if !step.Validated {`
  `    step.Validated = true`
  `    step.ValidationError = ""`
  `    if err := rm.stepStore.Update(step); err != nil { ... }`
  `}`
review_manager.go:126-132
  `if !policy.NeedsReview(step) { ... return &ReviewResult{ Status: ReviewApproved, Feedback: "Auto-approved (no review required)", Confidence: 1.0 }, nil }`
review_manager.go:241-245
  `return &ReviewResult{ Status: ReviewApproved, Feedback: fmt.Sprintf("Reviewer %s not available, auto-approved", reviewerID), Confidence: 0.5 }, nil`
review_manager.go:486-487
  `// Default to approved with low confidence if no clear signal`
  `return ReviewApproved, 0.3`
review.go:157-175 ShouldAutoApprove → ReviewApproved on a Description substring match ("*.md","update",…)
- why: The completion gate's own stated contract (tactical.go:1323-1326: "Validated is only set when a validatorManager ran") is inverted: after f127e765 every successfully-terminal step that passes through any approval path is stamped validated=true, so validated=false is the exception rather than the evidence signal. Three of those paths carry no evidence judgement at all: (a) NeedsReview false — an empty ToolHint (review.go:101 `return step.ToolHint != ""`) or a SkipReview hint, (b) reviewer registry miss, (c) unparseable reviewer output, which the parser defaults to APPROVED at 0.3 confidence. Additionally line 522 clears ValidationError unconditionally on approval, erasing the very marker 58a2ff42 introduced to keep unverified narration distinguishable downstream; because the claim gate itself is dead (finding 2) nothing else re-establishes it.
- claimed vs actual: Commit f127e765: "Unapproved/unverified paths (claim-vs-evidence marking, validation failures) still block as before." Actual: the claim-vs-evidence marking is unreachable (findings 2/3), and approval actively clears a previously-set validation failure marker, so no path blocks. The smallest input that defeats the gate: a step with an empty ToolHint, or a reviewer id absent from the registry → ReviewApproved → validated=true with zero evidence.
- fix shape: Stamp Validated=true only when the approving path had evidence to judge (hasMeaningfulEvidence(step.Evidence) or a real validator pass), and keep ValidationError intact when it is non-empty. At minimum stop clearing ValidationError in the approval branch and surface 'auto-approved without evidence' distinctly in the record.
- parent verification: VERIFIED by parent: review_manager.go:520-526 stamps Validated=true and clears ValidationError on every approval path, incl. :126-133 and :241-245.

### F12 [0] Automatic Stop() recovery signals a pid taken from the PID file with no token/identity validation (cross-instance kill + reused-pid kill), contradicting the wave's 'never signal an unvalidated pid' claim
- file:line: internal/llm/runtime_process.go:388-412 (recovery), :395-400 (validation applies only when asOperator), :423 (killProcessGroup), :235 (Start DOES compare the token)
- evidence: Ownership gate passes before the recovery: `if !asOperator && !p.spawnedByUs { return ErrRuntimeNotOwned }` (:384). Recovery then reads the pid file and validates ONLY for the operator path: `if asOperator { if mismatch := p.identityMismatch(entry.PID); mismatch != nil { ... } }` (:395-400), then unconditionally `proc, err := os.FindProcess(entry.PID)` (:401) → `p.cmd = &exec.Cmd{}; p.cmd.Process = proc` (:406-407) → `killProcessGroup(cmd, syscall.SIGTERM)` (:423). Compare the spawn path, which does validate identity: `if entry.Token == p.instanceToken` (:235) vs the observed-not-owned branch (:251-257). a341879a's own message: 'StopAsOperator verifies identity before signalling: the PID file outlives the process that wrote it, so its pid can have been reused ... The automatic Stop path is unchanged.' 3c4e4ebc's title claims 'never signal an unvalidated pid'.
- why: The pid file is per-endpoint in the shared run dir and OUTLIVES the process that wrote it. Two concrete unvalidated signals: (a) our own child died (the wait goroutine at :336-346 clears p.pid/p.cmd but deliberately leaves the pid file, and Stop's recovery is entered whenever `p.cmd == nil`), and another meept instance later rewrote that pid file for the same endpoint (second daemon, `meept runtime start`, eval harness) — Stop/StopAll then SIGTERMs the OTHER instance's live runtime, which the observed-not-owned guard exists to prevent; (b) the pid was reused by an unrelated process after our child died — the pid file still names it, so Stop SIGTERMs it, and killProcessGroupPID escalates to `kill(-pgid, sig)` when that reused pid leads its own group, taking down the whole unrelated group. Reachable automatically from StopAll (daemon.go:1904) on every clean shutdown and from the health-driven restart path (runtime_manager.go:659, :689 via RestartProvider). OLD behavior was byte-identical on this path, but the wave (i) added the process-alive probe + re-armed checker that actively drive the automatic stop/restart, and (ii) claimed the class closed.
- claimed vs actual: Claimed (3c4e4ebc): 'never signal an unvalidated pid'; a341879a claims reuse safety. Actual: only the operator path (StopAsOperator) and the sweep validate; the automatic path signals whatever pid the pid file names, and does not even compare the instance token that Start compares. Claim NOT satisfied.
- fix shape: In stopProcess's recovery branch, validate for ALL callers, not just asOperator: skip/refuse when `entry.Token != "" && entry.Token != p.instanceToken` (the pid file is not ours), and run identityMismatch on the recovered pid before signalling; treat 'pid not in the process table' / scan failure as 'nothing to signal' (clear the stale file) rather than 'signal anyway'.
- parent verification: VERIFIED by parent: internal/llm/runtime_process.go:384-412 - identityMismatch is called only when asOperator; the automatic path signals whatever pid the file names.

### F13 [0] 1. 270be60f removed the loopback seed AND the consumer unconditionally overwrites the server's loopback default, so `transport.http.enabled` with no `addr`/`port` binds ALL interfaces on an ephemeral port
- file:line: internal/daemon/daemon.go:1137 (with internal/config/schema.go:740-748, internal/comm/http/server.go:88, internal/comm/http/server.go:1027)
- evidence: internal/daemon/daemon.go:1136-1137 `httpCfg := http.DefaultServerConfig()` / `httpCfg.Addr = fullCfg.Transport.HTTP.ListenAddr()` — unconditional assignment. internal/config/schema.go:740-748 `func (c HTTPTransportConfig) ListenAddr() string { if c.Addr != "" { return c.Addr }; if c.Port > 0 { return fmt.Sprintf("127.0.0.1:%d", c.Port) }; return c.Addr }` — returns "" for the documented 'neither set' case. internal/comm/http/server.go:88 `Addr: "127.0.0.1:8081", // Bind localhost only (security fix)` is therefore clobbered with "". internal/comm/http/server.go:1027 `ln, listenErr := net.Listen("tcp", s.config.Addr)`. Go's own resolution: net/dial.go:878-889 `addrs, err := DefaultResolver.resolveAddrList(ctx, "listen", network, address, nil)` -> net/ipsock.go:255-285 `if addr != "" { ... }` ... `if host == "" { return addrList{inetaddr(IPAddr{})}, nil }` where inetaddr builds `&TCPAddr{IP: ip.IP, Port: portnum}` = nil IP + port 0, i.e. the wildcard address and an ephemeral port. Schema default at internal/config/schema.go:2589-2603: `HTTP: HTTPTransportConfig{ Enabled: false, // Addr intentionally left empty ... }`.
- why: Defect: an operator config that enables the HTTP transport without an explicit address (e.g. hand-edited meept.json5, or a config written before the shipped template gained `addr`) gets a TLS listener on 0.0.0.0:<random> instead of loopback. OLD behavior: at 84ce30f9 (the previous commit, whose whole purpose was 'default the HTTP transport to loopback instead of all interfaces') DefaultConfig seeded `Addr: "127.0.0.1:8081"`, so the same config bound loopback. 270be60f removed that seed and did not reintroduce a fallback at the assignment site, so the wave both (a) left 84ce30f9's loopback default inert and (b) turned the default into the wildcard bind 84ce30f9 was fixing. A random port also breaks every consumer that derives the endpoint (GUI, gui-daemon-connect.py, Makefile guidance).
- claimed vs actual: Commit 270be60f says 'ListenAddr resolves Addr > 127.0.0.1:<Port> > "" (the HTTP server then applies its own loopback default)' and 'Effective behavior is unchanged (the HTTP server defaults to the same 127.0.0.1:8081 when Addr is empty)'. docs/configuration/production-security.md (added by the same commit) states: '3. Neither set — the HTTP server binds its own loopback default, `127.0.0.1:8081`.' ACTUAL: the server's own default is never reached — daemon.go:1137 overwrites it with "", and net.Listen("tcp", "") binds all interfaces on port 0. The new test pins the false premise, not the behavior: internal/config/config_test.go:336-340 `if cfg.Transport.HTTP.Addr != "" { t.Errorf("expected HTTP addr to default to empty (server default applies) ...") }`.
- fix shape: Resolve at the assignment site: `addr := fullCfg.Transport.HTTP.ListenAddr(); if addr == "" { addr = httpCfg.Addr }` (keep the DefaultServerConfig loopback value), or make ListenAddr() return "127.0.0.1:8081" as its final fallback, and add a daemon-level test asserting that an enabled-http config with neither key yields a loopback Addr and never a wildcard/port-0 address.
- parent verification: VERIFIED by parent: internal/daemon/daemon.go:1137 assigns ListenAddr() unconditionally over DefaultServerConfig's 127.0.0.1:8081 (server.go:88); ListenAddr returns "" when both keys are empty (schema.go:740-748); net.Listen("tcp","") binds the wildcard on an ephemeral port. config_test.go:334-343 pins the false premise.

### F14 [0] 2. SetAutonomous(true) is applied to whatever loop the fallback branches pick — including the daemon's shared interactive AgentLoop — and is a one-way latch, silently disabling the file write review gate for all later chat turns
- file:line: internal/daemon/components.go:7856 (with internal/daemon/components.go:7839, 7847, 3057, 2583; internal/tools/builtin/filesystem.go:533; internal/tools/builtin/file_edit.go:389)
- evidence: internal/daemon/components.go:7838-7848 `if job.AgentID != "" && p.registry != nil { loop, err := p.registry.GetForTask(...); if err != nil { ... agentLoop = p.agentLoop } else { ... agentLoop = loop } } else { agentLoop = p.agentLoop }` then :7853-7857 `// Session-attached chat loops stay interactive — SetAutonomous is only reached on this job path.` / `if isStepJob { agentLoop.SetAutonomous(true) }`. The same pointer is the interactive loop: internal/daemon/components.go:3057 `jobProc := NewAgentJobProcessor(c.AgentLoop, logger)` and internal/daemon/components.go:2583 `c.ChatHandler = agent.NewChatHandler(c.AgentLoop, c.Dispatcher, msgBus, logger)` (also :2971). Only one call site exists and nothing clears it: `grep -rn "SetAutonomous" internal/` -> definition at internal/agent/loop.go:1356-1358 (`l.autonomous = autonomous`, field declared :846-853) and the single call at components.go:7856. Consumption: internal/agent/loop.go:6491 `if l.autonomous {` (injects tools.ContextWithAutonomous), and the gated staging blocks internal/tools/builtin/filesystem.go:533 `if t.pendingChangesRegistry != nil && !direct && !tools.AutonomousFromContext(ctx) {` and internal/tools/builtin/file_edit.go:389 (identical).
- why: Defect: a step job that carries a step_id but no agent id (or an agent id absent from the registry — both take the `agentLoop = p.agentLoop` branch) marks the process-wide interactive loop autonomous. Because the flag is never cleared, every subsequent interactive chat turn on that loop writes files directly instead of staging them, so changes.list/changes.accept (pending_change_created) is bypassed for interactive users — the preview/accept workflow AGENTS.md documents. OLD behavior: before 43ea4c29 the autonomous marker came only from the caller's context (tools.ContextWithAutonomous), so an interactive turn could never acquire it.
- claimed vs actual: Commit 43ea4c29 / the commit's own comment at components.go:7853-7854 claims 'Daemon job processor: step jobs mark their task-scoped loop SetAutonomous(true) — session-attached chat loops stay interactive.' The marker is applied to whatever the fallback selected, which for the two fallback branches is precisely the session-attached/main loop; and 'task-scoped' is not true of p.agentLoop.
- fix shape: Mark autonomy on the job's context (tools.ContextWithAutonomous around the job's RunOnce in the job processor) instead of on the loop; if the loop-level flag is kept, only call it on registry-created loops (never p.agentLoop) and clear it when the job finishes (defer agentLoop.SetAutonomous(false) / on task-loop release).
- parent verification: VERIFIED by parent: components.go:7838-7840 falls back to p.agentLoop, :7856 SetAutonomous(true); the same pointer is the ChatHandler loop (components.go:2583/:2971/:3057); SetAutonomous is never called with false anywhere.

### F15 [0] 3. The durable spawn record is written through pathutil.ExpandPath (os.UserHomeDir, MEEPT_HOME-blind) while the new sweeps scan config.MeeptPath("run"), so under MEEPT_HOME the record half of the orphan sweep never fires and a rig daemon writes pidfiles into the operator's real ~/.meept
- file:line: internal/pathutil/expand.go:11-15 (with internal/llm/runtime_config.go:149, internal/daemon/orphan.go:61, cmd/meept/doctor.go:378)
- evidence: internal/pathutil/expand.go:10-15 `func ExpandPath(path string) string { if strings.HasPrefix(path, "~") { homeDir, err := os.UserHomeDir()` — no MEEPT_HOME consultation anywhere in the function. internal/llm/runtime_config.go:149 `pidFile := pathutil.ExpandPath(cfg.PIDFile)`; the shipped pid files are tilde paths, e.g. config/models.json5:290 `"pid_file": "~/.meept/run/llama-general.pid",` (:125 mlx-classifier, :170 mlx-general, :237 llama-extract). The scans use the other resolver: internal/daemon/orphan.go:61 `records, err := llm.ScanSpawnRecords(config.MeeptPath("run"))`. Contrast internal/config/config.go:276-283, which DOES honor the override: `if override := os.Getenv(EnvMeeptHome); override != "" { override = expandTilde(override); ... if strings.HasPrefix(path, "~/"+DefaultHomeRel+"/") { return filepath.Join(override, path[len("~/"+DefaultHomeRel+"/"):]) } }`. Live evidence of the split on this host: `ls ~/.meept/run` -> `llama-general.pid`, `llama-general.pid.cmd` (the records live beside the tilde-resolved pidfile).
- why: Defect: with MEEPT_HOME set, RuntimeProcess writes the pidfile and its SpawnRecord under /Users/<operator>/.meept/run while the daemon sweep and `meept doctor` scan $MEEPT_HOME/run. The record-based source — added by this wave specifically because the config source is unreliable — is therefore always empty in an isolated rig, so a leftover runtime is unreapable there; and two daemons (rig + operator) share one pid directory, so the instance-token/ownership decisions cross the rig boundary. OLD behavior: before the wave there were no spawn records at all, so the sweep was config-only and the divergence did not exist; the wave introduced the writer/reader pair and wired them to two different homes.
- claimed vs actual: 799b0962 / 8b29dbdb and the wave notes claim the MEEPT_HOME leak is fixed ('Now resolves through config.MeeptPath'), and AGENTS.md names `config.MeeptHome()`/`MeeptPath()` as 'THE resolution points'. Within the same wave, the runtime lifecycle paths in internal/llm (pid_file, runtime logs) still resolve through os.UserHomeDir(), and the record scan was wired to the MeeptPath form.
- fix shape: Give internal/llm a MEEPT_HOME-aware expander (mirror the override branch of config.expandPath, as internal/llm/providers.go already mirrors userModelsConfigPath) and use it for pid_file/model_path/model_paths and the runtime log dir; derive the record scan directory from the expanded pid file (filepath.Dir(endpoint PIDFile)) rather than the fixed config.MeeptPath("run"); add a test that sets MEEPT_HOME and asserts pidfile == record dir == scan dir.
- parent verification: VERIFIED by parent: internal/pathutil/expand.go:10-15 uses os.UserHomeDir; internal/llm/runtime_config.go:149 expands pid_file with it; internal/daemon/orphan.go:61 scans config.MeeptPath("run").

### F16 [0] 4. The operator-stop identity guard is skipped entirely when no durable spawn record exists, so `meept runtime stop/restart` can signal an unrelated process from a stale pidfile
- file:line: cmd/meept/runtime.go:435-439 (with internal/llm/runtime_process.go:486-494)
- evidence: cmd/meept/runtime.go:434-439 `func runtimePIDConfig(pc *llm.ProviderConfig, pidFile string) *llm.RuntimeConfig { return &llm.RuntimeConfig{ PIDFile: pidFile, } }` — no SpawnCommand, no BaseURL. internal/llm/runtime_process.go:486-494 `func (p *RuntimeProcess) identityMismatch(pid int) error { var want []string; if p.config != nil { want = p.config.SpawnCommand }; if rec, err := ReadSpawnRecord(p.pidFile); err == nil && len(rec.Argv) > 0 { want = rec.Argv }; if len(want) == 0 { return nil }` — nil means 'cannot tell, nothing to compare', i.e. no refusal. Record writes are best-effort (the wave treats a write failure as a log line) and records did not exist before this wave.
- why: Defect: for any runtime spawned by a build older than this wave, or whose record write failed, `want` is empty and identityMismatch returns nil, so StopAsOperator sends SIGTERM/SIGKILL to whatever pid the stale pidfile names — the exact reused-pid accident the guard was added to prevent. The existing test only covers the record-present case, so the guard looks proven while being inert on the path the CLI actually takes.
- claimed vs actual: a341879a claims 'StopAsOperator verifies identity before signalling ... Identity comes from the durable record when one exists, else the config's spawn command; a mismatch is refused with an error naming both command lines instead of killing an unrelated process.' For the CLI, the 'else' branch is unreachable because the CLI supplies no spawn command, so identity is never verified.
- fix shape: Pass the provider's spawn command (or the registered RuntimeConfig) into the CLI stop path so `want` is always populated, and make identityMismatch fail closed — return an error saying identity cannot be verified — when neither a record nor a configured spawn command is available.
- parent verification: VERIFIED by parent: cmd/meept/runtime.go:434-439 supplies no SpawnCommand; runtime_process.go:486-494 returns nil when it has nothing to compare.

### F17 [0] Settings exit guard ignores the orchestrator-config editor (meept.json5) and the connection form, so those unsaved edits are still dropped silently
- file:line: ui/flutter_ui/lib/features/settings/settings_panel.dart:474 (guard predicate), 510-511 (_guardExit), 363 (editor mounted); ui/flutter_ui/lib/features/settings/orchestrator_config_editor.dart:76, 128, 293
- evidence: settings_panel.dart:474 `bool get _hasUnsavedConfigEdits => _hasChanges || _mainConfigDirty;` and :510-511 `Future<bool> _guardExit() async {\n    if (!_hasUnsavedConfigEdits) return true;`. The panel's own claim at :341-342: "// The shared back control and esc both ask this before leaving (the\n    // shell asks the same closure the registry holds), so unsaved\n    // meept.json5 (or client/models/menubar) edits are never dropped\n    // silently." But the same ListView always mounts the orchestrator editor: :363 `const OrchestratorConfigEditor(),` — which keeps its own flag and needs an explicit save: orchestrator_config_editor.dart:76 `bool _hasChanges = false;`, :128 `void _markChanged() { if (!_hasChanges) setState(() => _hasChanges = true); }`, :293 `else if (_hasChanges)` (the save button), :164 `await _client.saveOrchestratorConfig(payload);` writing "the orchestrator block in ~/.meept/meept.json5" (:311). The connection form is a second uncovered source: settings_panel.dart:664-686 ('save connection' button) and :201-251 `_saveConnectionSettings`.
- why: Both remaining edit sources live in the same panel but outside the dirty predicate, so `_guardExit` returns true with no dialog; the shell's back control / esc then disposes the panel and the orchestrator-block edits (a real meept.json5 change: parallel_phases, thresholds, model slots) are lost. OLD behavior: before this wave the panel had no exit dialog at all, so nothing regressed for these two sources — but the wave's stated deliverable ("ask before leaving a panel with unsaved config edits") is only half delivered, and the code comment now asserts a guarantee that is false. The wave's own tests cover only MainConfigEditor and the generic text editor (test/features/settings/main_config_editor_test.dart:446 group 'exit guard for the shared back control' and :539 'unsaved edits in the generic config editor also guard the exit'); nothing tests the orchestrator editor.
- claimed vs actual: Claimed: 'unsaved meept.json5 (or client/models/menubar) edits are never dropped silently' (settings_panel.dart:341-342) + commit 48c44461 'ask before leaving a panel with unsaved config edits'. Actual: meept.json5's orchestrator block is edited in that panel and is dropped silently by back/esc; only 2 of 4 edit sources consult the guard.
- fix shape: Give OrchestratorConfigEditor an `onDirtyChanged` callback (mirror MainConfigEditor) and OR it into `_hasUnsavedConfigEdits`; optionally include the connection form's own dirty flag the same way. Cleaner: move dirty tracking into one small provider (per-editor keys) that both editors write and the guard reads, so the predicate cannot drift again.
- parent verification: VERIFIED by parent: settings_panel.dart:474 predicate is `_hasChanges || _mainConfigDirty` only.

### F18 [0] The acceptance-PASS artifact does not exist: the only committed acceptance script computes the FAIL policy and writes FAIL
- file:line: tools/classifier-eval/results/m4-gold-acceptance.md:11 ; tools/classifier-eval/m4_gold_acceptance.py:83,108-120 ; tools/classifier-eval/results/m4-gold-acceptance.json:1-12
- evidence: m4-gold-acceptance.md:11: "| **tfidf-veto (ALT-5): A routes only when tfidf concurs** | 2 (2) | 46 | **87.35%** | **PASS** |". The committed script implements ONLY the double-confidence policy — m4_gold_acceptance.py docstring: "the ADOPTED double-confidence policy ... This is the FINAL-REPORT acceptance number", and it contains no veto/agreement branch; it ends `Path(.../results/m4-gold-acceptance.json).write_text(json.dumps({... "verdict": "PASS" if acc > 0.868 else "FAIL"}))`. The committed JSON therefore records `{"policy": "double-confidence", "a_routes": 7, "a_correct": 5, "chain": 41, "system_accuracy": 0.8456, "verdict": "FAIL"}`. `git log --oneline -- tools/classifier-eval/m4_gold_acceptance.py` = only `5fc03637`. The 87.35% row's only tool, alt_methods_sweep.py:149-157, prints to stdout and writes no artifact (`results["ALT-5 tfidf-veto on centroid"] = system_score(routes)` then `print(...)`), which alt-methods-correction.md:10-14 already calls out.
- why: The campaign's terminal claim (FINAL-REPORT.md:21 `| gold-replay acceptance | **87.35% PASS** ...`) cannot be recomputed from the repository. The only machine-readable acceptance artifact in git records the opposite verdict; the PASS number lives in a hand-authored markdown table cell whose producer is stdout-only and depends on an UNTRACKED corpus plus a live embed server. A number no one can re-derive is not evidence.
- claimed vs actual: Claimed: 87.35% PASS committed as "the acceptance artifact for 5fc03637" (7add6ce0 message). Actual: committed artifact m4-gold-acceptance.json = policy double-confidence, 0.8456, verdict FAIL; no committed script produces the veto row.
- fix shape: Commit a machine-readable per-policy results artifact produced by a committed script (add the veto-policy branch to m4_gold_acceptance.py so it runs BOTH policies and writes both rows), or strike the PASS row from the record and mark the veto verdict UNVALIDATED everywhere it is cited.
- parent verification: VERIFIED by parent: m4_gold_acceptance.py:83 CHAIN=0.868, :108-120 implements ONLY double-confidence and writes results/m4-gold-acceptance.json = {n:48, a_routes:7, a_correct:5, chain:41, system_accuracy:0.8456, verdict:"FAIL"}.

### F19 [0] The acceptance gate is arithmetically passable on a 1–2 case sample against a hard-coded constant
- file:line: tools/classifier-eval/m4_gold_acceptance.py:83,108,114 ; tools/classifier-eval/results/m4-gold-acceptance.md:10-11 ; internal/agent/tfidf_veto.go:12-19
- evidence: m4_gold_acceptance.py:83 `CHAIN = 0.868`; :108 `acc = (aok + bok + nchain * CHAIN) / len(s_texts)`; :114 `print(f"  VERDICT: {'PASS' if acc > 0.868 else 'FAIL'}")`. With n=48: 0 routes → 0.868 (FAIL, since the test is strict `>`); 1 route correct + 47 chain → (1+47×0.868)/48 = 0.8708 → PASS; 2 routes both correct → 87.35% PASS; 2 routes, 1 correct → 85.27% FAIL. Verified by arithmetic. alt-methods-correction.md:35-46 states the same sensitivity: "One flipped route moves the headline from 'beats the chain floor' to 'loses to it'". The in-code claim omits this: internal/agent/tfidf_veto.go:15-17 "Measured on the adjudicated gold replay: 87.35% system accuracy vs 84.56% without the veto — above the 86.8% chain-only floor".
- why: The verdict is decided by one of two route decisions at n=48, with 95.8% of the score coming from an unverified constant. That is not a policy comparison; it is one Bernoulli trial dressed as an accuracy measurement, and the sensitivity is absent from the FINAL-REPORT headline and from the code comment that cites it.
- claimed vs actual: Claimed: "tfidf-veto PASSES (87.35%)", a validated configuration. Actual: PASS requires exactly ≥1 correct route; identical policy with one route flipped = 85.27% FAIL. Effective sample = 2 cases.
- fix shape: Re-express the gate as a held-out measurement with a sample-size floor (e.g. require n_routes ≥ 20 and a binomial CI lower bound above the chain floor) and refuse to report a verdict when coverage is single-digit; add a regression guard test that fails the acceptance script on coverage < N.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F20 [0] Gold corpus and the acceptance ruler are not disjoint — a verbatim training case is one of the 48 scored replay cases (train-on-test)
- file:line: testdata/eval/classifier-adversarial-corpus.json5:165 ; tools/classifier-eval/m4_gold_acceptance.py:41-55,72-80 ; tools/classifier-eval/harvest_wave5.py:29
- evidence: Corpus line 165: `{ id: "h18-planexec-001", input: "[replay case 2c0434047ecad05e]", expected_intent: "quickplan", ... added_in: "iteration-18" ... }`. The untracked adjudicated ruler tools/classifier-eval/replay-gold.local.json5 contains an identical case ("[replay case 2c0434047ecad05e]", adjudicated: true). Read-only difflib comparison of all 48 replay texts against all 389 loadable corpus texts: 1 pair at similarity 1.000 (that pair), 1 pair at 0.894 (replay "[replay case 2a7a0eb076138ce4 prefix]meept.js" vs corpus h18-cfgops-001 at line 197 "add the projects section to the default config template"). The acceptance script fits the centroid head (m4_gold_acceptance.py:50-52) and, in the PASS policy, the tfidf logistic (alt_methods_sweep.py:117-121, `clf.fit([gold[i].text ...])`) on exactly that corpus. The wave-5 dedup guard only compares new corpus rows against the corpus (`if max_sim > 0.95: rejected`, harvest_wave5.py:29) — it never checks the replay ruler.
- why: At least one of the 48 acceptance cases was in the models' training set, so the acceptance number is contaminated by memorisation, and the documented process cannot detect the contamination. At a 2-route margin (finding 2) a single leaked case is the whole result.
- claimed vs actual: Claimed: independent "adjudicated real-traffic replay" ruler (FINAL-REPORT.md:25). Actual: ≥1/48 (4%) of the ruler is byte-identical to a committed training case; nothing in the pipeline screens for corpus↔replay overlap.
- fix shape: Add a corpus↔replay overlap guard (exact hash + cosine > 0.95) that runs before any acceptance measurement and refuses to score leaked cases; commit the guard so the ruler stays disjoint as the corpus grows.
- parent verification: VERIFIED by parent: testdata/eval/classifier-adversarial-corpus.json5:165 (h18-planexec-001, added_in iteration-18, whose own note says it matches 'the adjudicated replay gold for this verbatim text'); untracked tools/classifier-eval/replay-gold.local.json5 contains that exact string once.

### F21 [0] "SHIPPED" veto is unreachable from the shipped config, and the shipped gate is a different policy than the one measured
- file:line: config/meept.json5:636-645 ; internal/agent/embedding_prefilter.go:96-107,135-142,459-470 ; internal/agent/prefilter_veto_test.go:57-66 ; scripts/build_tfidf_veto.py:8,92-95
- evidence: Shipped config: `"classifier_prefilter": { "enabled": false, "base_url": "http://127.0.0.1:8090/v1", ... "threshold": 0.70, "assert_only": false, "centroids_path": "", "timeout_seconds": 2 }` — no `veto_path` key at all, and the whole Door-1 prefilter is disabled. Schema default is disable-by-empty: embedding_prefilter.go:135-142 `vetoPath := cfg.VetoPath; ... if vetoPath != "" { ... }`, schema.go:2408-2413 "Empty disables the veto (Door 1 routes on the kNN vote alone — legacy behavior)". The build script's default output is in-repo and not wired to the runtime: build_tfidf_veto.py:8 "internal/agent/testdata/prefilter_tfidf_veto.json  (in-repo default)", :94 `ap.add_argument("--out", default="internal/agent/testdata/prefilter_tfidf_veto.json")`; `git grep prefilter_tfidf_veto` finds only that script and a test that builds a fake model in t.TempDir; there is no Makefile/install target (Makefile has no veto/centroid target). The shipped gate's actual policy is kNN-5 unanimity @0.70: embedding_prefilter.go:96-107 `DefaultPrefilterThreshold = 0.70`, `defaultPrefilterK = 5`, "Unanimity is the precision instrument". Third test pins the disabled default: prefilter_veto_test.go:57-66 "verify the loading path does not panic when the file is absent (veto=nil) — the common case until build_tfidf_veto.py is run". No test pins the enabled state end-to-end (prefilter_veto_test.go:42 says "Simulate the routed branch's veto check directly" and only calls `p.veto.agrees(...)`). The measured policy is centroid margin 0.030 + sim 0.60 + veto (m4_gold_acceptance.py:93, alt_methods_sweep.py:140); the shipped policy is kNN unanimity with no margin gate.
- why: The accepted configuration and the shipped configuration are not the same program, and the shipped one has no replay measurement at all — so no acceptance result transfers to production. 'SHIPPED' describes dead code behind an unset knob; the docs' own gate list even rejects the head family in use (FINAL-REPORT.md:111 "k-NN majority heads of any k (E2E-negative)").
- claimed vs actual: Claimed: tfidf-veto "code SHIPPED" and "the acceptance-passing config must ship" (classification-architecture.md:174-175, FINAL-REPORT.md:99). Actual: enabled=false and veto_path absent in the only shipped template; the built model lands in the repo tree, not in $MEEPT_HOME; zero tests exercise Match-with-veto; zero replay measurements of the shipped kNN policy.
- fix shape: Add `veto_path` to the shipped template (documented value under $MEEPT_HOME), add a `make` target that both builds centroids + veto model into the home dir, and add one acceptance run of the exact shipped policy (kNN unanimity as configured) plus a test that fails if Match with a loadable veto does not suppress a disagreement.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F22 [0] FINAL-REPORT headlines a WITHDRAWN number (92.8% iter-16) and a wrong corpus count; the terminal artifact carries no corrections block
- file:line: docs/plans/classifier-iteration/FINAL-REPORT.md:21-24,35 ; tools/classifier-eval/results/iter-16-corrected/report.md:4-6,44-56 ; docs/plans/classifier-iteration/master.md:50 ; docs/plans/classifier-iteration/ITERATION-LOG.md:25
- evidence: FINAL-REPORT.md:22 `| synthetic E2E (3-stage cascade, iter 16) | 92.8% (bar was 91.78%) |` and :35 "3-stage cascade CLEARS bar (92.8%)"; :24 `| corpus | 139 base + 199 adversarial = 338 gold cases |`. But iter-16-corrected/report.md:4-6 "Status: **SUPERSEDES the verdict in results/iter-16/report.md**" and :44 `| 0.50 | 89 (87) | 52 (47) | 109 | 92.80% | **(87+47+0.868×109)/274 = 91.44%** | **FAIL**`, :52-56 "Verdict: M3 entry bar NOT MET ... The original 'clears bar' claim is withdrawn"; master.md:50 "**Iter-16's "E2E 92.8% clears the M3 bar" verdict is WITHDRAWN.**". The corpus row is also stale: `eval_harness.load_cases()` on the committed revisions returns base=139, adv=250 (total 389, 361 non-OOD, 28 OOD) at HEAD, so "199 adversarial = 338" is two waves behind; ITERATION-LOG.md:25 row 16 still reads "CLEARS BAR: E2E 92.80%" and was never corrected (5dd477cd patched only iter-19/20 rows).
- why: The campaign's final artifact re-asserts a number its own record withdrew, with no strike-through or footnote, so anyone reading FINAL-REPORT (or iterating on the campaign) inherits a false 'clears bar' belief and a wrong denominator. The correction discipline the wave itself documents (alt-methods-correction.md:1-5, rule 8) was not applied to the terminal report.
- claimed vs actual: Claimed: 92.8% clears the 91.78% bar; corpus 338. Actual: deterministic recompute 91.44% → FAIL (withdrawn); corpus 389 (361 scored / 28 OOD).
- fix shape: Append a CORRECTIONS block to FINAL-REPORT.md (strike 92.8% → 91.44% FAIL; fix the corpus row to 139+250=389 / 361 non-OOD) and add the same to ITERATION-LOG row 16, per the campaign's own append-only correction rule.
- parent verification: VERIFIED by parent: FINAL-REPORT.md:22 and :35 assert 92.8% 'CLEARS bar'; tools/classifier-eval/results/iter-16-corrected/report.md:44 computes 91.44% FAIL and :56 says the original claim is withdrawn; the corpus row (139+199=338) is wrong against the loader at HEAD (139+250=389).

### F23 [0] "Verbatim transcript text never enters git" is falsified by the committed corpus, and the provenance label understates real prompts
- file:line: testdata/eval/classifier-adversarial-corpus.json5:271,286 (and h20-015:282) ; tools/classifier-eval/harvest_wave5.py:104-106 ; docs/workflows/classification-architecture.md:206-214 ; docs/plans/classifier-iteration/FINAL-REPORT.md:126-128
- evidence: Committed rows: line 271 `{ id: "h20-004", input: "continue implementing with subagents", ... added_in: "harvest-20260910", source: "hermes-inspired" }`; line 286 `{ id: "h20-019", input: "use the colors of the german flag for the site theme", ... source: "hermes-inspired" }`. Read-only grep of the user's Hermes transcripts: "continue implementing with subagents" appears in 5 files including ~/.hermes/sessions/session_20260527_174045_f06b89.json; "use the colors of the german flag for the site theme" in session_20260327_222716_9b3a31.json; "[replay case 2c0434047ecad05e]" in 2 files (and that string is also corpus row 165 and the acceptance ruler case). Docs assert the opposite invariant: classification-architecture.md:206-208 "The directory is gitignored BEFORE any text is written: verbatim message text never enters git", :210-211 "corpus with provenance `added_in: "harvest-YYYYMMDD"`, `source: "live-session"`", FINAL-REPORT.md:126-128 "verbatim text never enters git"; harvest_wave5.py:35 stamps `"source": "hermes-inspired"`.
- why: The privacy claim that protects the campaign's most sensitive input (the user's own live traffic) is false, and the machine-readable provenance deliberately mislabels real live prompts as 'inspired', so a reviewer cannot tell which corpus rows are private traffic. It also contradicts the documented harvest convention (`source: "live-session"`), leaving the corpus unfilterable by provenance.
- claimed vs actual: Claimed: verbatim text never enters git; harvest rows carry source "live-session". Actual: ≥3 rows are byte-identical live user messages from ~/.hermes/sessions, committed and labelled "hermes-inspired".
- fix shape: Either redact/paraphrase live-derived rows before commit and record `source: "live-session"` accurately, or amend the invariant in the docs and inventory the affected rows; add a commit-time check for high-similarity matches against ~/.hermes/sessions (the harvest tool already has the join).
- parent verification: VERIFIED by parent: corpus rows 271 (h20-004 'continue implementing with subagents') and 286 (h20-019 'use the colors of the german flag for the site theme') are committed with source 'hermes-inspired'; both strings exist verbatim in ~/.hermes/sessions (session_20260527_174045_f06b89.json and session_20260327_222716_9b3a31.json).

### F24 [0] d382a91c's `go list` tag-filter silently drops whole packages from vet/staticcheck/U1000/mutexio/selflock — including the tag-gated magefiles (real build code) and any package `go list` cannot load
- file:line: .githooks/pre-commit-vet:60, .githooks/pre-commit-staticcheck:70-76, .githooks/pre-commit-u1000:70-72, .githooks/pre-commit-mutexio:84, .githooks/pre-commit-selflock:99
- evidence: vet:60 `if ! go list "$pkg" >/dev/null 2>&1; then` / `continue`; staticcheck:71 `if go list "$pkg" >/dev/null 2>&1; then` (filtered out otherwise) then :78-81 `if [ -z "$PACKAGES" ]; then echo -e "${GREEN}✅ No packages to check${NC}"; exit 0`; u1000:70 same `go list` guard; mutexio:84 and selflock:99 same guard. Measured skip set at HEAD (`python3 /tmp/audit_skipset.py`, 140 dirs with .go files): `./magefiles -> package github.com/caimlas/meept/magefiles: build constraints exclude all Go files`; only other miss is `./sdk/go` (nested module, already excluded by the pre-existing nested-module filter). magefiles holds 7 files (build.go, docs.go, gui.go, install.go, test.go, utils.go, docs_test.go) — real, compiled build tooling — and is now invisible to staticcheck, U1000, mutexio and selflock for every commit that stages magefiles/*.go. The predicate is broader than the stated intent: `go list` also fails on a conflicting package clause, e.g. in a scratch module `go list ./pkg5` (one good a.go + a _test.go declaring `package wrongname`) → `found packages pkg5 (a.go) and wrongname (a_test.go) in /tmp/golist-test/pkg5`, exit 1. pre-commit-build excludes `_test.go` (:27), so that package's real non-test code is then never vet/staticcheck'd by any hook, and the hook prints no skip notice at all.
- why: Two independent gate-integrity failures: (1) real code (magefiles) is permanently excluded from 4 analyzers — only `go vet -tags mage ./magefiles` runs, and only when magefiles itself is staged; (2) the guard is `go list` exit-0 rather than 'excluded by default build tags', so any package whose load fails is dropped silently and the gate reports success ('✅ go vet passed - no issues found' / '✅ No packages to check') for code it never analyzed. The comment claims a narrow, justified exclusion; the mechanism is a broad, silent one.
- claimed vs actual: Commit d382a91c: 'let the analyzer and vet hooks skip packages the default tags exclude' and each hunk comment 'Skip a package the default build tags exclude (magefiles is //go:build mage)'. Actual: the check is `go list <pkg>` exit status, which is also non-zero for packages that fail to load for unrelated reasons, and the skip is silent. magefiles/*.go is type-checked only by pre-commit-build's `go vet -tags mage ./magefiles` (:64-69); staticcheck/U1000/mutexio/selflock never see it.
- fix shape: Filter on the actual condition instead of load success: e.g. `go list -e -f '{{if .Error}}{{.Error.Err}}{{end}}'` and skip only when the error is 'build constraints exclude all Go files', or check `.IgnoredGoFiles` vs `.GoFiles`; and analyze tagged code with the tag (`staticcheck -tags mage ./magefiles`) instead of dropping it. At minimum, print a visible `SKIP <pkg>: <reason>` line so a skipped package can never look like a pass.
- parent verification: VERIFIED by parent: .githooks/pre-commit-vet:60 `if ! go list "$pkg" >/dev/null 2>&1; then continue`.

### F25 [0] ebaecc2f fixed only the staged-package build: the downstream build in the same hook still writes a stray 86 MB binary into the working tree (reproduced)
- file:line: .githooks/pre-commit-build:126 (and :91-133)
- evidence: New staged-package loop (:77-83) uses `if ! PKG_OUTPUT=$(go build -o /dev/null "$pkg" 2>&1)` — verified no artifact for library and main packages (`go build -o /dev/null ./internal/config` exit 0, `go build -o /dev/null ./cmd/meept` exit 0, nothing written). Section 2 still uses a bare multi-package build: :126 `if ! OUTPUT=$(go build $(printf '%s' "$DOWNSTREAM") 2>&1); then`. Go writes an executable only when exactly one package main is named, so a single-element DOWNSTREAM litters the tree. Reproduced in a throwaway clone at HEAD: `printf '\n// audit: trivial staged change\n' >> internal/daemon/daemon.go && git add … && bash .githooks/pre-commit-build` → hook printed `✅ go build passed — staged packages + clean downstream importers compile` (EXIT=0) and left `-rwxr-xr-x 1 caimlas staff 86341282 … meept-daemon` in the repo root. Reachable because internal/daemon's only importer is cmd/meept-daemon (measured: `internal/daemon -> cmd/meept-daemon`, plus internal/agent/q, internal/benchmark, internal/configui → cmd/meept; internal/sharedclient/menus → cmd/meept-lite; tools/analyzers/*/*). It is invisible because .gitignore:2 `/meept-daemon` hides it (git status stayed clean).
- why: The commit's stated property ('without a stray binary in the staged-package build') holds only for section 1; section 2 in the same file re-introduces the artifact, overwriting any ./meept-daemon present and littering the tree on ordinary commits (staging internal/daemon, internal/agent/q, internal/benchmark, internal/configui, internal/sharedclient/menus).
- claimed vs actual: Commit ebaecc2f: 'type-check tag-gated magefiles without a stray binary in the staged-package build' and the new comment at :73-76 ('A plain `go build` writes an executable for a `package main` (littering the tree …)'). Actual: the downstream build at :126 is exactly that plain `go build` and does write an executable.
- fix shape: Apply the same form to the downstream set: iterate DOWNSTREAM and run `go build -o /dev/null "$pkg"`, or build into a temp dir (`go build -o "$(mktemp -d)/" $DOWNSTREAM`). Optionally assert `git status --porcelain` is unchanged before exiting so a future write is caught.
- parent verification: VERIFIED by parent: .githooks/pre-commit-build:126 `go build $(printf '%s' "$DOWNSTREAM")` with no -o.

### F26 [0] pre-commit-dart-format validates the working tree, not the index: it PASSES while the staged blob (what gets committed) is unformatted
- file:line: .githooks/pre-commit-dart-format:53-54, :61, :93
- evidence: Hook collects staged paths (:53-54 `git diff --cached --name-only --diff-filter=ACMR | grep -E "^${DART_ROOT}/.*\.dart$"`), then :61 `[ -f "$f" ] || continue` and :93 `OUT="$("$DART_BIN" format --output=none --set-exit-if-changed "${FILE_ARR[@]}" 2>&1)"` — dart formats/reads the files on disk. Reproduced in a scratch git repo: staged blob `void main(){print(  "unformatted" );}` (confirmed via `git show :ui/flutter_ui/a.dart`), then formatted the working-tree file only (`dart format ui/flutter_ui/a.dart`), then ran the real hook from /Users/caimlas/git/meept → `Checking 1 file(s): ui/flutter_ui/a.dart` … `PASS: all 1 Dart file(s) checked are formatted.` HOOK EXIT=0, while `git show :ui/flutter_ui/a.dart` still returned `void main(){print(  "unformatted" );}`. The same command on the clean tree is green (`make fmt-check-gui` → `Formatted 175 files (0 changed)`, exit 0), so a commit made through that path lands unformatted Dart and a fresh checkout/CI immediately fails `make lint-ci`.
- why: The gate's whole purpose is to stop unformatted Dart entering history; it inspects content that is not the content being committed, so the failure mode it exists to prevent goes straight through. The identical working-tree-vs-index gap exists in pre-commit-build, but there it is documented as imprecision; here the documented contract is explicitly about the staged blob.
- claimed vs actual: .githooks/README.md:51 `| 17 | pre-commit-dart-format | Staged Dart files are dart-format clean |` and the hook header (:5-7) 'this hook enforces it on STAGED files only'. Actual: it checks the staged file's working-tree content.
- fix shape: Validate the index blob: `git show ":$f" | "$DART_BIN" format --output=show - | diff - <(git show ":$f")` (or write `git show ":$f"` to a temp file, run the formatter on that temp path, and report the staged path). Keep the staged-list collection as is.
- parent verification: VERIFIED by parent: the file list comes from `git diff --cached` (:53-54) but the check runs dart format on the working-tree paths (:61, :93).


## MEDIUM-HIGH (2)

### F27 [0] The observability pipeline discards the Door-1 verdict it was built to capture; two dashboard views are structurally empty/mis-bucketed
- file:line: internal/agent/embedding_prefilter.go:34-45,463-470 ; internal/agent/dispatcher.go:3502,3564-3589 ; tools/classifier-eval/harvest_outcomes.py:88-98,102-108 ; internal/agent/prefilter_verdict_test.go:300-313
- evidence: Go side captures the distinction: embedding_prefilter.go:44 `Suppressed bool // H6 gate or quickplan cue-guard suppression` with the comment "Suppressed (not abstained) so the observer can distinguish the two"; :463-470 emits `Suppressed: true, // veto suppression: distinguish from abstain`. But the persisted row carries no verdict: dispatcher.go:3564-3586 sets only `var pfMargin *float64; ... Margin: pfMargin, TurnNo: turnNo, Outcome: "pending"`, and on an abstain/suppression ClassifierMethod is the chain's — dispatcher.go:3502 "classifier_method intentionally stays empty for dispatches whose intent carries no Method", pinned by prefilter_verdict_test.go:308-313 "abstained row method = %q, want the chain's method". The harvest then reconstructs a 3-way verdict it cannot observe: harvest_outcomes.py:88-98 `WHEN classifier_method = 'embedding_prefilter' AND intent_type = '' THEN 'suppressed' ELSE 'abstain'` under the doc line "split by verdict (routed / abstain / suppressed)". The `'suppressed'` branch is unreachable, so cue-guard suppressions (the rows the quickplan analysis most needs) are indistinguishable from ordinary abstains. Separately harvest_outcomes.py:102-108 `v_fallback_trend ... WHERE classifier_method = 'fallback'` counts a method no production code writes — `grep -rn '"fallback"' --include=*.go internal/` yields only internal/metrics/store_test.go:125 and a comment at dispatcher.go:124.
- why: The L2/L3/L4 pipeline is the evidence source for the whole outcome loop; it claims routed/abstain/suppressed granularity and a fallback-traffic trend, but the storage layer drops the flag and the views report categories that can never be populated. Any conclusion drawn from v_margin_hist or v_fallback_trend (e.g. 'the gate is at optimum') rests on empty or merged buckets.
- claimed vs actual: Claimed (5b2b32e4, test name TestDispatcher_PrefilterCueSuppressedMarginPersisted): "the verdict survives gate suppression too". Actual: only the margin float survives; the verdict flag is dropped at persistence, so 'suppressed' is unreachable and 'fallback' never appears.
- fix shape: Add a `prefilter_verdict` column (routed/abstain/suppressed/veto_suppressed) written from the stashed PrefilterVerdict, and drop or fix v_fallback_trend to filter methods that are actually emitted; add a test asserting a cue-suppressed row is distinguishable in the DB, not just in Go.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F28 [0] In-loop outcome labels are absence-of-evidence proxies, and the 'corrected' rows feed corpus candidates
- file:line: internal/agent/dispatcher.go:41-45,3591-3609 ; tools/classifier-eval/harvest_outcomes.py:45-67 ; docs/plans/classifier-observability/design.md (S2, per code comment)
- evidence: dispatcher.go:45 `const reRouteWindow = 3`; :3591-3603 "A same-agent follow-up resolves the prior row 'ok'; a different-agent dispatch within reRouteWindow turns marks it 'corrected' (design.md S2 proxy for \"user re-asked\")"; the ok label is therefore set by default whenever a same-agent turn follows within 3 turns. The views built on it are presented as accuracy: harvest_outcomes.py:45-55 `CREATE VIEW v_door_accuracy ... ROUND(1.0 * SUM(CASE WHEN outcome = 'ok' ...) / COUNT(*), 4) AS ok_rate`, :58-67 v_correction_rate. The corpus-candidate query selects outcome in ('corrected','failed_replan').
- why: `ok` means 'no different-agent dispatch seen within 3 turns' — it cannot detect a wrong route the user worked around, and it can over-count `corrected` for ordinary multi-agent sequences (coder → debugger on a new request). Because corrected rows become the next corpus anchors, the bias is not merely display: it propagates into future gold data.
- claimed vs actual: Claimed (3/L3 message, architecture doc): "accuracy is measurable in-loop via harvest_outcomes.py". Actual: the only computed rate is an agent-switch proxy with one-sided sensitivity, and it is the harvest's candidate generator.
- fix shape: Rename the columns/views to correction_proxy, require an explicit user-correction predicate (e.g. within-turn re-ask of the same text or an explicit re-route) before a row may become a corpus candidate, and state the proxy's sensitivity in the view comment and the doc.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).


## MEDIUM (10)

### F29 [0] 5. 'websearch' in DefaultAlwaysFullTools never matches the registered tool name 'web_search', so a core tool still ships stubbed under the default indexed schema mode — the same defect b733ad18 fixed for json_extract, left unrepaired in the same list
- file:line: internal/config/schema.go:1472 (with internal/tools/builtin/tool_web_search.go:136, internal/tools/registry.go:211-218, 279-284, internal/daemon/components.go:5784)
- evidence: List: internal/config/schema.go:1470-1473 `return []string{ "shell", "file_read", "file_edit", "file_write", "memory_search", "memory_store", "web_fetch", "websearch",`. Registered name: internal/tools/builtin/tool_web_search.go:136 `func (t *WebSearchTool) Name() string { return "web_search" }`, registered at internal/daemon/components.go:5784 `registry.Register(webSearchTool)` (Registry keys by tool.Name()). Normalization on both sides is only lower+trim, so the spellings never meet: internal/tools/registry.go:152-157 `func normalizeToolName(name string) string { return strings.ToLower(strings.TrimSpace(name)) }` used by internal/tools/registry.go:279-284 `func isAlwaysFullTool(alwaysFull map[string]struct{}, name string) bool { _, ok := alwaysFull[normalizeToolName(name)]; return ok }`, while the set is built from the config list at internal/tools/registry.go:211-218 `set[strings.ToLower(strings.TrimSpace(name))] = struct{}{}`. Because it misses, internal/tools/registry.go:260-263 replaces the parameters: `if indexed && !isAlwaysFullTool(alwaysFull, name) { description = indexedStubDescription(name, description); params = emptyObjectParameters() }`. The repo already documents the split: internal/daemon/deterministic_wiring_test.go:25-27 `// The search tool registers under "web_search" (snake_case); the // "websearch" spelling only appears in config lists like // DefaultAlwaysFullTools.` The agent that consumes it is granted `web_search` (config/agents/researcher/AGENT.md).
- why: Defect: under indexed mode (the default; internal/agent/loop.go:1157-1162 default branch -> SchemaModeIndexed) web_search is presented to the model as a tool_view one-liner with an empty parameter schema, so the model must pay an expansion round-trip before every search — the exact failure b733ad18 documents for json_extract ('the small driver model answered partial instead of paying the expansion round-trip'). If gbnf_constrained is on, the generated grammar uses the same stubbed definitions (internal/llm/client.go GrammarForTools(chatOpts.tools)), so search arguments become grammar-restricted to an empty object. OLD behavior: n/a — the mismatch predates the wave; it is reported as an unclosed defect in the wave's claim, not as a regression.
- claimed vs actual: b733ad18's message and internal/config/schema.go's doc claim the list 'keeps its full parameter schema under indexed schema mode' for the curated core tools. b733ad18 added json_extract correctly but pinned the list only against a hardcoded copy (internal/config/schema_tools_test.go), so no test can see the registry-name mismatch and web_search stays stubbed.
- fix shape: Change the entry to "web_search" (or accept both spellings in normalizeToolName, e.g. fold '_'/'-'), and add a test that walks DefaultAlwaysFullTools() against a real registry, failing on any entry with no matching tool name.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F30 [0] 6. scripts/gui-daemon-connect.py still derives the endpoint from `transport.http.addr` only, so the `transport.http.port` alias this wave introduced is silently dropped on the operator-facing connect path (`make gui-connect-check`, endpoint, probe)
- file:line: scripts/gui-daemon-connect.py:464 (with :400-412)
- evidence: scripts/gui-daemon-connect.py:461-464 `def effective_endpoint() -> dict: http = read_http_block() ... host, port = parse_addr(str(http.get("addr", "")))` and :400-412 `def parse_addr(addr: str) -> tuple[str, int]: raw = (addr or "").strip(); if not raw: return DEFAULT_HOST, DEFAULT_PORT`. The file contains no reference to the alias (grep for 'port' returns only DEFAULT_PORT, comments and usage text). The Go side resolves precedence differently: internal/config/schema.go:740-748 (`Addr` > `127.0.0.1:<Port>` > ""), consumed at internal/daemon/daemon.go:1137.
- why: Defect: an operator who configures only `transport.http.port: 18095` (documented in docs/configuration/production-security.md by the same wave) gets a daemon on 127.0.0.1:18095 while the connect tooling checks/probes 127.0.0.1:8081 and Makefile:144's gui-connect-check guidance ('cannot discover the daemon's transport.http.addr') reports a mismatch. Silent two-sided divergence — the same class the alias was created to close. OLD behavior: before the alias existed, a port-only config bound the seeded Addr default, so both sides agreed at 127.0.0.1:8081.
- claimed vs actual: 270be60f claims 'Every consumer resolves through it: internal/daemon/daemon.go (httpCfg.Addr and the boot log helper).' That holds for the Go consumers but not for the shipped operator tooling, so the alias is only partially wired.
- fix shape: In effective_endpoint()/parse_addr, fall back to the port alias when addr is empty and port > 0 (mirroring the Go precedence), and add a port-only case to the script's tests/self-check.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F31 [0] 7. `meept doctor --fix` misreports the orphan check: candidates dropped by the reaper's pre-signal validation are counted as survivors (non-zero exit), and a failed re-scan (0/0) is counted as a full repair
- file:line: cmd/meept/doctor.go:219 (with internal/llm/runtime_sweep.go:195-204, 236-240, 249-252)
- evidence: cmd/meept/doctor.go:216-226 `candidates, confirmed := llm.ReapOrphanRuntimesFromConfigsAndRecords(orphanCfgs, orphanRecords, 2*time.Second, slog.Default())` / `if len(confirmed) == len(candidates) { c.detail += fmt.Sprintf(" [stopped %d/%d orphans]", ...); c.ok = true } else { ... c.ok = false }`. The first return value is the FULL detection list, while the second contains only pids that were validated and observed gone: internal/llm/runtime_sweep.go:195-204 `orphans, err := OrphanRuntimesFromConfigsAndRecords(cfgs, records); if err != nil { ...; return nil, nil }; return orphans, ReapRuntimeProcesses(orphans, ...)`, and inside the reaper internal/llm/runtime_sweep.go:249-252 `info, ok := live[t.PID]; if !ok || info.PPID != 1 || info.Command != t.Command { log.Debug("orphan sweep: pid entry changed since detection; skipping", "pid", t.PID); continue }`, plus internal/llm/runtime_sweep.go:236-240 `live, scanErr := scanByPID(list); if scanErr != nil { ...; return nil }`.
- why: Defect: a pid that is already gone (or whose command changed) between detection and validation is dropped from `confirmed` without being recorded anywhere, so len(confirmed) < len(candidates) and doctor prints '[stopped N/M orphans; K survived]' with ok=false and a non-zero exit for a survivor that does not exist. Conversely a scan failure inside the reaper returns (nil, nil) — 0 == 0 — which doctor marks repaired. Both are the 'misreport runtime state' class 84235715 set out to remove. OLD behavior: before the wave the check did not report counting semantics at all; the wave introduced the N/M comparison that is now wrong.
- claimed vs actual: 84235715 claims the check is 'repaired only when every candidate is confirmed gone; survivors keep it failing and are counted.' Dropped/skipped candidates are counted as survivors, and an unverifiable scan is counted as a repair.
- fix shape: Return the dropped pids (or a (considered, reaped, skipped) triple) from ReapOrphanRuntimesFromConfigsAndRecords and count only genuinely surviving candidates in doctor; treat a reaper scan failure as an explicit 'could not verify' state (ok=false, distinct detail) instead of 0/0.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F32 [0] 8. The 'loopback-only main config read/write' claim covers only the write: GET /api/v1/config/main has no loopback check and returns the raw meept.json5, which carries the transport API keys
- file:line: internal/comm/http/main_config_handlers.go:45 (with internal/comm/http/main_config_handlers.go:115-128 and config/meept.json5:33)
- evidence: Write has the check: internal/comm/http/main_config_handlers.go:65-73 `func (s *Server) handleSaveMainConfig(w http.ResponseWriter, r *http.Request) { ... if !isLoopbackRequest(r) { s.writeError(w, http.StatusForbidden, "config write is restricted to loopback clients"); return }`. Read does not — it discards the request: internal/comm/http/main_config_handlers.go:45 `func (s *Server) handleGetMainConfig(w http.ResponseWriter, _ *http.Request)` and writes the payload straight through (:51-56). The payload is the verbatim file: internal/comm/http/main_config_handlers.go:17-21 `type mainConfigPayload struct { Path string; Content string; Writable bool }` fed by config.ReadMainConfig() (unredacted text). The file carries credentials: config/meept.json5:33 `"api_keys": [],            // Valid API keys. Empty = use default dev key (see docs).` and this host's live config has the transport block with `"addr": "127.0.0.1:18099"` in the same file. Auth layering is real (the mux is wrapped by s.middleware at internal/comm/http/server.go:974, which builds the API-key/store middleware at :1541-1556), so exposure requires a valid key — not loopback.
- why: Defect: the commit message promises loopback-only READ and WRITE; only the write is restricted. Any authenticated non-loopback client (and in multiuser mode any user key, including a non-owner) can read every credential the main config holds, while the same client cannot write it. Also note the loopback test itself trusts r.RemoteAddr (internal/comm/http/main_config_handlers.go:119 `host, _, err := net.SplitHostPort(r.RemoteAddr)`) rather than a proxy header — that part is safe against header spoofing (unlike internal/comm/http/auth.go:264-276 extractClientIP, which trusts X-Forwarded-For/X-Real-IP) — but it means the write becomes remotely reachable if the daemon is ever fronted by a loopback reverse proxy, since every proxied request then arrives from 127.0.0.1. OLD behavior: at cf527d44 the only reader was GET /api/v1/config/memory, whose handler also had no loopback check, but no endpoint claimed loopback-only read.
- claimed vs actual: Commit bc481e55 title: 'model/agent usage on metrics/live and loopback-only main config read/write'. The shipped docs are more careful than the commit (docs/reference/http-api.md, docs/reference/http-api-complete.md, menubar/README.md all say 'loopback clients only' on the POST row only), so the defect is the commit-level over-claim plus the unredacted credential read.
- fix shape: Either add the same isLoopbackRequest gate to handleGetMainConfig (matching the commit's claim), or redact secret-bearing keys (transport.http.api_keys, provider credentials) from the read payload and correct the commit/doc language to 'write restricted to loopback'.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F33 [0] 9. HealthChecker.run's deferred write to the shared `running` flag can clear a newer run's flag, after which Stop() no-ops and Start() can arm a second concurrent checker for the same endpoint (cross-package: internal/llm, surfaced by the wave's runtime-lifecycle changes)
- file:line: internal/llm/health_checker.go:78-82 (with internal/llm/health_checker.go:202-209)
- evidence: internal/llm/health_checker.go:77-82 `func (h *HealthChecker) run(ctx context.Context, stopCh <-chan struct{}) { defer func() { h.mu.Lock(); h.running = false; h.mu.Unlock() }()` — the run goroutine now owns the flag. internal/llm/health_checker.go:202-209 `func (h *HealthChecker) Stop() { h.mu.Lock(); defer h.mu.Unlock(); if h.running && h.stopCh != nil { close(h.stopCh); h.stopCh = nil; h.running = false } }` — Stop only acts when the flag is set. The restart path does Stop-then-Start on the same endpoint, so a stale run finishing after the new Start clears the new run's flag.
- why: Defect: if the previous run is still inside a health GET (bounded by HealthTimeout) when Start arms a new stopCh, the stale goroutine's defer sets running=false while the new channel is live. A later Stop() then sees running==false, never closes the active channel, and the checker polls for the daemon's lifetime; a later Start() likewise no longer sees a live run and arms an extra one. OLD behavior: before 3c4e4ebc the flag was written only by Start/Stop under the mutex, so a run goroutine's exit could not clear a newer run's flag.
- claimed vs actual: 3c4e4ebc claims 'The "stopped" flag becomes "running", cleared by the run goroutine itself; Stop closes only the active run's channel' and 'only Stop ends it (and StopAll calls Stop for every endpoint)'. Clearing the flag from the run goroutine is exactly what lets Stop miss the active channel, so 'only Stop ends it' does not hold.
- fix shape: Give each run a generation token and only clear the shared state when it still owns it, e.g. inside the mutex `if h.stopCh == stopCh { h.stopCh = nil; h.running = false }`, so a stale run's exit cannot clear a newer run's flag; add a restart-race test.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F34 [0] A committed measurement artifact was rewritten in place, destroying the previous verified result
- file:line: tools/classifier-eval/results/m4-silver/cascade-validation.json (diff in commit 7add6ce0) ; tools/classifier-eval/results/alt-methods-correction.md:1-5 ; tools/classifier-eval/m4_gold_acceptance.py:115
- evidence: `git show 7add6ce0 -- tools/classifier-eval/results/m4-silver/cascade-validation.json` changes an existing results JSON: `-  "routes": 11, -  "correct": 8, -  "precision": 0.727` → `+  "routes": 9, + "correct": 7, + "precision": 0.778`; `- "stageC_chain": 33, - "expected_system_accuracy": 0.8468, - "tau": 0.147` → `+ "stageC_chain": 34, + "expected_system_accuracy": 0.844, + "tau": 0.152`; the `misses` list is replaced (first miss changes from `["A","[replay case 2c0434047ecad05e]","code","plan"]` to `["B","commit, omit the .env ...","git","code"]`). That is the same file 5dd477cd cited as the source of the tau 0.147→0.152 correction, i.e. the artifact was retro-fitted to the corrected number. The campaign's own rule (results/alt-methods-correction.md:1-5) is the opposite: "The original commit and its message are left untouched — history is preserved; this file is the correction of record", reinforced by skill rule 8 ("Never edit a committed results JSON"). Structurally the harness writes straight into the tracked results dir — m4_gold_acceptance.py:115 `Path(".../results/m4-gold-acceptance.json").write_text(...)`, eval_harness.py:63 `RESULTS = ... / "results"` — so any re-run silently overwrites the record.
- why: The pre-edit silver validation (11 routes / 0.8468) is unrecoverable from the repo, so the campaign's later numbers cannot be compared to the earlier verified ones, and the record of what was believed at the time is gone. This is the same failure mode the wave documented and then committed.
- claimed vs actual: Claimed: 7add6ce0 "refreshed ... silver cascade-validation after the 389-case corpus run". Actual: an append-only-record violation that silently rebased a verified result without a strike-through or footnote.
- fix shape: Make result files write-once (timestamped filenames from the harness), keep the old JSON untouched and add a correction file; add a pre-commit guard that rejects modifications to existing results/*.json.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F35 [0] Wave-5 analysis misreports the corpus composition behind its 'key number'
- file:line: tools/classifier-eval/results/wave5-confusion.md:3-5 ; tools/classifier-eval/wave5_confusion.py:12-14 ; docs/plans/classifier-iteration/FINAL-REPORT.md:24
- evidence: wave5-confusion.md:3 "Corpus: 389 classified cases (358 gold + 31 base overlap dedup)" and :5 "Analysis over ALL classified rows (not per-fold): 61 wrong routes total". Running the committed loader (the method the campaign's own rule mandates) gives a different decomposition: base=139, adversarial=250, total=389, non-OOD=361, OOD=28, and 0 duplicate texts ("distinct case keys: 389 ... duplicate-text cases: 0"). 358 was the pre-wave-5 total (219+139), not a component of the current corpus, and there is no 31-case 'base overlap dedup'. The same commit's own message says "61 wrong routes at m=0.030 over the 389-case corpus".
- why: The denominator of the analysis that decided 'the gate is at optimum, margin stays 0.030' is described incorrectly (a grep-style count plus an invented dedup block), so the 61/55/6 numbers cannot be reproduced or audited by anyone using the documented loader, and the base-corpus contribution to the confusion (139 rows) is hidden.
- claimed vs actual: Claimed: 389 = 358 gold + 31 base overlap dedup. Actual: 389 = 250 adversarial + 139 base, 0 overlaps.
- fix shape: Regenerate the composition line from eval_harness.load_cases() output (print len(base)/len(adv)/ood) inside wave5_confusion.py and correct the doc; adopt the loader as the only count source everywhere (already the stated rule).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F36 [0] The 'gate is at optimum' decision rests on in-sample margins from a leaky fit
- file:line: tools/classifier-eval/wave5_confusion.py:30-51 ; tools/classifier-eval/results/wave5-confusion.md:19-36 ; tools/classifier-eval/results/wave5-confusion.md:54
- evidence: wave5_confusion.py:24 `C = np.stack([V[...intents[i]==l].mean(axis=0) for l in labs])` then :30-39 scores every gold case `sims = C @ V[i]` — i.e. each scored case is inside the mean that produced its own class centroid, with no fold split. The doc admits the shape ("Analysis over ALL classified rows (not per-fold)") and draws the campaign decision from it: "**55 of 61 wrong routes (90%) have margin < 0.030**", "median wrong margin: 0.0076", "Margin 0.030 is doing its job ... The gate is at/near its optimum for this corpus", "Margin stays 0.030". The declared harness convention everywhere else is 5-fold cross-evaluation with a tracked fold cache (eval_harness.py:8-24, fold-assignment.json).
- why: Self-inclusion inflates each case's own-class similarity and therefore the margin: the wrong-route margin distribution is optimistically wide, so '90% already gated' and 'raising the gate buys ~nothing' are not the same statement as they would be under the campaign's own per-fold protocol. The sensitivity of the no-change decision to that bias was never measured.
- claimed vs actual: Claimed: "gate is at optimum, residual is semantic" as a measured finding. Actual: an in-sample argmax analysis (not the protocol used for every other number), un-rerunnable without :8090 + the untracked corpora.
- fix shape: Re-run the confusion analysis per fold (score each case with its test fold's centroids) and report both distributions; only then state the gate-optimum conclusion.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F37 [0] Measured (sklearn) and shipped (Go) veto models are different functions, with no parity test and a train/inference feature mismatch
- file:line: internal/agent/tfidf_veto.go:12-19,120-126 ; scripts/build_tfidf_veto.py:80 ; tools/classifier-eval/alt_methods_sweep.py:117-129 ; internal/agent/testdata/prefilter_tfidf_veto.json (untracked)
- evidence: The 87.35% number comes from sklearn: alt_methods_sweep.py:117-121 `TfidfVectorizer(analyzer="char_wb", ngram_range=(2,4), min_df=1, sublinear_tf=True) ... LogisticRegression(max_iter=2000, C=10.0)` fitted on `gold` (base+adversarial). The shipped inference is a hand-written re-implementation that deliberately drops a training feature: tfidf_veto.go:120-126 "sublinear-tf log compression is skipped at inference (documented trade-off ... the veto only needs argmax stability, not calibrated probabilities)" while scripts/build_tfidf_veto.py:80 `tf = 1.0 + math.log(c)  # sublinear tf (sklearn sublinear_tf=True)`. No test compares Go output to the Python model: the committed veto tests build synthetic one-feature models (tfidf_veto_test.go:27-50, prefilter_veto_test.go:19-23). The only real model on disk is untracked and reports train-set accuracy as a quality signal: header `{"built_at": "2026-09-11T01:00:00...", "corpus": ["testdata/eval/classifier-adversarial-corpus.json5"], "train_docs": 202, "train_accuracy": 0.9901, ...}` — 202 docs (adversarial only, not the base+adversarial corpus the accepted policy was fitted on), and `train_accuracy` is logged at startup (embedding_prefilter.go:143-146 `"train_accuracy", veto.trainAcc`) as if it were validation.
- why: The published effect size was produced by a different implementation and a different training corpus than the code that would run in production; the shape is unverified (log-tf present in training, absent at inference) and the only quality figure surfaced to operators is train accuracy.
- claimed vs actual: Claimed: the Go implementation of the measured tfidf-veto (commit 10fe6d6d "Char n-gram TF-IDF logistic (stdlib Go inference ...)"). Actual: no parity evidence, a documented feature asymmetry, an untracked model trained on a smaller corpus, and a train-accuracy log field.
- fix shape: Add a golden parity test that loads a committed (or fixture) model and asserts Go argmax matches a recorded Python prediction set for N corpus prompts; align the inference features with training (or retrain without sublinear_tf) and rename the log field to train_accuracy_insample.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F38 [0] Working tree mutates the eval gold (agent labels) and adds an untracked guard that derives gold labels from the implementation
- file:line: testdata/eval/classifier-adversarial-corpus.json5 (uncommitted diff, 7 rows) ; internal/agent/classifier_lanes_test.go:58-70 (untracked) ; internal/agent/llm_classifier.go (uncommitted, +120/-40) ; internal/agent/testdata/prefilter_tfidf_veto.json (untracked, 2,057,746 B)
- evidence: `git status --porcelain` → ` M testdata/eval/classifier-adversarial-corpus.json5`, ` M internal/agent/llm_classifier.go`, `?? internal/agent/classifier_lanes_test.go`, `?? internal/agent/testdata/prefilter_tfidf_veto.json`. The corpus diff changes 7 rows' `expected_agent: "reviewer"` → `"coder"` (h10-review-001..005, h19-qp-021, h20-qp-029), e.g. `-    { id: "h10-review-001", ... expected_intent: "review", expected_agent: "reviewer", ...}` → `+ ... expected_agent: "coder",`. The untracked guard asserts gold agent labels against the shipped route table: classifier_lanes_test.go:65-70 `if got := agentForIntent(p[1]); got != p[2] { t.Errorf("%s: agentForIntent(%q) = %q, want the corpus agent %q", ...) }`, over goldCorpora including the adversarial corpus (:18-22).
- why: The gold corpus is not frozen: an uncommitted edit changes what any re-run of the acceptance/centroid pipeline would read (so the committed numbers no longer equal what the tree computes), and the new guard makes `expected_agent` a function of the implementation — i.e. the agent half of the gold becomes unfalsifiable, while the centroid index copies that field straight into production routing (scripts/build_prefilter_centroids.py:202 `"agent": agent`).
- claimed vs actual: Claimed: corpus is a stable campaign artifact with per-case provenance (fold cache + added_in). Actual: 7 rows are mid-edit in the live tree and the intended end state is 'labels match the code'.
- fix shape: Freeze the corpus for measurement: land the relabel as its own commit with a stated adjudication reason, pin corpus content by hash in the acceptance scripts, and have the lanes guard compare the corpus against a committed route table rather than being used to edit the gold.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).


## MED (31)

### F39 [0] ralph cap publishes task.failed with keys its only subscriber cannot read - the user sees an empty failure
- file:line: internal/agent/ralph_loop.go:353-366 vs internal/agent/handler.go:1446-1512
- evidence: ralph_loop.go: `payload, err := json.Marshal(map[string]any{"task_id": taskID, "reason": reason, "source": "ralph_loop"})` ... `Topic: "task.failed"`; handler.go:1447-1456 decodes `TaskID/Name/FailedJobs/CompletedJobs/TotalJobs/LinkedSessions/Error/FailedStep`.
- why: `reason` is not a field of the subscriber's struct, and `linked_sessions` is absent, so the push falls to the broadcast branch with Name="" and Error="" -> formatTaskFailedMessage renders '## task failed: 

**progress:** 0/0 steps completed, 0 failed' and the notification reads ' failed:  (0/0 steps completed)'. AGENTS.md's honest-completion contract ('a failed task's terminal event carries the error text') is violated on this path.
- claimed vs actual: d8833a60 claims the task is 'terminalized as failed' with a durable record; the operator/log record is durable but the user-facing event is empty.
- fix shape: Publish the handler's key set (name, error, failed_jobs, completed_jobs, total_jobs, linked_sessions) or emit the task.completed {status:failed, result:...} shape the task path already uses.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F40 [0] inputContainsGitVerb matches substrings, re-opening the run-10 misroute the guard was written to close
- file:line: internal/agent/dispatcher.go:4013-4032 (guard consumed at :1291)
- evidence: `for _, kw := range []string{"commit", "push", "pull", "merge", "branch", "rebase", "revert", "checkout", "stash", "cherry-pick"} { if strings.Contains(lower, kw) { return true } }` with guard `(intent.Type == string(IntentGit) && !inputContainsGitVerb(input)) && (isSecondPersonWorkRecall(input) || isWorkStatusRecall(input))`
- why: No word boundaries: 'did the emergency change get made?' contains 'merge' -> inputContainsGitVerb=true -> the git verdict survives arbitration and the committer runs contextless again; same for 'emerged'. The same wave fixed exactly this class for the meridiem ('\b[0-9]{1,2}(:[0-9]{2})?\s*(am|pm)\b', dispatcher.go:4005) but not here.
- claimed vs actual: 38fcb08f claims git verdicts without git verbs no longer survive recall arbitration. False for any input whose English contains 'merge'/'branch'/'push'/'pull' as a substring; no test in the wave covers a non-verb containing one of the keywords.
- fix shape: Match with word boundaries (`\bcommit\b|...`) and add a negative case ('emergency', 'emerged') plus the positive run-10 string.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F41 [0] Arbitration branch order makes the recall route unreachable for imperatives
- file:line: internal/agent/dispatcher.go:1280-1292
- evidence: `if (intent.Type == string(IntentPlatform) || (intent.Type == string(IntentSchedule) && !hasTimeSignal(input))) && hasLeadingImperativeVerb(input) { ... intent = nil } else if (...) && (isSecondPersonWorkRecall(input) || isWorkStatusRecall(input)) { ... return IntentRecall }`
- why: The imperative branch is tested first and both matchers are true for opens like 'update me: did the file get created?' (hasLeadingImperativeVerb=true, isWorkStatusRecall=true). The first branch fires, sets intent=nil, records platform_action_arbitration, and the session-aware recall route added by 16f1f8a2/c6e6f336 never runs - the turn degrades to keyword/heuristic/quickplan instead of recall.
- claimed vs actual: 16f1f8a2 claims 'recall arbitration covers time-signal-free schedule verdicts'; c6e6f336's test set uses questions without leading imperatives, so the claim holds only for non-imperative openings.
- fix shape: Evaluate the recall matchers before the imperative override (a status question outranks an imperative-looking first token) and pin 'update me: did X get created?'.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F42 [0] Compound collapse classifies real work intents (search/analyze/report) as chat-like and silently drops the second work item
- file:line: internal/agent/dispatcher.go:2177-2199
- evidence: `case string(IntentChat), string(IntentPlatform), string(IntentRecall), string(IntentReport), string(IntentSearch), string(IntentAnalyze): chatLike++` ... `if actionable == 1 && chatLike >= 1 && nonChatIntent != nil { ... multi.IsCompound = false; multi.CompoundType = "" }`
- why: IntentSearch/IntentAnalyze/IntentExplore route to the research/explore agents (intent.go:136-139) and are work, not conversation. 'search the web for X and write it to notes.md' -> [search, write] -> actionable==1, chatLike>=1 -> collapsed to a single intent, the search half never runs (previously compound -> orchestrator with two steps). DetectCompound itself only excludes chat/platform (:2000), so the collapse undoes the verdict the same file deliberately kept.
- claimed vs actual: b0816ffa claims 'genuine multi-work compounds are unaffected'; only code+debug is pinned, so does not hold for analyze/search/report pairings.
- fix shape: Restrict the chat-like arm to IntentChat/IntentPlatform/IntentRecall (or require the surviving intent to be an executable lane), and pin search+write and analyze+write.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F43 [0] Chunking's pending-only gate runs after Plan already promoted root steps, so the chunker's primary target is never split
- file:line: internal/agent/orchestrator_chunking.go:100-108 vs internal/agent/orchestrator.go:381-386 and internal/agent/strategic.go:594-596
- evidence: orchestrator_chunking.go: `// Only pending steps are chunk candidates.` / `if step.State != task.StepPending { ...; continue }`; orchestrator.go:381-386 calls `o.chunkToExecutorCapacity(ctx, req.TaskID)` AFTER `o.strategic.Plan(...)` returns; strategic.go:594-596 `// Promote root steps (no dependencies) to ready` / `sp.stepStore.PromoteReadySteps(req.TaskID)`.
- why: Every dependency-free step is StepReady by the time the chunker looks, so with a one-step plan there are zero candidates and the oversized root step (the step the chunker exists to split) is skipped. The doc comment at orchestrator.go:377-379 ('Proactively chunk oversized steps ... before tactical scheduling kicks in') is falsified; the guard eliminated the crash by disabling the feature for the steps that matter, not by distinguishing 'has a live job' from 'ready'.
- claimed vs actual: 3d2f738b's stated fix ('only splits steps still in StatePending - steps handed to tactical scheduling carry live jobs') is literally true but silently disables initial-pass chunking for all root steps; the change is undocumented.
- fix shape: Gate on the actual collision proxy (step.JobID == "" / a 'not yet scheduled' predicate) or run the chunker before PromoteReadySteps.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F44 [0] Reply-guard choke point does not cover the task-event push paths
- file:line: internal/agent/handler.go:964-969 vs :1387-1440 (handleTaskCompleted) and :1500-1505 (handleTaskFailed)
- evidence: handler.go:969 `response.Reply = applyReplyGuard(response.Reply)` with the comment 'Guard here - the LAST writer before persistence/push - so every reply path (direct, routed, sync-wait, platform fast path) is covered'; handleTaskCompleted builds `reply := h.formatTaskCompletedMessage(...payload.Result...)` and calls `h.sendResponse("task-completed-"+payload.TaskID, response)` without any guard.
- why: applyReplyGuard has exactly two call sites (handler.go:969 in handleRequest, loop.go:2636 in RunOnce). Every bus-driven push (task.completed, task.failed, review.completed, collab/pair results) assembles its reply from raw step results and publishes it through sendResponse -> publishChatMessage, so a roster/JSON-shaped step Result still reaches WS/HTTP clients as a chat_message bubble - the A2 failure class the guard was written for.
- claimed vs actual: 60052632 claims 'reply guard at single choke point ... covering every reply path'. False for the push paths; no test drives a push path.
- fix shape: Apply the guard inside sendResponse/publishChatMessage (the true last writer) and add a test that asserts the published payload for handleTaskCompleted is sanitized.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F45 [0] Anti-hallucination nudge claims 'nudge ONCE' but has no latch
- file:line: internal/agent/loop.go:4767-4777
- evidence: `// Don't fail the turn: nudge ONCE with an explicit tool-use instruction ...` / `if l.unbackedSideEffectClaims(response.Content) && !l.turnExecutedFileTools() { if iteration < l.config.MaxIterations { ... conv.AddUserMessage("[system: Your response claims you created or modified files ...]"); l.publishIteration(conversationID, iteration); continue } }`
- why: There is no per-turn latch (grep for antiHalluc/nudge latch: none; resetTurnGuards at loop.go:3571-3588 only clears turnToolCalls). A model that cannot emit tool calls and repeats the claim is re-nudged every iteration up to MaxIterations (loop.go:45 DefaultMaxIterations = 25), i.e. the budget burn the comment says must not happen - and each nudge also re-broadcasts iteration telemetry.
- claimed vs actual: 3da3cf7a's comment explicitly promises a single nudge ('repeated nudges ... would burn the iteration budget'). The code does not implement the promise and loop_antihallucination_test.go never counts nudges.
- fix shape: Add a per-turn `antiHallucinationNudged bool` reset in resetTurnGuards, and assert one nudge across N iterations.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F46 [0] The clause-start check in unbackedSideEffectClaims is dead code because the suffix list contains the empty string
- file:line: internal/agent/loop.go:2991-2994 and :3044-3054
- evidence: `var claimBoundarySuffixes = []string{ "", "[", "(", ... }` and `for _, suf := range claimBoundarySuffixes { if strings.HasSuffix(before, suf) { bounded = true; break } }`
- why: strings.HasSuffix(s, "") is always true, so `bounded` is unconditionally true and 'mid-clause verbs are descriptions, not claims' never applies; only the passiveFollowers map filters. Plain descriptive text ('The build generated three artifacts in dist/', 'the run updated nothing') now trips the guard, producing false nudges and, at the budget edge, a bogus '[unverified: no tools were executed this turn] ' prefix on a correct answer.
- claimed vs actual: 3da3cf7a claims 'Clause-anchored matching keeps hypotheticals ... out'. The anchoring is inert; the test passes only through the passive-voice map.
- fix shape: Handle the clause-start case explicitly (`if before == "" { bounded = true }`) and drop "" from the suffix list; add a mid-clause verb test.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F47 [0] The tfidf-veto Door-1 check is unreachable by default: veto_path has no producer
- file:line: internal/agent/embedding_prefilter.go:132-146 + internal/config/schema.go:2413
- evidence: embedding_prefilter.go: `// Loaded ONLY when cfg.VetoPath is set — an empty path disables the veto so Door 1 routes on the kNN vote alone` / `vetoPath := cfg.VetoPath; var veto *tfidfVeto; if vetoPath != "" { ... }`; schema.go:2413 `VetoPath string `json:"veto_path" toml:"veto_path"``.
- why: grep over the tree finds no writer of the key: no entry in the classifier_prefilter block of config/meept.json5, no default in internal/config, no daemon override (the daemon copies cfg.Orchestrator.Prefilter verbatim). The previous revision (10fe6d6d) auto-loaded config.MeeptPath("prefilter_tfidf_veto.json"); 511e47fb removed that loader and shipped no way to set the replacement, so a feature the wave marked 'SHIPPED' runs only if an operator hand-writes an undocumented key. The committed 2MB model at internal/agent/testdata/prefilter_tfidf_veto.json is read by nothing but the training script.
- claimed vs actual: 10fe6d6d/511e47fb claim working tfidf-veto plumbing; the plumbing exists but is unreachable at runtime, and docs/workflows/classification-architecture.md still describes the old MEEPT_HOME auto-load contract.
- fix shape: Ship the key in the config template + schema defaults (or restore the MEEPT_HOME default path) and correct the doc.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F48 [0] Combined cap: the sync ceiling (110s) is below the wave's own measured task time (112s)
- file:line: internal/agent/handler.go:1868-1882
- evidence: `// 110s (was 90s, e2e run 5 2026-09-10): a healthy 4-step task on the local 8B model took ~112s; the 90s bound returned "still running" while the task finished 22s later, failing A4's reply-content check.` / `syncTaskWaitTimeout = 110 * time.Second`
- why: The measurement cited as the reason for the raise exceeds the new bound, so the exact A4 shape still trips it; the sibling comments were not updated (handler.go:54 '90s sync-reply bound', ralph_loop.go:337 '10-minute wait'), so the code now disagrees with its own documentation.
- claimed vs actual: 1d9f67f2/2c36b6b2 claim the bound was raised to cover the measured task; it was raised to just under it.
- fix shape: Derive the bound from the same measurement basis as the client timeout (or set it above 112s with headroom) and fix the two stale comments.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F49 [0] Two wave tests pin nothing: the chunking guard test and the agent-override test never call the code they claim to cover
- file:line: internal/agent/orchestrator_chunking_guard_test.go:29-53, :55-72 and internal/agent/dispatcher_platform_override_test.go:22-36
- evidence: orchestrator_chunking_guard_test.go pins a hard-coded local `[]task.StepState{StepPending, StepScheduled, ...}` list and asserts StepPending is in it; the budget test re-implements the clamp locally (`if budget < minExecutorBudget { budget = minExecutorBudget }`). dispatcher_platform_override_test.go asserts constants and `IntentType(string(IntentPlatform)).ShouldDispatchAsync(false)` plus `_ = strings.TrimSpace("")`.
- why: Neither test constructs a Dispatcher/Orchestrator nor calls chunkToExecutorCapacity / RouteToAgent, so reverting orchestrator_chunking.go:102 or the `&& !result.AgentOverrideApplied` guard at dispatcher.go:2255 leaves the suite green - dead tests that read as live coverage.
- claimed vs actual: 3d2f738b claims 'Pins: orchestrator_chunking_guard_test.go (pending-only vocabulary, budget floor...)'; d409be61's pin is explicitly static and defers the behavioral half to the e2e smoke run.
- fix shape: Drive the real entry points (chunkToExecutorCapacity with a seeded store; ClassifyAndRoute with an agent override) and assert the observable effect.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F50 [0] The claim-vs-evidence branch returns from OnJobCompleted before the step is made terminal — the step stalls and the task can never complete
- file:line: internal/agent/tactical.go:1050-1054 (step state set only at 1201-1206)
- evidence: tactical.go:1050-1054
  `// Do NOT fail the step (the work may genuinely be done by`
  `// non-file tools or already-present artifacts); mark it so`
  `// downstream consumers can tell unverified narration from a`
  `// validated result, and let the normal completion flow proceed.`
  `return nil`
The normal flow it skips: step state is only set at 1201-1206 (`SetState(step.ID, task.StepCompleted)`) / the review block at 1158-1199, and the step was put in StepScheduled at scheduleStep (738).
- why: `return nil` aborts the whole handler: no SetState, no PromoteReadySteps/ScheduleReadySteps (1272-1288), no IncrementCompletedJobs (1231), no task finalization (1315+), and no `task.step_completed` event. The step stays StepScheduled (non-terminal) forever, AreAllCompleted (1295) is false, allStepsTerminalWithFailures is false, so the task never leaves its active state and waitForTaskCompletion delivers only the degraded ceiling reply. Latent today because finding 2 keeps the branch unreachable, but the two must be fixed together: fixing the predicate alone makes this stall the DEFAULT behaviour for every hallucinated-report step. The comment's stated intent ('let the normal completion flow proceed') is the opposite of what the code does.
- claimed vs actual: Commit 58a2ff42 comment: "mark it so downstream consumers can tell unverified narration from a validated result, and let the normal completion flow proceed." Actual: the branch returns early, so nothing proceeds.
- fix shape: Do not return: fall through to the normal completion path after marking (or, if the verdict must block, perform a state transition — e.g. SetState(StepNeedsInfo) plus a task-level event — so the task finalizes instead of hanging).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F51 [0] Validation max-retries is documented as 'mark step as needs_info for human review' but sets no state — now reachable because the validator was wired
- file:line: internal/agent/tactical.go:1122-1131
- evidence: 1122 `// Max retries exceeded - mark step as needs_info for human review`
1123 `ts.logger.Warn("Validation max retries exceeded", "step_id", step.ID, "retry_count", step.ValidationRetryCount, "max_retries", maxRetries,)`
1128 `if err := ts.stepStore.Update(step); err != nil { ts.logger.Warn("failed to persist step", ...) }`
1131 `return validationErr // Don't proceed to completion`
- why: No SetState is performed; the step keeps StepScheduled and the function returns an error to orchestrator.handleJobCompleted (internal/agent/orchestrator.go:430), which only logs it. The step never becomes terminal, the task never finalizes, and the sync reply degrades to the ceiling text. This dead path became live in this wave: 58a2ff42 wired ValidatorManager for the first time (its own message: "ValidatorManager was constructed nowhere in the daemon"), so `ts.validatorManager != nil` is now true on the daemon path (internal/agent/tactical.go:397 ← components.go:2763/2763-2770) and validation failures are now reachable — combined with finding 8 they fire on legitimate work.
- claimed vs actual: Code comment claims a needs_info state transition; nothing transitions. Commit 58a2ff42 claims the gate is now live and bounded (retries via MaxValidationLoops); the failure terminus (no state change, error swallowed by the bus handler) is unchanged and unbounded.
- fix shape: In the max-retries-exceeded branch call `ts.stepStore.SetState(step.ID, task.StepNeedsInfo)` (or StepFailed) before returning, and have the orchestrator's handler treat an OnJobCompleted error as a step-level failure rather than a log line.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F52 [0] Wiring the validator turned two successful tool outcomes into guaranteed validation failures (delete_file and list_dir evidence)
- file:line: internal/tools/builtin/filesystem.go:745-750 and 906-915 vs internal/validator/filesystem.go:68-73, 91-103
- evidence: DeleteFileTool (filesystem.go:745-750) — after `os.Remove(resolved)` succeeded:
  `evidence := []models.Evidence{ models.NewEvidence( models.EvidenceFileExists, resolved, "deleted", t.Name(), ), }`
ListDirectoryTool (filesystem.go:906-915):
  `models.NewEvidence( models.EvidenceFileExists, resolved, fmt.Sprintf("entries=%d,recursive=%v,truncated=%v", ...), t.Name(), )`
Validator (validator/filesystem.go:68-73):
  `case models.EvidenceFileExists:`
  `    ... } else if err := v.validateFileExists(ev.Subject); err != nil {`
  `        result.Errors = append(result.Errors, fmt.Sprintf("file not found: %s", ev.Subject))`
validateFileExists (91-103): `info, err := os.Stat(path) ... if info.IsDir() { return fmt.Errorf("path is a directory, not a file") }`
Validator table (validator/manager.go:35-36): `"list_dir": NewFilesystemValidator(), "file_delete": NewFilesystemValidator()`
- why: EvidenceFileExists is used by those tools to mean 'the path's state was observed', not 'the path exists'. A step that deletes a file emits file_exists for a path that no longer exists → os.Stat fails → validation error; a step that lists a directory emits file_exists for a DIRECTORY → info.IsDir() → validation error. With the gate now live (finding 7's wiring) those steps fail, consume 2 retries, and then hit the no-state-change terminus (finding 6) — a stall on a correct execution. This is the newly-reachable false-positive surface of the wiring; nothing pins it (validator/integration_test.go only uses handwritten evidence).
- claimed vs actual: Commit 58a2ff42: the wired validator "checks evidence subjects against the REAL filesystem, so a fabricated file_exists fails the gate unless the artifact exists." Actual: it cannot distinguish 'observed then removed' or 'is a directory' from a fabrication, so real successes fail while fabricated prose (which never reaches the validator) passes.
- fix shape: Introduce distinct evidence types (path_absent / dir_observed) or have the validator treat EvidenceFileExists with value "deleted" and directory subjects as observations rather than existence assertions; add pins for delete_file and list_dir evidence through ValidateStep.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F53 [0] The claimed 'validator constrained to the session's project root' does not exist: AllowedDirs is never set and relative evidence resolves against the daemon's CWD
- file:line: internal/daemon/components.go:2763-2765; internal/validator/filesystem.go:42-45
- evidence: components.go:2763 `c.ValidatorManager = validator.NewValidatorManagerWithLogger( logger.With("component", "step-validator"))` — no AllowedDirs / config argument anywhere in the daemon.
validator/filesystem.go:42-45 `func (v *FilesystemValidator) isPathAllowed(path string) error {`
  `    if len(v.allowedDirs) == 0 { return nil }`
Grep: `NewFilesystemValidatorWithConfig` / `AllowedDirs` have zero non-test callers in internal/ and cmd/.
Commit 58a2ff42's own comment admits: "Relative evidence paths resolve against the validator process working directory — scratch-daemon runs set this to the project dir, so step-relative paths check the right tree."
- why: The security-bounding claim under which the gate was approved is unimplemented, so the live validator will stat any absolute path on the host (no rejection of out-of-project subjects), and relative subjects are validated against the daemon process CWD — the exact fallback AGENTS.md forbids ('Never use os.Getwd() as a project/working-directory fallback in daemon code'). Step working directories are per-session (resolveStepWorkingDirFor, components.go:7677+); the validator has no access to them, so a legitimate relative subject can be reported 'file not found' (false failure → findings 7/8) or a same-named file in the daemon's CWD can satisfy the check (false pass).
- claimed vs actual: Commit 58a2ff42: "A step-scoped filesystem validator constrained to the session's project root ... Bounded: paths outside the project are REJECTED as evidence, not followed." Actual: allowedDirs is empty for every production validator, isPathAllowed returns nil unconditionally, and the resolution base is the daemon's CWD.
- fix shape: Build the validator with FilesystemValidatorConfig{AllowedDirs: []string{projectRoot}} from the step/session working dir at scheduling time, or make the validator run against the step's resolved working dir; fail closed when no root is resolvable.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F54 [0] Tool evidence is drained without any flush against the subscriber goroutine (WaitGroup exists but is never waited on) — real evidence can silently vanish
- file:line: internal/daemon/components.go:7583-7602 and 7993-7996
- evidence: 7589 `p.toolEvidence.wg.Add(1)`
7590 `go func() {`
7591 `    defer p.toolEvidence.wg.Done()`
7592 `    for msg := range sub.Channel { p.toolEvidence.absorb(msg.Payload) }`
7593 `}()`
7993 `if isStepJob && p.toolEvidence != nil {`
7994 `    if tev := p.toolEvidence.drain(conversationID); len(tev) > 0 { result["tool_evidence"] = tev }`
Collector struct (7611-7615): `mu sync.Mutex; byConv map[string][]map[string]any; wg sync.WaitGroup` — `grep` shows the only wg uses are Add/Done at 7589/7591; nothing ever calls Wait.
- why: The evidence for a step job is published on the bus during RunOnce and absorbed asynchronously by this goroutine (bus.Subscribe returns a buffered channel — internal/bus/bus.go:289-303), while Process drains the map immediately after RunOnce returns (7915 → 7994). There is no synchronization point between them, so a step that really executed file_write can be reported with no tool_evidence at all when the drain wins the race. Downstream, that missing evidence is indistinguishable from a hallucination: with finding 4 the validator then passes vacuously and the step is stamped validated. The unused WaitGroup suggests the author intended a flush that was never implemented (and a naive Wait would deadlock, since the goroutine exits only when the channel closes at shutdown).
- claimed vs actual: Commit 58a2ff42: "TOOL-issued evidence cannot be hallucinated without a real execution behind it" and the collector is described as making tool-issued ground truth available to the gate. Actual: delivery to the result envelope is racy, and the loss is silent (best-effort absorb, no sequencing).
- fix shape: Sequence the delivery: drain with a bounded retry/flush (e.g. wait until the collector has observed a completion count for that job, or publish evidence synchronously on the job's completion path) instead of an unsynchronized map read after RunOnce.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F55 [0] bestStepResult ignores Validated and the fallback ignores state — the sync reply can ship the exact step result the gate refused
- file:line: internal/agent/handler.go:1623-1645 (callers 1904, 1925)
- evidence: 1630 `done := s.State == task.StepCompleted || s.State == task.StepApproved`
1631 `if done && s.Sequence > bestSeq { best, bestSeq = s.Result, s.Sequence }`
1638 `// Fallback: any non-empty result (e.g. approved-state variants).`
1639 `for _, s := range steps { if s != nil && s.Result != "" { return s.Result } }`
Callers: 1904 (sync-wait ceiling) and 1925 (terminal task).
- why: Neither loop consults s.Validated or s.ValidationError, and the fallback consults no state at all — a step left StepScheduled by the claim-gate early return (finding 6) still has Result set (SetResult at tactical.go:982), so the ceiling branch returns that stranded result as the user-facing answer. 58a2ff42's stated purpose was that 'downstream consumers can tell unverified narration from a validated result'; the primary downstream consumer (the chat reply) is precisely the one that discards the flag.
- claimed vs actual: Commit 58a2ff42 claims unverified narration is marked so it can be distinguished downstream; commit 60052632 then routes the ceiling reply through bestStepResult, which cannot distinguish it. Net: a step marked "file side-effect claims without tool-issued evidence (unverified narration)" can still be delivered as the assistant's answer.
- fix shape: Skip steps with Validated==false (or with a non-empty ValidationError) in the first loop and require a terminal state in the fallback, falling back to the generic still-running reply instead of shipping an unverified record.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F56 [0] spawnedByUs is set before the spawn is validated or attempted, so a REFUSED spawn leaves the instance holding kill rights on a runtime it never spawned
- file:line: internal/llm/runtime_process.go:262 (set), :265-267 (no spawn command), :278-283 (duplicate-spawn refusal)
- evidence: `p.spawnedByUs = true` (:262) is executed before `if len(p.config.SpawnCommand) == 0 { return fmt.Errorf("no spawn command configured") }` (:265) and before `if endpointBusy { return fmt.Errorf("refusing to spawn %s: endpoint %s already has a listener ...") }` (:278-283).
- why: Both early returns leave spawnedByUs=true with p.cmd==nil and p.pid==0 — exactly the state that makes the next Stop() take the recovery branch (finding 1) instead of refusing with ErrRuntimeNotOwned. A daemon that refused to spawn onto an occupied port therefore claims ownership of that endpoint; when a pid file for that endpoint appears later (another instance adopting/serving it), its StopAll will signal that live foreign runtime. OLD: identical ordering existed; the wave added the refusal that makes it reachable without a successful spawn, and added the ownership contract this violates.
- claimed vs actual: a341879a/b28ea9c3 claim the ownership guard keeps later boots/instances from stopping a runtime they did not spawn; this path grants that right on a refusal.
- fix shape: Move `p.spawnedByUs = true` to immediately before `p.cmd.Start()` succeeds (or set it only after the PID file write), so a refused or invalid start leaves the zero value (non-owner).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F57 [0] The orphan reclaim never deletes the durable spawn record, so records accumulate and a stale record later overrides an explicit `auto_stop_on_exit: false`
- file:line: internal/llm/runtime_sweep.go:452-469 (removes only the PID file), internal/llm/runtime_record.go:105-112 (RemoveSpawnRecord), internal/llm/runtime_sweep.go:432-434 (record's own AutoStop decides)
- evidence: `removePIDFileForPid` ends with `if rmErr := os.Remove(ep.cfg.PIDFile); ...` (:465) — no RemoveSpawnRecord call. Grep of production callers of RemoveSpawnRecord yields exactly one: runtime_process.go:475 inside `clearPIDFileAndRecord`. Record matching uses the SNAPSHOT, not the config: `func sweepableRecord(rec SpawnRecord) bool { return rec.AutoStop && len(rec.Argv) > 0 }` (:433). The sweep test that exercises a record-only reap (`TestSweepOrphanRuntimes_ReapsRecordOnlyLeftover`, runtime_sweep_test.go:442-476) uses a temp-dir record and asserts nothing about the record file.
- why: A reap removes the PID file so the next Start does not adopt a dead pid, but leaves `<pid>.cmd` in `config.MeeptPath("run")`, which the daemon and `meept doctor` re-scan at every boot (orphan.go:61, doctor.go:378). Sequence: user runs the default config (auto_stop absent => true), the daemon crashes, boot 2 reaps the leftover and leaves the record; the user then sets `auto_stop_on_exit: false` and starts their own server with the same argv (`meept runtime start`, or a wrapper); its parent is init, so boot 3 matches the STALE record whose AutoStop=true and SIGTERM/SIGKILLs the user's runtime — the explicit opt-out documented as 'the only setting that is left alone' is not honored. Records therefore never expire and grow one file per spawn for the life of the install. OLD: no records existed; the sweep matched configs only, so the config's current value always decided.
- claimed vs actual: f9e57150/a341879a describe records as config-independent proof of a meept spawn; docs/configuration/llm-lifecycle.md promises 'Only `auto_stop_on_exit: false` is left alone'. Actual: a stale record outvotes false.
- fix shape: Call `llm.RemoveSpawnRecord(ep.cfg.PIDFile)` (and the record whose PIDFile matches the reaped pid's endpoint key) inside removePIDFileForPid; and/or have sweepableRecord consult the current config's AutoStop when a config exists for the same endpoint key.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F58 [0] The boot sweep reaps runtimes the operator deliberately started with `meept runtime start` (ppid==1 + argv match cannot distinguish a 'leftover' from a running operator runtime)
- file:line: internal/llm/runtime_sweep.go:142-152, :147 (match criteria), :390-404 (live-owner veto that cannot fire); cmd/meept/runtime.go:344-350; docs/configuration/llm-lifecycle.md
- evidence: Detection is only `if p.PPID != 1 || !matchesSpawnCommand(p.Command, cfg.SpawnCommand) { continue }` (:147). The CLI writes its record and PID file through the same code: `runtimeProc := llm.NewRuntimeProcess(rtCfg)` … `if err := runtimeProc.Start(ctx, io.Discard, io.Discard); err != nil` … `fmt.Printf("Runtime %s started (PID: %d)\n", ...)` (cmd/meept/runtime.go:345-350), then the CLI process exits; Start uses `SysProcAttr{Setpgid: true}` (runtime_process.go:297), so the runtime is re-parented to init (ppid==1). The veto cannot rescue it: `filePID, err := ParsePIDFile(cfg.PIDFile); if err != nil || filePID == pid { continue }` (runtime_sweep.go:395-397) — the CLI's own PID file names the SAME pid, so no veto. Docs claim the sweep targets leftovers 'of a dead meept generation' and that 'Only `auto_stop_on_exit: false` is left alone'.
- why: `meept runtime start` is documented as the operator way to start a warm runtime, and it always leaves a ppid==1 process with exactly the endpoint's spawn_command and a matching PID file. The next daemon boot (StartupOrphanSweep is called before StartAll, daemon.go) SIGTERMs and then SIGKILLs it, then spawns a fresh runtime — destroying a service the operator started (and any client using it) and reloading the model. `meept doctor --fix` does the same. The old sweep matched on a MEEPT_DAEMON_CHILD tag that ps never printed on macOS, so it never killed anything; this behavior is INTRODUCED by f9e57150. With ef6358a8's absent-means-true default, an operator who never set the key is opted into being reaped with no way to know.
- claimed vs actual: Claimed: leftovers from a dead generation, user-managed servers survive. Actual: 'parent is init' is satisfied by every runtime started by any meept process that has since exited (CLI, tool subprocess), so a deliberate operator start is reaped.
- fix shape: Record the spawning instance's boot id + expected parent in the SpawnRecord and require 'spawning instance is gone AND the recorded pid file is stale/not-ours' before reaping; or have `meept runtime start` refuse to leave a sweepable state (e.g. write AutoStop=false into the record when the operator starts it out-of-daemon), and document the sweep's reach in the CLI help.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F59 [0] mlx_lm reasoning-only TEXT answers still return ErrEmptyResponse — the wave fixed only the tool-call subset of the field it aliased
- file:line: internal/llm/client.go:1511-1539, :1571; internal/llm/models.go:596-601 (ReasoningText), :605-608 (ContentString)
- evidence: `content := msg.ContentString()` (:1511) and `reasoning := msg.ReasoningText()` (:1512); the reasoning text is consulted only for calls — `if len(lfmCalls) == 0 && reasoning != "" { ... parseLFMToolCalls(reasoning) ... }` (:1527-1531) — and then `if content == "" && len(msg.ToolCalls) == 0 && len(lfmCalls) == 0 { return nil, ErrEmptyResponse }` (:1537-1538). ContentString has no reasoning fallback: `if len(m.Content) == 0 { return "" }` (models.go:606-608). 799b0962's own message states the premise: 'mlx_lm returns the whole reply in a `reasoning` field with `content` absent ... every mlx_lm turn read as an EMPTY response and the loop burned its nudge ladder to "stopped after extended thinking"'.
- why: For a prose answer (no tool call) from mlx_lm the reply lives entirely in `reasoning`, `content` is "", no calls are recovered, and the client returns ErrEmptyResponse instead of the text — the exact failure the commit set out to fix still occurs on every non-tool turn, and Response.Content is empty even when a call was recovered. The mlx provider remains the registered classifier alternate after 20d02738 moved the driver to llama.cpp, so the path is live. OLD: same for prose; the wave's change covers labels-only equality.
- claimed vs actual: Claimed: the mlx alias + ReasoningText() fix the empty-response failure. Actual: only tool-call-only replies are fixed; text replies still short-circuit to ErrEmptyResponse.
- fix shape: In parseResponse (and the streaming accumulator), when ContentString() is empty and ReasoningText() is non-empty and no tool call was recovered, promote the reasoning text to Content (optionally stripping recovered call blocks) instead of returning ErrEmptyResponse.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F60 [0] HealthChecker's `running` flag is shared across runs and cleared by whichever run goroutine exits, so Stop can become a no-op and later Starts arm concurrent, unstoppable runs
- file:line: internal/llm/health_checker.go:62-75 (Start/re-arm), :77-82 (deferred clear), :202-211 (Stop), :151 (generation guard)
- evidence: Start: `if h.running { return }` ... `h.stopCh = make(chan struct{}); h.running = true` ... `go h.run(context.WithoutCancel(ctx), stopCh)` (:64-74). run's exit path unconditionally clears the flag: `defer func() { h.mu.Lock(); h.running = false; h.mu.Unlock() }()` (:78-81). Stop only acts when the flag is set: `if h.running && h.stopCh != nil { close(h.stopCh); h.stopCh = nil; h.running = false }` (:206-210).
- why: Stop() returns immediately after setting running=false while run A's goroutine is still parked in its select; a Stop→Start pair (exactly what RestartProvider does: StopProvider then StartProvider, runtime_manager.go:701-706) arms run B, and A's deferred clear then sets running=false while B is live. With running=false, Stop() is a no-op, so chB is never closed and B can never be stopped (StopAll at shutdown included); a further Start arms run C, and B+C both tick — B's verdicts are dropped by the generation guard (:151) but its HTTP GET of /health continues on every interval. On a daemon that auto-restarts a flapping runtime this accumulates leaked goroutines and request load for the process lifetime, and 'Stop is idempotent and safe against the run goroutine exiting on its own' is not true. OLD: the old `stopped` flag was never cleared by the run goroutine, so this interleaving did not exist — INTRODUCED by b28ea9c3/3c4e4ebc.
- claimed vs actual: Claimed (3c4e4ebc): 'The "stopped" flag becomes "running", cleared by the run goroutine itself; Stop closes only the active run's channel.' Actual: clearing the shared flag from a superseded goroutine loses track of the active run, which is what makes Stop a no-op and enables concurrent runs.
- fix shape: Have each run own its generation: on exit, `if h.stopCh == stopCh { h.running = false }` (mirroring the checkOnce generation guard), and have Stop close the channel of the run it believes is active even when the flag was cleared.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F61 [0] `meept doctor --fix` reaps orphans without the live-owner veto the daemon's sweep applies, so it kills runtimes the daemon would leave alone
- file:line: cmd/meept/doctor.go:218; internal/llm/runtime_sweep.go:195-204 vs :354-379 (daemon path)
- evidence: Doctor: `candidates, confirmed := llm.ReapOrphanRuntimesFromConfigsAndRecords(orphanCfgs, orphanRecords, 2*time.Second, slog.Default())` (doctor.go:218) → `ReapRuntimeProcesses(orphans, waitAfterTerm, nil, nil, log)` (runtime_sweep.go:203). The daemon path applies the veto first: `if m.hasLiveOwnerAmong(candidates, o.Command, o.PID) { m.logger.Warn("orphan sweep: leaving process alone ..."); continue }` (:365-369). Docs: '`meept doctor` reports the same leftovers as its `orphan-children` check, and `--fix` sends them SIGTERM.'
- why: Detection parity is real (both call FindOrphanRuntimesWithRecords), but the KILL decision is not: doctor's reaper has no live-owner veto, so a pid matched by a command that another endpoint records a live owner for (two providers sharing a command line, endpoint keys that do not normalize localhost vs 127.0.0.1) is signalled by --fix while the daemon's own boot sweep would have skipped it. Two surfaces making different decisions about the same process is exactly the class the wave's shared-inputs refactor claimed to remove. OLD: doctor's check matched a tag ps never printed, so it was inert; making it live without the veto is the regression.
- claimed vs actual: Claimed: 'Built ONCE per doctor run and shared by the report path and the --fix reaper, so the two can never disagree' (doctor.go:340-344). Actual: doctor and the daemon disagree on the veto, and doctor's veto-less reaper is strictly more destructive.
- fix shape: Route doctor's reap through the same predicate: expose the veto (e.g. a ReapOrphanRuntimesWithVeto helper taking the config set) and have doctor call it, or move hasLiveOwnerAmong's check into ReapRuntimeProcesses for all callers.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F62 [0] Desktop window close and web browser Back leave the panel with no guard at all
- file:line: ui/flutter_ui/lib/main.dart:120-126 (close handler), 51-54 (prevent-close armed); ui/flutter_ui/lib/widgets/tool_panel_shell.dart:73-77 (PopScope rationale)
- evidence: main.dart:51-54 `if (!kIsWeb) { await windowManager.setPreventClose(true); windowManager.addListener(_WindowCloseHandler()); }` and :120-126 `class _WindowCloseHandler extends WindowListener { @override void onWindowClose() async { await WindowGeometryService.save(); await windowManager.setPreventClose(false); await windowManager.destroy(); } }` — no dirty check, no dialog. For route pops, tool_panel_shell.dart:73-77: "This veto cannot live on the route as a `PopScope`: go_router leaves a tool panel through [NavigatorState.pop] rather than `maybePop`, and the embedded path replaces the route with `go`. Neither consults a route-level veto". A repo-wide grep for `PopScope|onPopInvoked|WillPopScope` in lib/ returns only that comment line (:73), i.e. no route-level veto exists.
- why: The settings panel lives at the top-level route `/settings` (core/router.dart:83), so the desktop close button (Cmd+Q / red button) and — on the web target — the browser Back button both unmount the panel without ever calling `requestExit`/`_guardExit`. OLD behavior: identical loss (no guard existed at all), but the wave's whole point was to stop silent loss of unsaved config edits, and closing the window is the most common way a desktop user ends a session. On web, HTML5/hash history entries created by `context.go('/settings')` make Back a one-click silent discard.
- claimed vs actual: Claimed: 'unsaved meept.json5 ... edits are never dropped silently' (settings_panel.dart:341-342). Actual: two OS-level exit paths drop them with no warning; only the in-panel back control and esc are covered.
- fix shape: Read a dirty flag (provider) in `_WindowCloseHandler.onWindowClose`: when dirty, re-arm `setPreventClose(true)` and show the shared `showDiscardEditsDialog` before calling `destroy()`. For route pops, add a router listener or a `PopScope`/`onPopInvokedWithResult` at the settings route (or a `GoRouter` `onExit`) that awaits the same guard. Note also that the dialog offers only 'discard and exit' / 'cancel' — there is no 'save and exit' anywhere, so a user who wants to keep edits must cancel, save, then exit.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F63 [0] Whole-file meept.json5 save silently reverts a concurrent orchestrator (block) save — stale full-document overwrite
- file:line: ui/flutter_ui/lib/features/settings/main_config_editor.dart:122-138 (save), 103-110 (snapshot); ui/flutter_ui/lib/features/settings/orchestrator_config_editor.dart:164; ui/flutter_ui/lib/features/settings/settings_panel.dart:363
- evidence: main_config_editor.dart:107-108 `_loadedContent = file.content; _controller.text = file.content;` then :130 `await _client.saveMainConfig(text);` — sdk_client.dart:1698-1700 `Future<void> saveMainConfig(String content) async { await _post('/api/v1/config/main', body: {'content': content}); }` — a whole-file POST of the text captured at load time. The same panel hosts the block editor: settings_panel.dart:363 `const OrchestratorConfigEditor(),` → orchestrator_config_editor.dart:164 `await _client.saveOrchestratorConfig(payload);` which writes the same file ("// orchestrator block in ~/.meept/meept.json5 — saved atomically, other keys preserved", :311), and the daemon overwrites the file wholesale: internal/comm/http/main_config_handlers.go:95 `path, err := configCli.WriteMainConfigAtomic(body.Content)`.
- why: Load 'main' -> edit the orchestrator block -> save it (file content changes on disk) -> press save in the main editor: the editor's T0 text wins and the orchestrator change is silently reverted, with a green 'saved to <path>. restart the daemon to load the change.' notice (main_config_editor.dart:135-137). The write is a full-document overwrite of the loaded text, so unknown keys are preserved — the defect is staleness, not key loss. The wave put a whole-file editor beside a block editor with no conflict detection; the daemon exposes nothing (no ETag/mtime) for the client to compare against.
- claimed vs actual: Claimed: the editor 'writes the whole file back' and 'a failed save NEVER discards the user's text' (main_config_editor.dart:24-27) plus the loopback/validation contract (sdk_client.dart:1690-1697) — all true. Actual: nothing detects that the file changed after load, so a successful save can destroy another editor's (or the CLI's) successful save.
- fix shape: Re-GET /api/v1/config/main immediately before POST and compare content (or have the daemon return a content hash/mtime in mainConfigPayload and send it back as `if_match`); when it differs, offer reload / merge / overwrite instead of writing the stale text; at minimum warn when the loaded text and the file diverge.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F64 [0] Sidebar layout's leader-key navigation bypasses the shared exit guard (the top-tabs layout guards the same two callbacks)
- file:line: ui/flutter_ui/lib/features/home/sidebar_home_screen.dart:122,125 (unguarded) vs ui/flutter_ui/lib/features/home/home_screen.dart:248-254 (guarded)
- evidence: sidebar_home_screen.dart:122 `_leaderController.onGlobalSearch = () { context.goToolSearch(); };` and :125 `_leaderController.onBranches = () { context.goToolBranches(); };` — both call go_router raw. The same commit guarded the sidebar's other navigation: :174-178 `Future<void> _onLeaderNavigate(String path) async { final allowed = await ref.read(toolExitGuardProvider).requestExit(); if (!allowed) return; if (!mounted) return; context.go(path); }`. The top-tabs layout was updated for exactly these two callbacks: home_screen.dart:248-250 `if (_selectedTab == HomeTab.sessions) { unawaited(_navigateGuarded(context.goToolSearch)); }` and :252-254 `_leaderController.onBranches = () { unawaited(_navigateGuarded(context.goToolBranches)); };`.
- why: A menu/shortcut navigation that replaces an open panel must ask the same guard (that is the stated "one exit guard"). In the sidebar these two still replace the route unguarded. Reachability today is limited — the palette item 'projects' (sidebar_home_screen.dart:250-252 -> onBranches) is the only live caller of `onBranches`, and while the settings panel route is open the sidebar (and its palette) is unmounted — so this is a latent path, not a reproducible loss; it becomes live the moment any dirty panel is hosted inside the sidebar layout.
- claimed vs actual: Claimed (commit cfa3db3d): 'route the menu and tab switches through one exit guard', and "both home layouts open menu picks through the same `openToolFromMenu` helper, which is what keeps the exit behaviour identical" (tools_dropdown.dart:47-53). Actual: the sidebar layout still has two raw `goTool*` navigations.
- fix shape: Route both callbacks through the same async guarded helper the top-tabs layout uses (`_onLeaderNavigate(path)` / a `_navigateGuarded`-equivalent on the sidebar state), so a refused navigation changes nothing in both layouts.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F65 [0] Docs and test claim every tool panel renders the shared shell; the prompts panel does not (and the test silently skips it)
- file:line: ui/flutter_ui/lib/features/prompts/prompt_panel.dart:105-107, 124-131, 190-196; docs/workflows/flutter_gui.md:75; test/features/home/tools_menu_test.dart:203-217
- evidence: docs/workflows/flutter_gui.md:75: "Every tool panel opens through the router (`/tools/<name>`, and `/settings` for the settings screen) and renders inside the shared chrome in `ui/flutter_ui/lib/widgets/tool_panel_shell.dart`, so one control set works identically in every panel". prompt_panel.dart has no ToolPanelShell (grep `ToolPanelShell(` in lib/ returns it for metrics, settings, calendar, memory, changes, branches, search, skills, reflection — prompts is the only menu tool missing): :105-107 `void _closePanel() { context.go('/'); }`, :124-131 its own `Focus(focusNode: _keyboardFocusNode, onKeyEvent: ... if (event.logicalKey == LogicalKeyboardKey.escape) { _closePanel(); } return KeyEventResult.ignored;` and :190-196 its own close IconButton. The test asserts the claim for every menu tool *except* one: tools_menu_test.dart:203-206 `testWidgets('every menu entry has a panel with the shared back control', ...) { ... // prompts is intentionally not built here (see _panelBuilders doc). if (tool == 'prompts') continue;`.
- why: prompts is one of the six hamburger-menu tools (tools_dropdown.dart:53-60 `_knownTools`), so the documented/asserted 'one control set in every panel' is false for one sixth of the surface, and its esc/close path is a second, panel-local exit that never consults the exit guard or the registry (the escaping used by c1cb479e). Impact today is a UX/claim inconsistency, not data loss, because the prompts panel is read-only (nothing unsaved to drop); it becomes a loss path as soon as that panel gains an editor.
- claimed vs actual: Doc claim line 75 + the test name 'every menu entry has a panel with the shared back control' vs actual: prompt_panel.dart builds its own header, its own esc handler and its own close button, and the test explicitly `continue`s past it.
- fix shape: Wrap PromptPanel's body in `ToolPanelShell(title: 'prompts', exitGuard: ...)` and delete `_closePanel`/`_keyboardFocusNode`, or, if the deviation is intentional, state the exception in docs/workflows/flutter_gui.md and rename/parameterise the test so it cannot read as full coverage.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F66 [0] `make graphs-check` fails on the current tree and no CI or hook runs it, while AGENTS.md labels it '(CI)'
- file:line: docs/generated/bus-topology.md, docs/generated/bus-topology.json, docs/generated/http-routes.json (stale) — AGENTS.md:71; Makefile:546-547; scripts/gen-connectivity-graph.py:867-889
- evidence: In the live repo and in a fresh `git clone --depth 1` of HEAD (691d83f6), `python3 scripts/gen-connectivity-graph.py --check` → `EXIT=1` and `❌ Stale generated files: bus-topology.json, http-routes.json, bus-topology.md — Run: python3 scripts/gen-connectivity-graph.py`. Diff of committed vs freshly generated (in the /tmp copy) is embedded absolute line numbers only, e.g. `- | `agent.action` | `internal/agent/loop.go:6835` |` vs `+ … internal/agent/loop.go:6848`, `- | `agent.result` | `internal/agent/loop.go:6865` …` vs `+ … :6878`. Nothing runs the check: `grep -rn 'graphs\|graphs-check' .github/workflows/` → no matches, and no .githooks/* invokes it. AGENTS.md:71 states `make graphs-check         # Verify generated files are fresh (CI)`.
- why: The wave's own regeneration commits (53bb495b, a81c81c3, dd6db9a1, 0e888fc4) were overtaken by later code commits in the same wave, so the shipped artifacts are stale at HEAD; the freshness gate that would have caught it is documented as a CI gate but exists nowhere in CI, and the generator's absolute line numbers make the check fail after any edit above those lines (so it would be red almost constantly if it were wired in).
- claimed vs actual: AGENTS.md:71 claims CI verifies generated-file freshness; no workflow references graphs-check (ci.yml runs build/vet/golangci/gosec/mutexio/predid; code-quality.yml runs .githooks/pre-commit). The artifacts AGENTS.md tells debuggists to read first are stale.
- fix shape: Wire `make graphs-check` into ci.yml (and/or pre-commit step) and make the artifact stable: emit symbol/function identity (or line numbers only for files that did not change) instead of raw line offsets, so the check fails on real topology drift rather than unrelated insertions.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F67 [0] docs/reference/generated is stale at HEAD and no target or CI step runs the freshness check the wave added (`mage docsCheck`)
- file:line: docs/reference/generated/agent.md, docs/reference/generated/llm.md — magefiles/docs.go:171-236; Makefile:685-687
- evidence: In a `git clone` of HEAD: `mage -d magefiles docsCheck` → `EXIT=1`, output `STALE: agent.md (github.com/caimlas/meept/internal/agent)` … `output does not match current files. Did you forget to run gomarkdoc?` / `Error: generated docs are stale; run 'mage docsGenerate' to regenerate`, plus `STALE: llm.md (github.com/caimlas/meept/internal/llm)` and a diff hunk proving real content drift: `7082 |        - \t    // AgentOverrideApplied records that the client explicitly named this` … `7089 |        - \t    AgentOverrideApplied bool \`json:"-"\`` (present in the source, absent from the committed page). Wiring check: `grep -rn 'docsCheck\|docs-check' Makefile .github/workflows/ .githooks/` → no matches; `make docs-generate` (Makefile:685-687 `mage -d magefiles docsGenerate`) is the only docs target and no workflow invokes it.
- why: The wave added orphan-pruning + DocsCheck machinery and regenerated the pages, but the pages are already stale at HEAD (later wave commits added API surface after the regeneration) and nothing enforces freshness — so the 'generated docs are up to date' property is unguarded and already false.
- claimed vs actual: Commit 0e888fc4/dd6db9a1 'regenerate the generated references against the current tree'; DocsCheck's own doc comment (magefiles/docs.go:168-170) says 'Returns an error if any file differs, suitable for use in CI'. Actual: two pages differ at HEAD and no CI/target calls DocsCheck.
- fix shape: Add a `docs-check` Makefile target (e.g. `mage -d magefiles docsCheck`) and a CI step (ci.yml or code-quality.yml) that runs it; treat a gomarkdoc-missing environment as a hard failure rather than an implicit pass where gomarkdoc is expected.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F68 [0] e2e harness port remap is stale after 20d02738: the scratch daemon is pointed at the user's live runtimes (8080/8083/8084 are never rewritten) and the general-runtime health wait polls a dead port
- file:line: scripts/e2e-naive-user-chat.sh:412-425, :485-491
- evidence: Remap block only rewrites the two legacy literals: :420 `-e "s/127\\.0\\.0\\.1:8081/127.0.0.1:${MLX_CLASS_PORT}/g"`, :421 `-e "s/127\\.0\\.0\\.1:8082/127.0.0.1:${MLX_GEN_PORT}/g"`, :422-423 the `"--port", "8081"/"8082"` variants. config/models.json5 at HEAD has no 8082 at all: :163 `"baseURL": "http://127.0.0.1:8083/v1"` (`local`, MLX general — moved off 8082 by 20d02738), :171 `"--port", "8083"`, :230 `"baseURL": "http://127.0.0.1:8084/v1"` (local-extract), plus the new default driver `local-gguf` at :283/:287 `http://127.0.0.1:8080` with `"model": "local-gguf/lfm-8b-gguf"` (models.json5:11). So the sandboxed copy keeps the real user ports, and the wait then polls a port no provider declares: :486 `MLX_GEN_PORT=$((HTTP_PORT + 2))`, :491 `wait_for_runtime "$MLX_GEN_PORT" "general" || true` (nothing binds HTTP_PORT+2 → the hook's own timeout path 'not healthy after 120s (classification may SKIP/fallback)').
- why: The harness's isolation promise is broken in the same wave that moved the ports: the scratch daemon adopts the user's live llama.cpp/MLX endpoints (exactly the shared-runtime contamination the lockfile commit 8d37e0a9 was written to prevent), verdicts can drift, and every run pays a spurious 120s warning. It also silently exercises the wrong model relative to the run's intent.
- claimed vs actual: Script header :16-17 ('no code path can touch the user's ~/.meept or the running daemon') and :412-414 ('Local runtime ports (mlx classifier :8081, mlx general :8082) collide with the USER'S live runtimes, so remap them into an ephemeral range'). Actual: only the classifier port is remapped; the driver (:8080), MLX general (:8083) and local-extract (:8084) are inherited from the user's config.
- fix shape: Derive the remap from the config instead of hardcoding literals (parse the copied models.json5 providers' baseURL/spawn `--port` and rewrite each loopback port to a probed free port), or fail loudly when a 127.0.0.1:<port> literal survives the rewrite; and probe the derived ports (bind/close) rather than assuming HTTP_PORT+1/+2 are free.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F69 [0] pre-commit-u1000's 'already seen' substring match drops a parent package when a subdirectory package is staged first (pre-existing, in a hook the wave edited)
- file:line: .githooks/pre-commit-u1000:47-52
- evidence: `dir=$(dirname "$file")` … `case "$PACKAGES" in *"$dir"*) ;;  # already seen` / `*) PACKAGES="${PACKAGES}${dir}\n" ;;`. With a staged file in internal/llm/metrics/ seen before one in internal/llm/, `$PACKAGES` already contains `internal/llm/metrics`, so `*internal/llm*` matches and internal/llm is never added — the U1000 gate never runs on it and still prints `✅ No unused code (U1000) violations detected`. Attribution: `git blame -L 47,52 .githooks/pre-commit-u1000` → 282573c0 (pre-wave), i.e. not introduced by the wave, but the wave rewrote the surrounding loop (:67-72) without fixing it, and it is the same failure class as finding 1 (a gate that reports success for packages it never analyzed).
- why: Silent false pass on real packages; the dedupe must be exact, not substring.
- claimed vs actual: Hook header: 'runs staticcheck's U1000 check on staged Go packages'. Actual: staged packages whose path is a substring of an already-collected path are skipped.
- fix shape: Compare full paths: build a unique list with `sort -u` on exact lines (as pre-commit-mutexio does) or test membership with `case "\n$PACKAGES" in *"\n$dir\n"*)`.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).


## LOW (25)

### F70 [0] Client agent override does not survive a clarification round trip or a report-router handoff
- file:line: internal/agent/dispatcher.go:1817-1826 and :2389-2400 (guard at :2255)
- evidence: ResumeAfterClarification builds `result := &DispatchResult{ Task: createdTask, AgentID: intent.AgentType, Intent: intent, ... }` with no AgentOverrideApplied field (the flag is `json:"-"` and only ClassifyAndRoute sets it at :1087); the handoff child builds `nextResult := &DispatchResult{ AgentID: nextAgentID, Intent: result.Intent, ... }`.
- why: RouteToAgent's platform shortcut is gated on `result.Intent.Type == string(IntentPlatform) && !result.AgentOverrideApplied` (:2255). Any turn that goes through clarification or a handoff re-enters with the flag false and its classified intent possibly still 'platform', so handlePlatformIntrospection swallows the turn again - the exact defect d409be61 fixed for the direct path.
- claimed vs actual: d409be61 claims the override 'must survive the intent-Type shortcut'; it survives only the direct RouteToAgent entry point, not its two parallel re-entry paths.
- fix shape: Thread the override (or the boolean) through ResumeAfterClarification's signature and copy AgentOverrideApplied into the handoff child result.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F71 [0] A live third quickplan producer leaves RequiresPlanning=false, so the C1 async-dispatch fix does not cover the LLM lane
- file:line: internal/agent/llm_classifier.go:637 (twin :524) + internal/agent/intent.go:209 + internal/agent/handler.go:770
- evidence: llm_classifier.go:637 `requiresPlanning := resp.Intent == string(IntentPlan)`; intent.go:209 `case IntentQuickPlan: return requiresPlanning`; handler.go:770 `case h.dispatcher.ShouldDispatchAsync(result) && result.Task != nil:`
- why: 691d83f6 (same wave) made IntentQuickPlan emittable by the LLM classifier, but the classifier still derives RequiresPlanning only from IntentPlan, so an LLM-classified quickplan returns ShouldDispatchAsync=false: the task is created (dispatcher.go:1071) with nothing to plan it and the turn falls to the inline chat fallback - the C1 failure fac36b5e claims to close.
- claimed vs actual: fac36b5e claims 'Both live producers set the struct field ShouldDispatchAsync reads for quickplan'. At HEAD there are three producers; the LLM one (made live by 691d83f6) does not.
- fix shape: Set RequiresPlanning from the lane's own predicate (`IntentType(resp.Intent).RequiresPlanning()`) at both llm_classifier sites and pin an LLM-quickplan async dispatch.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F72 [0] Needs_info force-completion refuses to record validation unless the step was loaded by the review path
- file:line: internal/agent/tactical.go:1469-1479
- evidence: `if result.Status == ReviewNeedsInfo { if err := ts.stepStore.SetState(step.ID, task.StepCompleted); ... }`
- why: HandleReviewResult already reloads the step and sets Validated on the needs_info branch (review_manager.go), but the tactical-side duplicate at :1479 re-checks `if !step.Validated` against its own stale copy; when the review path did not set it (e.g. HandleReviewResult returned before the reload branch), the SetState(completed) leaves Validated=false and the gate at :1327 blocks. Minor by itself, but it is a fourth SetState+Update pair on the same stale-state pattern as finding 1.
- claimed vs actual: 6f462f55 claims needs_info forced-completion 'validates the step on record'; the flag is only reliable when the review manager's own copy was already set.
- fix shape: Load/refresh the step after SetState before writing the validation flag, as in finding 1's fix.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F73 [0] fileOperationToolNames under-approximates the tool set, so successful media/spreadsheet/skills turns get annotated or nudged
- file:line: internal/agent/loop.go:3007-3011 (:4767 consumer)
- evidence: `var fileOperationToolNames = []string{ "file_write", "file_edit", "file_delete", "resolve", "shell" }`
- why: executionVerbRe matches generated/updated/created/saved/edited/changed, but generate_image, generate_video, spreadsheet_write, skills_create/skills_patch, cron_create, schedule_create, git_commit and delegation tools (a subagent that writes the file) are absent. A turn that really ran generate_image and says 'Generated image at ~/.meept/media/x.png' enters the nudge ladder and, at the budget edge, receives the '[unverified: no tools were executed this turn] ' prefix on a correct result.
- claimed vs actual: 3da3cf7a claims execution-backed claims are exempt; the allow-list only covers the five file/shell tools, so the exemption is narrower than the claim.
- fix shape: Treat any tool execution this turn as backing for the nudge (keep the annotation path for zero-tool turns), or extend the list to every write-capable tool.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F74 [0] tfidfVeto load-time validation is incomplete, so a shape-mismatched model panics inside the dispatch path
- file:line: internal/agent/tfidf_veto.go:63-65, :91-96, :121-131
- evidence: `if len(m.Vocab) == 0 || len(m.Classes) == 0 || len(m.Coef) != len(m.Classes) { ... }` then `s := t.intercept[c]` / `for j, xv := range vec { s += row[j] * xv }` / `vec[j] += t.idf[j]`
- why: Only len(Coef)==len(Classes) is checked; IDF/Intercept lengths and per-row width vs len(Vocab) are not, and Match has no recover. A model written by a different revision of scripts/build_tfidf_veto.py (or a partially regenerated vocab) panics the classifier goroutine instead of disabling the veto. Also note the vectorizer indexes bytes (`text[i:i+n]`) while the training script windows code points, so non-ASCII prompts silently produce a divergent feature vector despite the 'Matches the training script's feature extraction' comment.
- claimed vs actual: 10fe6d6d claims a validated, safe veto; the validation is partial and the doc contract about the loader path is stale.
- fix shape: Validate all vector lengths at load and disable the veto on mismatch (and bound-check in the loops); rune-window the n-grams or document the ASCII-only contract.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F75 [0] step.TokenUsage is still never populated for step jobs — token aggregation and progress publishing remain dead (the wave documented the fact but only deleted a conjunct)
- file:line: internal/daemon/components.go:7976-7984; internal/agent/tactical.go:1237-1252
- evidence: components.go:7976-7984 builds the step-job result map with keys job_id / response / status / success / evidence / step_id / task_id — grep for `token_usage` in internal/daemon/components.go returns nothing, so `step.TokenUsage` stays 0.
tactical.go:1237 `if step.TokenUsage > 0 { ts.taskStore.AddTokenUsage(step.TaskID, step.TokenUsage) ... }`
1248 `if step.TokenUsage > 0 || completedCount > 0 { ... ts.publishTokenProgress(t) }`
Commit 2fcf2365: "step.TokenUsage is NEVER populated — no production code writes it (no token_usage key in the step-job result envelope)."
- why: Task-level token accounting undercounts step tasks and task.progress events never carry real usage for job-driven tasks. The wave discovered the condition while removing the reasoning guard's `TokenUsage > 0` conjunct (2fcf2365) but left the root cause unfixed, and tactical_test.go:362-380 still fabricates `"token_usage": 300` in the payload, so the test suite gives false assurance that the field is populated.
- claimed vs actual: 2fcf2365 claims only that the guard must not depend on TokenUsage; the underlying gap (no producer for the field) is disclosed in the message but not fixed, and the test that exercises the aggregation path supplies the field by hand.
- fix shape: Populate token_usage in the AgentJobProcessor result envelope from the loop's usage for the turn, or delete the unreachable aggregation branches and the hand-populated test payload so the dead path is visible.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F76 [0] Bare-JSON tool-call miner silently drops any call object preceded by a comma (inArray treats 'after a comma' as 'inside an array')
- file:line: internal/llm/lfm_tool_calls.go:124-130, :145-149
- evidence: `inArray := func(pos int) bool { ... return j >= 0 && (content[j] == '[' || content[j] == ',') }` (:124-130) and `if inFence(start) || inArray(start) { kept.WriteString(content[i : start+1]); i = start+1; continue }` (:145-149). The doc comment claims only 'An object that is an ELEMENT of a JSON array is likewise not a call shape' (:122-123).
- why: A comma is not evidence of an array — it is ordinary prose punctuation. Model output like 'Here is the config, {"name":"file_write","args":{...}}' or two call objects separated by a comma both leave the call as raw text, so the turn runs zero tools and the step auto-approves prose: the exact failure this half exists to fix, silent (no warning log) and unnoticeable. OLD: the miner did not exist; adding it with a false-negative gate leaves the leak shape partially open while the code claims to close it.
- claimed vs actual: Claimed: 'mines only structurally call-shaped objects, skips objects inside fences ... and inside JSON arrays'. Actual: also skips any comma-preceded object, including prose calls.
- fix shape: Track actual bracket nesting (a depth/stack scan tracking whether '[' is open) instead of inspecting the previous byte, so only genuine array elements are skipped; keep a test for a comma-preceded bare call.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F77 [0] The '\boxed unwrap' claim does not hold for the documented shape: the literal `\boxed` token stays in the reply content
- file:line: internal/llm/lfm_tool_calls.go:169-174; internal/llm/lfm_bare_json_test.go:14-33
- evidence: After mining, `out := strings.TrimSpace(kept.String()); out = strings.ReplaceAll(out, `\boxed{`, ""); out = strings.ReplaceAll(out, `\fbox{`, "")` (:171-173). For the documented shape `\boxed{\n "name": "json_extract", ...}` the box's '{' IS the mined object's opening brace, so `kept` receives `content[i:start]` = `\boxed` (no '{'), and ReplaceAll(`\boxed{`) does not match. The pinned test only checks `if strings.Contains(rest, "json_extract")` (lfm_bare_json_test.go:30-32), so a remainder of `\boxed` passes.
- why: The remainder is returned as Response.Content, so the reply a user sees is the stray token `\boxed` (or `}` when the model double-braces) instead of empty/prose; harmless-ish but it is the value the loop treats as meaningful content and it contradicts the commit message ('unwraps a dangling \boxed{ }'). OLD: no unwrap existed at all, so this is a partial fix presented as complete.
- claimed vs actual: Claimed (799b0962): 'unwraps a dangling \boxed{ }'. Actual: only the literal `\boxed{` sequence is removed; the dangling `\boxed` (and a stray `}` in the double-brace variant) survives into content.
- fix shape: Normalize both spellings: strip a trailing `\boxed`/`\fbox` token as well as `\boxed{`, and drop an orphaned closing brace when the wrapper was consumed; assert the exact remainder string in the test.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F78 [0] Endpoint start lock treats 'the recorded owner pid is alive' as 'the lock is held', so a reused pid makes a fresh lock look held for up to 10s and fails the spawn; release deletes the lock path unconditionally
- file:line: internal/llm/runtime_startlock.go:90-103, :107-116
- evidence: `if info, err := os.Stat(path); err == nil && time.Since(info.ModTime()) > startLockStaleAfter { return true }` ... `return !processAlive(pid)` (:91-102), where processAlive is `syscall.Kill(pid, 0) == nil`. acquire loops until `time.Now().After(deadline)` (startLockWait = 10s) and then returns `fmt.Errorf("endpoint start lock %s is held by another meept process; retry once that start finishes", path)` (:79-81). release: `os.Remove(path)` unconditionally (:113).
- why: A lock file left by a crashed start (or by a start that crashed) whose recorded pid has been reused by an unrelated live process is 'not stale' for up to 30s, so the next Start blocks 10s and then fails with a misleading 'held by another meept process' — a boot-time spawn failure for every endpoint affected, and the daemon's StartAll joins it as an error. Separately, if a stale lock is stolen and recreated while the previous owner is still inside releaseStartLock, the release removes the NEW holder's lock, so the mutual exclusion the lock exists for is dropped. Failure direction is fail-closed (no duplicate spawn), hence LOW.
- claimed vs actual: Claimed: the lock is 'self-healing: a lock whose owner pid is gone, or that is older than startLockStaleAfter, is taken over'. Actual: 'owner pid is gone' is decided by signal-0 on a raw pid with no identity check, so reuse converts a dead owner into a live-looking one for up to the staleness window.
- fix shape: Store an identity alongside the pid (boot id/token, or the endpoint key) and compare it with the process table before deciding the lock is held; have release verify the file still contains our pid/token before removing it.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F79 [0] `meept runtime start --wait` health-waits without the process-alive probe, so a foreign listener on the endpoint still reports the CLI's dead child as healthy
- file:line: cmd/meept/runtime.go:352-363; internal/llm/runtime_manager.go:184 (probe wired only for the manager)
- evidence: CLI: `hc := llm.NewHealthChecker(rtCfg, baseURL); hc.Start(ctx); defer hc.Stop()` ... `if err := hc.WaitForHealthy(ctx, rtCfg.SpawnTimeout); err != nil { ... } fmt.Printf(" - healthy\n")` (runtime.go:354-363) — no SetProcessAliveProbe call, unlike `hc.SetProcessAliveProbe(proc.IsRunning)` (runtime_manager.go:184).
- why: The commit claims 'HealthChecker requires the spawned process to be alive, so an endpoint that answers 200 for someone else's process cannot mask a dead child'. That is true for the manager but not for the CLI wait path, which is the surface an operator uses to validate a runtime: if the port is taken after the pre-spawn probe (or the spawn command does not declare the port so the probe is skipped), `--wait` prints 'healthy' while meept's child holds the model with no socket. The probe's own doc acknowledges the CLI case, so this is a documented gap presented as closed in the commit body.
- claimed vs actual: Claimed: a 200 alone cannot make a dead runtime read healthy. Actual: it can on the CLI path (no probe bound).
- fix shape: Bind the probe in runRuntimeStart as well (`hc.SetProcessAliveProbe(runtimeProc.IsRunning)`), which is available immediately after Start.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F80 [0] The start lock and the duplicate-spawn guard are keyed on different identities (pid file vs endpoint address), so two providers reaching one port via different loopback spellings are not mutually excluded
- file:line: internal/llm/runtime_startlock.go:37-42; internal/llm/runtime_process.go:132-161; commit 3c4e4ebc note
- evidence: Lock key: `return pidFile + ".lock"` (:41) — per PID file. Probe key: `host, port := hostPortFromBaseURL(p.config.BaseURL)` → `net.JoinHostPort(host, port)` (runtime_process.go:136-140), used with `spawnCommandBindsPort` (:157). 3c4e4ebc's own message: 'two providers can share one command line with different pid files (endpoint keys do not normalize localhost against 127.0.0.1), so attribution is ambiguous'.
- why: Two providers configured as `http://127.0.0.1:8080` and `http://localhost:8080` have different pid files (different locks) but the same port. Their probes can both see the port free and both spawn; the loser is exactly the mlx_lm shape the guard was built for (survives a failed bind, loaded model, no socket) while the winner answers /health. The wave identified the non-normalization for the veto but not for the lock/probe, so the duplicate-spawn guarantee is not delivered across spellings. Not a new code path so much as an identity mismatch in the wave's own new mechanisms.
- claimed vs actual: Claimed: 'the probe alone cannot close the window ... The probe + adoption check + spawn + PID-file write therefore run under one exclusive lock file' — mutual exclusion. Actual: exclusion is per pid file, not per endpoint address.
- fix shape: Key the lock on the normalized endpoint address (or the endpoint key) instead of the PID file path, and normalize loopback aliases in ComputeEndpointKey.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F81 [0] 10. The prompt project tier follows only the project.set topic, so project.detect (TUI/HTTP project switch) leaves the tier stale, and one process-global tier is retargeted by any session's binding
- file:line: internal/rpc/projects.go:376 (with internal/rpc/projects.go:158, 185, 277 and internal/daemon/prompt_dirs.go:87-91)
- evidence: Only three publishers exist — `grep -n publishProjectChanged internal/rpc/projects.go` -> :158 (register), :185 (unregister), :277 (set) — while the detect path calls the manager directly and returns: internal/rpc/projects.go:376-379 `p, err := pm.DetectFromPath(ctx, req.Path); if err != nil { return nil, fmt.Errorf("detect project: %w", err) }`. DetectFromPath auto-registers the repo as active (internal/project/manager.go:237 `Status: "active",`), which is what GetActive orders by, and the tier only moves on the event: internal/daemon/prompt_dirs.go:87-91 `for range sub.Channel { ... prompt.SetProjectDir(resolve()) }` — the payload (session id / path) is discarded and one global tier is re-pointed.
- why: Defect: after a project.detect the daemon's active project is the new repo while prompt Get/List keep resolving the previous project's .meept/prompts (or no project tier at all). Separately, because the tier is process-global and the subscriber ignores the event payload, session B binding to project B flips the tier that session A's prompt requests read (with the value cached once per event and never re-resolved per lookup). OLD behavior: before 1eef34fc the tier did not follow the active project at all, so this is a partial fix rather than a regression.
- claimed vs actual: 1eef34fc claims 'prompt project tier follows the active project at runtime'. It follows only project.set, and it follows it globally rather than per session.
- fix shape: Publish the project-change event from every path that mutates active status (handleDetect / ProjectService.DetectProject), or resolve the project tier lazily from ProjectManager.GetActive per prompt operation; key the tier per session/conversation so one session's binding cannot retarget another's overrides.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F82 [0] 11. configui renders 'auto stop on exit: on' for every provider with no lifecycle block — the opposite of the accessor added by the same commit — and toggling it materializes a brand-new lifecycle block
- file:line: internal/configui/sections_models.go:79 (with internal/llm/runtime_config.go:43-45, 434-437)
- evidence: internal/configui/sections_models.go:50-53 substitutes a zero value when the provider has none: `lc := llm.RuntimeLifecycleConfig{}; if p.Lifecycle != nil { lc = *p.Lifecycle }`, and :76-79 renders the effective default: `NewToggleField("lifecycle.auto_stop_on_exit", "auto stop on exit", lc.AutoStopOnExitOrDefault())`. On a zero value that returns true: internal/llm/runtime_config.go:43-45 `func (c RuntimeLifecycleConfig) AutoStopOnExitOrDefault() bool { return c.AutoStopOnExit == nil || *c.AutoStopOnExit }`. The provider-level accessor disagrees for exactly this case: internal/llm/runtime_config.go:434-437 `func (p ProviderConfig) IsAutoStopOnExit() bool { if p.Lifecycle == nil { return false }; return p.Lifecycle.AutoStopOnExitOrDefault() }`.
- why: Defect: every non-local provider (openai/anthropic/…) displays the toggle ON even though the daemon's own accessor reports false and the provider manages no runtime. An operator who trusts the display toggles it off, which marks the field dirty and makes save.go materialize an empty lifecycle block for a provider that had none. OLD behavior: before ef6358a8 the toggle rendered the raw field (zero/false), matching the accessor.
- claimed vs actual: ef6358a8 claims 'configui: the lifecycle toggle renders the effective value'; for a nil lifecycle block the rendered value (true) is the opposite of the value the commit's own accessor reports (false).
- fix shape: Render from the provider accessor (pass AutoStopOnExitOrDefault() only when p.Lifecycle != nil, else false), or hide/gate lifecycle fields for providers with no lifecycle block.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F83 [0] 12. The bundled prompt tier is still resolved against the process CWD and can be stored as a bare relative path, contradicting the wave's 'never the process CWD' contract
- file:line: internal/daemon/bundled_path.go:24-37 (with internal/daemon/prompt_dirs.go:53)
- evidence: internal/daemon/bundled_path.go:24-37 `func resolveBundledPath(rel string) string { if rel == "" || filepath.IsAbs(rel) { return rel }; if _, err := os.Stat(rel); err == nil { return rel }; if exe, err := os.Executable(); err == nil { candidate := filepath.Join(filepath.Dir(exe), rel); if _, err := os.Stat(candidate); err == nil { return candidate } }; return rel }` — a relative `rel` is tested against the CWD first, and if neither candidate exists the relative string itself is returned. It is called with a relative path: internal/daemon/prompt_dirs.go:53 `Bundled: resolveBundledPath("config/prompts"),`.
- why: Defect: the shipped prompt tier follows the daemon's launch directory (AGENTS.md: 'often the meept repo itself'), and when nothing is found a relative path is handed to the prompt service, which then reads it against whatever the CWD is at read time. A launch dir that ships config/prompts shadows the shipped prompts. Impact is bounded because Bundled is the lowest-priority tier, but the contract stated in the code is not met. OLD behavior: unchanged by the wave — 095603b6 removed the other CWD dependence and left this one, while claiming the bundled tier is 'never the process CWD'.
- claimed vs actual: 095603b6 / internal/daemon/prompt_dirs.go:36-37 claim 'so the prompt service never depends on the daemon process's working directory' and internal/services/prompt_service.go documents the bundled dir as 'the shipped repo/executable layout (never the process CWD)'. Actual resolution is CWD-first and may remain relative.
- fix shape: Resolve the bundled tier to an absolute path (filepath.Abs of the CWD candidate or the executable-relative candidate) and never return a bare relative path; assert in the daemon test that the resolved bundled dir is absolute.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F84 [0] A refused tab switch does not stop callers' side effects, so 'a refused switch changes nothing' is not true for all callers
- file:line: ui/flutter_ui/lib/features/home/home_screen.dart:519-523 (and 304-306 claim); ui/flutter_ui/lib/features/sessions/sessions_list.dart:107-109, 138-141
- evidence: home_screen.dart:307-320 `_requestTabSwitch` returns early on refusal, but the palette caller does not wait: :520-523 `case 'new session': _onLeaderTabSelected(HomeTab.sessions.index); ref.read(createSessionRequestProvider.notifier).state = true;` with :289-293 `_onLeaderTabSelected(int index) { if (...) { unawaited(_requestTabSwitch(HomeTab.values[index])); } }`. Child callers navigate themselves as well: sessions_list.dart:107-109 `ref.read(activeSessionProvider.notifier).state = session; ref.read(tabActivationProvider.notifier).state = HomeTab.chat; if (mounted) context.go('/');` and :138-141 `_doActivateSession(...) { ... ref.read(tabActivationProvider.notifier).state = HomeTab.chat; context.go('/'); }`. The claim being contradicted is home_screen.dart:304-306: "A refused switch changes nothing: the tab, the route and the panel's text all stay where they were."
- why: A guard refusal leaves the route mutated (the unconditional `context.go('/')` in sessions_list runs before/independently of the guard) and leaves `createSessionRequestProvider` armed, so `_selectedTab` (HomeTab.plans/agents/…) and the URL can diverge from what is rendered and a session may be created after a switch the user cancelled. Latent while no guard can be registered with a home layout mounted (see F7), but it is the exact class of bug the wave set out to close.
- claimed vs actual: Claimed (comment + commit cfa3db3d 'a refused switch changes nothing'); actual: `_requestTabSwitch` swallows its outcome (Future<void>), so callers cannot know, and two callers mutate route/provider state outside the guard.
- fix shape: Make `_requestTabSwitch` return `Future<bool>` and gate the callers' side effects on it (`if (!await ...) return;`), or funnel session activation and 'new session' through one guarded switch that returns the answer.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F85 [0] The shared exit-guard registry has no reachable registration at HEAD: the menu/tab-switch guard the wave added is dead plumbing
- file:line: ui/flutter_ui/lib/providers/tool_exit_guard.dart:33,51; ui/flutter_ui/lib/features/settings/settings_panel.dart:111; ui/flutter_ui/lib/core/router.dart:83
- evidence: The only `register` call in the tree is settings_panel.dart:111 `_exitGuardRegistry.register(_exitGuard);`. SettingsPanel is reachable only as the top-level route router.dart:83-88 `path: '/settings', name: 'settings', builder: (context, state) { return const SettingsPanel(); }` — a page that replaces HomeScreen/SidebarHomeScreen entirely, so neither the tab bar (home_screen.dart:642-647) nor the hamburger menu (:657-676) exists while a guard is registered. The embedded slot could host it (chat_tab.dart:64-65 `case 'settings': return const SettingsPanel();`) but nothing ever sets that value: the single setter is home_screen.dart:668-671, reached only for `ToolMenuOutcome.noRoute`, and `toolRouteFor('settings')` is non-null (tools_dropdown.dart:22). The wave's tests therefore hand-register a guard: tool_exit_guard_test.dart:12-15 "The open panel is stubbed with a guard registered in the provider (and, for the lifetime cases, a small panel that registers and releases), rather than building the whole settings panel".
- why: `_requestTabSwitch` (home_screen.dart:307-320), `_navigateGuarded` (:324-329), `openToolFromMenu` (tools_dropdown.dart:57-70) and the sidebar guard (:174-178) all call `requestExit()`, which returns true immediately whenever nothing is registered — which is always, in the shipped UI. The code is correct, but the deliverable 'the menu and tab switches ask the open panel's exit guard' cannot be observed or verified outside the unit tests that stub the registration, so a future regression in those paths stays green. (The shell's own latch `_exitPending` and the registry's release-on-allow are correct: one press equals one dialog, and a released guard is not asked twice.)
- claimed vs actual: Claimed (commit cfa3db3d): 'latch panel exits and route the menu and tab switches through one exit guard'. Actual: nothing can be registered while a home layout is mounted, so the tab-switch/menu guard is unreachable; only the shell path (back control, esc) is exercised in production.
- fix shape: No minimal code fix; to make the claim true, give a dirty panel a host inside a home layout (e.g. restore an `activeToolProvider = 'settings'` entry point, or let the menu open the settings panel in the chat-tab slot) and add one integration test that opens the real panel inside HomeScreen and switches tabs. This is also the natural home for the shared dirty flag in F1.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F86 [0] Metrics panel's model/agent usage is erased by any metrics_update WS message (latent: no producer today)
- file:line: ui/flutter_ui/lib/providers/metrics_provider.dart:123-133 and 160; ui/flutter_ui/lib/features/metrics/metrics_panel.dart:339-342
- evidence: metrics_provider.dart:160 `state = _stateFromPayload(msg);` where :123-133 builds a *fresh* state, `modelUsage: ModelUsage.parseList(data['models'])`, `agentUsage: AgentUsage.parseList(data['agents'])`, `totals: MetricsTotals.parse(data['totals'])` — absent keys become null. Only the HTTP path carries those keys: internal/comm/http/live_metrics_usage.go:28-33 `type liveMetricsResponse struct { *metrics.LiveMetricsSnapshot; Models []metrics.ModelUsage \`json:"models"\`; ... }` while the WS relay forwards the bare snapshot (`case strings.HasPrefix(topic, "metrics."): eventType = "metrics_update"`, internal/comm/http/server.go:727-728). A null section renders as a note: metrics_panel.dart:339-342 `_SectionNote('model usage not reported by this daemon')`.
- why: Any metrics_update message would replace the whole state, so tables populated by the HTTP fetch would flip to 'not reported by this daemon' and the new 'updated <time>' stamp would advance while the usage rows vanish. Verified today this cannot fire: no Go code publishes a `metrics.*` bus topic (only the classifier matches the prefix), /api/v1/metrics/stream is a stub (`s.writeJSON(w, http.StatusOK, map[string]string{KeyStatus: "websocket_not_implemented", ...})`, server.go:2108-2119) and the client's `subscribe channel 'metrics'` is only logged by the daemon. So the defect is dormant, not live.
- claimed vs actual: Claimed (api_models.dart:756-760): missing sections 'degrade to a note instead of crashing the whole panel' for 'an older daemon (or the degraded 503 path)'. Actual: on the WS path a *newer* daemon's usage data would be reported as 'not reported', i.e. a false negative rather than a degradation.
- fix shape: In the WS branch merge instead of replace (`state = state.copyWith(current: MetricsSnapshot.fromJson(msg), receivedAt: DateTime.now())`, leaving the nullable usage fields on `_unset`), or have the daemon emit the enriched liveMetricsResponse on the WS/metrics path too.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F87 [0] Removed files/terminal panels left a stale layout doc comment and a dead terminal SDK surface
- file:line: ui/flutter_ui/lib/layouts/layout_4_top_tabs_panels.dart:13-14; ui/flutter_ui/lib/services/sdk_client.dart:1736-1765
- evidence: layout_4_top_tabs_panels.dart:11-15 `/// +----------------------------------------------------------+` / `/// |  - search   |                                | - branches |` / `/// |  - memory   |     Main Content Area          | - files    |` / `/// |  - prompts  |                                | - terminal |` — the widgets were removed (the only remaining matches for 'files'/'terminal' under lib/layouts are these two comment lines; the `_ToolItem` list ends at :279 `const _ToolItem(Icons.call_split, 'branches'),`). The same commit updated lib/layouts/README.md ("Right: Collapsible project panel (branches)") but not this file's own ASCII map. Dead client API for the removed tool: sdk_client.dart:1736 `Future<sdk.CommandHistory?> getTerminalHistory()`, :1747 `getTerminalHistoryRaw()`, :1753/1761 `/api/v1/terminal/exec`, :1765 `/api/v1/terminal/clear`.
- why: docs/workflows/flutter_gui.md:86 states "there is no `/tools/terminal` route, no menu entry, no sidebar case, and no layout-4 quick-access item" — true for the widget list, but the layout file's own documented structure still advertises both removed tools, and the SDK still exposes terminal endpoints, which is how a removed tool re-appears in a future panel list. No dangling widget/route/provider references remain (grep for FilesPanel|files_panel|TerminalPanel|terminal_panel|goToolFiles|goToolTerminal|FileEntry|toolsFiles|toolsTerminal over lib/ and test/ returns nothing; the removed `FileEntry` model in api_models.dart has no other user; lib/features/files and lib/features/terminal are gone).
- claimed vs actual: Claimed: 'drop terminal tool' + no files panel and no layout-4 quick-access item. Actual: widget/menu/route removal is complete and clean; the doc comment in the layout file and the client terminal methods were not swept.
- fix shape: Update the layout_4 ASCII structure comment to list only branches; delete (or annotate as TUI-only) the terminal methods in sdk_client.dart.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F88 [0] HashInput ignores its salt parameter while the doc comment claims the salt bytes are part of the derivation
- file:line: internal/metrics/hash.go:36-49,40-43
- evidence: hash.go:44-49 `func HashInput(saltID string, salt []byte, message string) string { h := sha256.New(); h.Write([]byte(saltID)); h.Write([]byte{0x00}); h.Write([]byte(message)); ... }` — `salt` is never used — while :38-43 states "The salt bytes are part of the signature for forward compatibility: a future keyed derivation changes only this function". The 32-byte salt file is created and rotated (classifierSaltFile, LoadOrCreateSalt) but has no effect on stored hashes.
- why: The stated privacy mechanism is misdescribed: rotating the salt without changing the saltID leaves every historical hash identical, which is precisely what the comment promises cannot happen. Per-install entropy still exists via the random 16-hex saltID, so the practical claim (cross-install dictionary attacks fail) holds.
- claimed vs actual: Claimed: 'salt bytes are part of the signature'. Actual: only saltID is hashed; salt is dead input.
- fix shape: Either feed salt bytes into the derivation (and version the hash prefix) or correct the comment to say the saltID is the whole key and the salt file is reserved/unused.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F89 [0] Dead columns and unmeasured metrics reported as values
- file:line: internal/metrics/store.go:319,1029,1052 ; cmd/meept/session.go:544 ; tools/classifier-eval/results/iter-7/summary-20260911-133854.json ("p50_ms": 0.0)
- evidence: `input_summary` still exists in the schema (:319) and is still selected by both queries (:1029-1032, :1052-1056) and consumed by the CLI (:544 `timestamp := getStringOr(entry, "input_summary", "")`) although every writer now stores "" (dispatcher.go:3575 `InputSummary: ""`). The committed sweep artifacts report `"p50_ms": 0.0` for every policy while eval_harness.py:12 advertises "p50 latency" as a protocol metric; lat comes only from cache-miss embed samples (eval_harness.py:599-605), so a fully cached run reports 0.
- why: Post-hardening, the CLI path silently renders empty summaries, and a latency metric that is `0` when unmeasured reads as 'instant' rather than 'no samples' — both make the evidence files say things they did not measure.
- claimed vs actual: Claimed: privacy hardening removed raw input with the field 'kept for compatibility, written as empty'. Actual: readers were not updated, and the harness reports an unmeasured latency as 0.0.
- fix shape: Either drop the field from reads/CLI (or display '—'), and emit p50_ms as null/omit when latency_ms is empty.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F90 [0] Dart gate is asymmetric: the hook skips (exit 0) when the SDK is absent, while lint-ci hard-fails on the same condition
- file:line: .githooks/pre-commit-dart-format:79-83; Makefile:503-512
- evidence: Hook :79-83 `if [ -z "$DART_BIN" ]; then echo -e "${YELLOW}SKIP: dart not found …"; exit 0`. Makefile :504 `@test -n "$(DART)" || { echo "Error: dart not found. Install Flutter or add dart to PATH, then run 'make fmt-gui'."; exit 1; }` — verified: `make fmt-check-gui DART=` → `Error: dart not found …` / `make: *** [fmt-check-gui] Error 1` (exit 2), and the Makefile comment claims 'fmt-check-gui treats that as a hard error, never a silent pass'. `lint-ci: lint analyzers audit-scripts fmt-check-gui` (Makefile:471) therefore hard-requires Flutter/llvm on any machine running the canonical gate.
- why: On a machine without Dart the commit gate reports all checks passed while checking nothing for the Flutter tree; on a machine without Dart the canonical 'is this branch shippable' target aborts. Neither side is wrong by itself, but the two cannot both be the enforcement point for the same claim (hook header :4-5: '`make lint-ci` enforces this repo-wide via the fmt-check-gui target; this hook enforces it on STAGED files only').
- claimed vs actual: Hook README row claims staged Dart is dart-format clean; without the SDK the hook exits 0 with only a yellow SKIP line, so 'all pre-commit checks passed' (main hook :108) is printed anyway.
- fix shape: Pick one policy: make the hook fail (or set a non-zero exit for the whole pre-commit run) when Dart is missing and Dart files are staged, or make `fmt-check-gui` degrade to a warning in non-CI runs; state the policy in both places.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F91 [0] The generated nav file targets a MkDocs plugin the docs build does not load, so generator-controlled ordering/listing is inert
- file:line: docs/reference/generated/.nav.yml (regenerated in 70f0a46a); magefiles/docs.go:150-162
- evidence: mkdocs.yml:38-43 plugins = `search`, `mermaid2`, `git-revision-date-localized`, `awesome-pages`; docs/requirements.txt:4 pins `mkdocs-awesome-pages-plugin>=2.9.0`; the installed venv has `mkdocs_awesome_pages_plugin-2.10.1`. That plugin (v2 line, latest 2.10.1) reads `.pages` files; `.nav.yml` belongs to the renamed v3 package `mkdocs-awesome-nav`, which is neither in requirements.txt nor in the plugin list (upstream migration: 'The plugin has been renamed from mkdocs-awesome-pages-plugin to mkdocs-awesome-nav', '.pages is called .nav.yml by default' in v3). All 13 nav files in the tree are `.nav.yml` (docs/.nav.yml, docs/reference/.nav.yml, …), and magefiles/docs.go:160 writes `.nav.yml` as the generator's nav output.
- why: The wave reordered/regenerated docs/reference/generated/.nav.yml ('deterministic output') and the generator emits it, but the docs build ignores it — ordering and inclusion are unenforced, and the nav file's omission of pages has no effect (docs/reference/generated/daemon.md is not produced by the generator, is not in .nav.yml, and is still published by the plugin's auto-inclusion).
- claimed vs actual: Commit 70f0a46a presents the regenerated .nav.yml as part of pinning docs generation ('document only in-module packages, prune orphaned generated pages, deterministic output'). Actual: nothing consumes it in this build configuration.
- fix shape: Switch the docs build to `mkdocs-awesome-nav` (`requirements.txt` + `mkdocs.yml` plugins entry `awesome-nav`) or rename the nav files back to `.pages`; then decide whether daemon.md should be produced/linked (currently an orphan the pruner deliberately ignores, magefiles/docs.go:317-343 only reports gomarkdoc-headed files).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F92 [0] Stale hook-count and lint-ci claims in Makefile/AGENTS.md after the wave added the 17th hook
- file:line: Makefile:229; AGENTS.md:76; AGENTS.md:80
- evidence: Makefile:229 `@echo "Installed 15 pre-commit checks via .githooks (see .githooks/pre-commit)."`; AGENTS.md:76 `make hooks                # core.hooksPath -> .githooks (15 pre-commit checks)`; the hook itself now runs 17 steps (`.githooks/pre-commit` :39-104, labels `[1/17]`…`[17/17]`, step 17 = dart formatting) and `.githooks/README.md:37` was updated to 'Entry point that runs all checks sequentially (17 total)'. AGENTS.md:80 still describes `make lint-ci` as 'golangci-lint + analyzers + audit scripts', while Makefile:471 is `lint-ci: lint analyzers audit-scripts fmt-check-gui`.
- why: AGENTS.md's own maintenance rule ('Every commit MUST review this file … Build commands — new Makefile targets or changed build steps are listed') is violated by the wave that changed the hook set; a reader counting gates from the docs gets the wrong number and misses the Dart gate and its Flutter/CI requirement.
- claimed vs actual: Docs claim 15 pre-commit checks and a lint-ci of 'golangci-lint + analyzers + audit scripts'; actual is 17 hooks and lint-ci additionally requires the Dart SDK (`fmt-check-gui`).
- fix shape: Update Makefile:229 and AGENTS.md:76/80 in the same commit as any hook change (or generate the count from the hook list / drop the number).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F93 [0] .githooks/README.md table mis-numbers the checks and omits four of them, including the new row's neighbours
- file:line: .githooks/README.md:37, :40-51
- evidence: Header :37 'Entry point that runs all checks sequentially (17 total):' then rows `| 1 | pre-commit-deferred |` … `| 11 | pre-commit-feature-docs |` and `| 17 | pre-commit-dart-format | Staged Dart files are dart-format clean |` — the numbering jumps 11 → 17 and steps 12-16 never appear; the list also omits `pre-commit-build` (step 1 in .githooks/pre-commit), `pre-commit-concurrency` (13) and `pre-commit-mutation` (14). Every retained row disagrees with the hook's own label (README says mutexio = 2, hook prints `[3/17]`; README vet = 4, hook prints `[5/17]`; README feature-docs = 11, hook prints `[15/17]`).
- why: The hook reference is the document a developer consults after a blocked commit; its numbering cannot be mapped onto the printed step numbers, so the new Dart step is the only one a reader can locate.
- claimed vs actual: Table implies an ordered 1..17 list; actual list has 12 rows with a 5-number gap.
- fix shape: Renumber the table to match .githooks/pre-commit's [n/17] labels and add the missing rows (build/16 ascii/13 concurrency/14 mutation), or drop the column.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F94 [0] e2e lockfile is taken ~96 lines before the EXIT trap that releases it, and its staleness check is pid-reuse-sensitive
- file:line: scripts/e2e-naive-user-chat.sh:79-100 (lock), :113-121, :161-177 (cleanup/trap)
- evidence: Lock is acquired at :80-91 (`mkdir "$LOCK_DIR"`, `echo $$ >"$LOCK_DIR/pid"`, `E2E_LOCK_HELD=1`), but `trap cleanup EXIT` / `INT` / `TERM` are installed only at :175-177; between them sit plain assignments plus `WORK="$(mktemp -d …)"` (:113) and `WORK="$(cd "$WORK" && pwd)"` (:121). A SIGINT/SIGTERM or error in that window leaves `${TMPDIR}/meept-e2e.lock` behind, and because liveness is decided by `kill -0 "$LOCK_PID"` (:85) a recycled pid makes the leaked lock permanent (every later run exits 3 until manual removal, :94/:98). The acquisition itself is atomic and race-free (mkdir is O_EXCL; a reader that sees no `pid` file falls through to the else branch and exits 3), and normal/failing exits, SIGINT and SIGTERM all reach `cleanup`, which removes the lock (:164-166). Also dead code: :487 `if [ -n "$MLX_CLASS_PORT" ]` is always true (the value is arithmetic, :485).
- why: Small but real window where the guard's own failure mode (a lock nobody can clear without a pid that still exists) can be created by a normal Ctrl-C; the cost is that every subsequent e2e run fail-fasts.
- claimed vs actual: Commit 8d37e0a9: 'cleanup removes it on every exit path'. Actual: every path after trap installation (:175).
- fix shape: Install the trap before acquiring the lock (or move the lock acquisition after the trap), and record a start timestamp / process start time alongside the pid so a recycled pid cannot keep a dead lock alive; drop the always-true `-n` check at :487.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).


## INFO (6)

### F95 [0] The recall override bypasses dispatcher bookkeeping and hard-codes confidence 0.85
- file:line: internal/agent/dispatcher.go:1301-1314
- evidence: `d.recordClassificationMethod("platform_recall_arbitration")` then `return &Intent{ Type: string(IntentRecall), Confidence: 0.85, AgentType: config.AgentIDChat, ... }`
- why: Every sibling return path calls recordAgent + recordIntentType and applyContextWeighting; this one records only the method, so by_agent/by_intent under-count the arbitration the wave added for measurement, and a discarded platform verdict of 0.2 is upgraded past its 0.70 threshold by the constant.
- claimed vs actual: The wave documents classifier observability as the reason for these arbitration paths; the new path is only half instrumented.
- fix shape: Call the shared recording tail and derive confidence from the discarded verdict (or document the constant).
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F96 [0] prefilter_veto_test never calls Match; the verdicts it computes are discarded
- file:line: internal/agent/prefilter_veto_test.go:18-44
- evidence: `if !p.veto.agrees("anything at all", "code") { t.Log(...) }` / `_ = verdicts`
- why: Deleting the veto fallthrough block in embedding_prefilter.go:459-472 leaves the suite green; TestPrefilter_TfidfVetoLoadsFromMeeptPath:55 still carries the comment describing the MEEPT_HOME auto-load deleted by 511e47fb.
- claimed vs actual: 10fe6d6d claims '8 new tests' including disagreement fallthrough - the fallthrough is not exercised.
- fix shape: Drive Match (stubbed embedder + conflicting veto) and assert nil + Suppressed.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F97 [0] `StalePIDRemoval` has no production caller while a PID file left by an unexpected runtime exit is exactly what the unvalidated Stop recovery reads
- file:line: internal/llm/runtime_process.go:530-538; :336-346 (exit path leaves the PID file); :388-411 (recovery reads it)
- evidence: `grep -rn "StalePIDRemoval" --include=*.go internal/ cmd/` returns only the definition (`runtime_process.go:532`). The spawn's wait goroutine clears in-memory state only: `p.mu.Lock(); p.pid = 0; p.cmd = nil; p.mu.Unlock()` (:339-342) — no `os.Remove(p.pidFile)`.
- why: The documented 'Stale PID files (from crashes) are automatically cleaned up on next startup' (llm-lifecycle.md §4) is done by Start's stale branch (runtime_process.go:259-260), not by StalePIDRemoval. The window in between — runtime dead, PID file present, in-memory cmd nil — is what makes Stop's recovery branch reachable at all (findings 1 and 2); a cleanup on the exit path would shrink it to the moment of exit.
- claimed vs actual: Claimed by the docs: stale PID files are cleaned up on next startup (true via Start), and Stop never signals an unvalidated pid (false, see finding 1). The helper meant to express the cleanup is dead code.
- fix shape: Either delete StalePIDRemoval or call it from the wait goroutine's exit path (removing the PID file + record only when the on-disk pid still matches the exited pid) so a dead runtime leaves no stale handle behind.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F98 [0] Verified: the GUI's main-config write matches the daemon's endpoint, shape and loopback rule (one adjacent stale daemon comment)
- file:line: ui/flutter_ui/lib/services/sdk_client.dart:1662-1700; internal/comm/http/main_config_handlers.go:75-93, 115-128
- evidence: sdk_client.dart:1698-1700 `Future<void> saveMainConfig(String content) async { await _post('/api/v1/config/main', body: {'content': content}); }` and :1671-1688 `getMainConfig()` reading `path`/`content`/`writable` and throwing on a malformed envelope. Daemon: main_config_handlers.go:75-87 `var body struct { Content string \`json:"content"\` }` -> `configCli.ValidateMainConfigJSON5(body.Content)` (400) -> :70-73 `if !isLoopbackRequest(r) { ... http.StatusForbidden }` -> :95 `configCli.WriteMainConfigAtomic(body.Content)`; :115-128 isLoopbackRequest parses RemoteAddr and requires ip.IsLoopback(). The retired duplicate route has zero Dart references: a repo-wide grep for 'config/memory' finds none under ui/flutter_ui (users_panel.dart:62-68 now reads `getMainConfig()`), and the daemon no longer registers it (removed at server.go in 67d8c867; its own test asserts 404).
- why: No defect in the GUI write path: it is a full-document overwrite of the text the user loaded (so unknown keys survive) to the same endpoint/shape the daemon accepts, and loopback-only is enforced and documented. Two adjacent nits (daemon/site scope, reported because the audit asked to verify the seam): (1) internal/comm/http/api_handlers.go:4082-4083 still says "main_config_handlers.go for the deprecated /api/v1/config/memory alias" although no alias exists (the route is unregistered and the daemon's test pins 404); (2) site/generated/http-routes.json:424 still advertises `GET /api/v1/config/memory`. Also, a 403 that means 'file not writable' (main_config_handlers.go:89-91) is reported to the user as the loopback rule (main_config_editor.dart:150-154: "the daemon accepts config writes only from the same host (loopback), so meept.json5 cannot be saved from this gui. your edits are still here."), which is the wrong explanation — though the save button is disabled while `writable` is false, so it is nearly unreachable.
- claimed vs actual: Claimed (sdk_client.dart:1690-1697): 400 -> daemon json5 text verbatim, 403 -> loopback, 503 -> no config service, all surfaced with statusCode preserved. Actual: matches; `_handleError` (sdk_client.dart:155-211) reads `message`/`error` and writeError emits `{"error": ...}` (server.go:1656-1658), so the daemon's json5 parser message reaches the editor verbatim.
- fix shape: Optional: distinguish the two 403 bodies on the GUI side (match the daemon's 'not writable' text) and delete the stale alias comment / regenerate site/generated/http-routes.json.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F99 [0] Verified: the prompts error surface works, including a non-200 with an empty body (minor truncation only)
- file:line: ui/flutter_ui/lib/features/prompts/prompt_panel.dart:67-75, 81-103, 210-235; ui/flutter_ui/lib/services/sdk_client.dart:1238-1290
- evidence: prompt_panel.dart:67-75 `catch (e) { setState(() { _error = _describeLoadError(e); _isLoading = false; }); }` with :81-103 mapping 401/418/404/503 and `default: if (error.statusCode > 0) return 'daemon error (http ${error.statusCode}): ${error.message}'; return error.message;`. The decoder no longer coerces a wrong shape to []: sdk_client.dart `_decodePromptsEnvelope` throws `SdkApiException(message: 'malformed prompts response: missing "prompts" key ...')` / `'unexpected prompts response type ...'`. For a non-200 with an EMPTY body, dio raises badResponse, `_handleError` (sdk_client.dart:155-211) keeps `statusCode ?? 0` and falls back to `'Server error: $statusCode'`, which the panel renders as `daemon error (http 500): Server error: 500`. A `{"prompts": null}` body cannot occur: the daemon always emits a non-nil slice (internal/services/prompt_service.go:281 `result := make([]PromptEntry, 0, len(seen))` -> api_handlers.go:4335 `map[string]any{"prompts": entries}`).
- why: The commit's goal holds: the panel shows a specific, actionable cause for every failure class instead of a silent empty list, and the empty-list state (prompt_panel.dart:251-296) is reachable only for a genuine 200-with-[]. Two cosmetic notes, not defects: the message is duplicated in the default branch ('daemon error (http 500): Server error: 500'), and `_buildErrorState` clips the daemon text to 3 lines with an ellipsis (:232-233), so a long daemon message is partly hidden.
- claimed vs actual: Claimed (commit 0ca75914): 'prompts panel surfaces the daemon error instead of a silent empty list'. Actual: correct, including the empty-body case requested by the audit; worth trimming the duplicated fallback text and the 3-line clip.
- fix shape: Drop the redundant 'Server error: N' when constructing the default message, and let the error text wrap (or add a copy/expand affordance) instead of maxLines: 3.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).

### F100 [0] CI's pre-commit job cannot execute the hook suite at all (macOS-only interpreter), so the wave's new gate is unreachable there
- file:line: .github/workflows/code-quality.yml:22-34; .githooks/pre-commit-build:1; .githooks/pre-commit-dart-format:1; .githooks/pre-commit:23-24
- evidence: code-quality.yml runs on `ubuntu-latest` and executes `git config core.hooksPath .githooks; .githooks/pre-commit`. `.githooks/pre-commit` has `set -e` (:28) and calls step 1 :39-40 `"$SCRIPT_DIR/pre-commit-build"`, whose shebang is `#!/opt/homebrew/bin/bash` (pre-wave, 045a5336). On Linux the interpreter does not exist, so exec fails (126/127) and the parent aborts at `[1/17]` — the wave's new `.githooks/pre-commit-dart-format` (same shebang) is never reached. AGENTS.md:76 itself documents 'sub-hooks use /opt/homebrew/bin/bash'.
- why: Pre-existing (attribution: 045a5336 for build/staticcheck), but it means the CI job named 'Pre-commit Hooks' is red and every claim that the pre-commit suite gates CI (including the wave's 'enforce dart format in pre-commit and lint-ci') is unverified in CI. No workflow runs `make lint-ci`, `make graphs-check` or `mage docsCheck` at all.
- claimed vs actual: The wave's hook comment (pre-commit-dart-format:5-7) and .githooks/README present the hook set as the enforcement layer; in CI that layer does not run.
- fix shape: Either run the hook suite on macOS runners, or switch sub-hook shebangs to `#!/usr/bin/env bash` with a documented bash>=4 requirement (and let CI install bash 5), and add `make lint-ci` to ci.yml if it is meant to be the shippable gate.
- parent verification: AUDITOR-REPORTED - not parent-verified in this pass (see the disclosure ledger).


## Disclosure ledger - what this pass did NOT verify

1. No fix was applied, and none is implied: this is a report.
2. Of 100 findings, the parent source-verified 24 (every CRITICAL/HIGH plus the
   acceptance-artifact claims). The remaining ~76 (mostly MED/LOW/INFO) carry
   the auditor's evidence but no independent parent reproduction - read them as
   "one agent read this and quoted code", not as established fact.
3. No HTTP traffic was captured: the scope-D auditor's read-only curl probe
   against the already-listening daemon on 127.0.0.1:18099 was denied, so every
   HTTP-route claim is code-derived.
4. Flutter tests and `dart analyze` were NOT run (both write to
   ui/flutter_ui/.dart_tool in a repo with live siblings). The GUI findings rest
   on source reads; the wave's new Dart tests are unproven.
5. The m4 gold-replay acceptance run and the wave-5 confusion run were NOT
   re-executed (they need an untracked replay corpus plus a live :8090 embed
   server plus ModernBERT weights). Their numbers are analysed arithmetically,
   not re-measured.
6. `make graphs-check` and `mage docsCheck` freshness were proven stale by the
   scope-G auditor in a /tmp clone at HEAD, not in this working tree.
7. No cross-check against the sibling sessions' own intent for these commits:
   commit messages were treated as claims to falsify, not as ground truth.
8. Not audited at all in this pass: internal/security, internal/employee,
   internal/auditlog, the menubar/Swift client, and everything outside the
   wave's touched paths.

## Recommended order of work (report only; no fix is authorized by this pass)

1. Stop the hang class first: tactical.go:1139-1143 (C-3/F1), the dead
   predicate + early return at tactical.go:1040/1054, and the review_manager
   stale-state writes. These three together convert ordinary work into a task
   that never finalizes.
2. Then the exposure class: daemon.go:1137 (wildcard bind) and the
   loopback-gate asymmetry on GET /api/v1/config/main.
3. Then 691d83f6's closed async gate (C-0) before the classifier change is
   relied on in any run.
4. Treat every eval headline as UNVALIDATED until the corpus/replay overlap
   guard and a committed producer for the veto policy exist.
