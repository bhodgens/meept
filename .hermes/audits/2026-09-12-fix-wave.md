# meept fix wave 2026-09-12/13 - from .hermes/audits/2026-09-12-bughunt-wave.md

Authority: user directive "fix issues with subagents". Base sha at dispatch
691d83f6; the tree moved to 01847c4e during the wave (sibling sessions).

Shape: 7 isolated fixers with disjoint file ownership (plan:
/tmp/meept-fixwave-2026-09-12/PLAN.md). No fixer ran a git write command.

## Group 1 - step state + validation gates (internal/agent/{tactical,review_manager,ralph_loop}.go)
Fixed: persist-before-assign; the len(Evidence)==0 predicate defeated by the
zero-value decode artifact (now hasMeaningfulEvidence, artifact no longer
stored); the early `return nil` in the claim-vs-evidence branch; the
SetState-then-Update stale-state writes on both approval paths and the
needs_info path; Validated no longer stamped without meaningful evidence and
ValidationError no longer cleared on approval; the task gate re-scoped to
EXPLICIT verdicts plus a residual net (HasValidator && meaningful evidence &&
!Validated) with a new terminalizeStepCompleted for the three bypass paths;
ralph cap now evaluates the final attempt's evidence before failing; IsTerminal
guards on failTaskAtCap and finalization; the task.failed payload now carries
the subscriber's key set.
New: ValidationRetryCount had NO DB column, so the retry branch re-queued
unboundedly - an in-memory counter now binds the cap.
Pins: 15 tests across tactical_validation_gate_test.go, ralph_loop_cap_evidence_test.go.
Not fixed (not owned): internal/validator/{filesystem,manager,task}.go still
treat "no errors" as valid.

## Group 2 - llm runtime lifecycle (internal/llm/*, cmd/meept/runtime.go, internal/pathutil/expand.go)
Fixed: automatic Stop now refuses a foreign instance token and identity-checks
every caller (fail-closed); spawnedByUs set only after a successful spawn; the
reap deletes the durable spawn record and a config auto_stop_on_exit:false
outvotes a stale record; `meept runtime start` writes AutoStop=false so the
sweep spares an operator runtime; doctor's reaper now applies the same
live-owner veto as the daemon; CLI stop passes a spawn command so identity is
verifiable; the health checker's run flag is generation-scoped; mlx prose
replies no longer return ErrEmptyResponse; ExpandMeeptPath honors MEEPT_HOME
(pid dir == record dir == scan dir under an override); the bare-JSON tool-call
miner no longer drops a comma-following call; StalePIDRemoval is now called
from the exit path.
Pins: 20 tests, mutation-verified (each fails without its fix).
Not fixed (not owned): runtime_startlock.go lock key, runtime_logs.go dir,
configui toggle (fixed by group 3 instead).
Docs flagged: AGENTS.md and docs/configuration/llm-lifecycle.md describe the
pre-change sweep reach.

## Group 3 - daemon + HTTP + config
Fixed: the daemon no longer assigns an empty listen address (httpListenAddrFor
keeps the loopback default) and the boot log cannot print addr="";
GET /api/v1/config/main is now loopback-gated like its POST half; the
autonomous marker moved from a loop latch to the job's turn context
(stepJobTurnContext) so the interactive loop is never mutated; "websearch" ->
"web_search" in DefaultAlwaysFullTools (the new registry-walking test
immediately caught a second latent instance: platform_status is only
registered under [multiagent].enabled); the bundled prompt tier is now always
absolute; the configui lifecycle toggle renders the provider accessor.
Pins: 9 new tests; config_test.go's false-premise assertion corrected.
Premise correction: the wildcard-bind claim was NOT reachable (see the
CORRECTIONS block in the audit report) - server.go:240-242 re-applies the
loopback default inside NewServer.

## Group 4 - routing + classifier (internal/agent/{llm_classifier,dispatcher}.go)
Fixed: C-0 - both classifier sites now derive RequiresPlanning from the lane, so
an LLM-classified quickplan opens the async gate; inputContainsGitVerb is
word-boundary matched; the recall branch outranks the imperative override; the
compound collapse only treats Chat/Platform/Recall as chat-like; the recall
override now records agent/intent bookkeeping.
Answered (item 5, source-proven): 26 of 27 lanes resolve to an on-disk
AGENT.md; quickplan -> "orchestrator" is a pipeline label, and the async branch
resolves the planner spec via publishPlanRequest -> orchestrator.plan ->
strategic.Plan, so no orchestrator agent dir is needed. Pinned, not asserted.
Also modified: llm_classifier_test.go's `code` case, which pinned the old
incoherent RequiresPlanning=false (the fix makes it true, matching the semantic
producer).
Residual (not owned): dispatcher_compound_misroute_test.go keeps a test-only
copy of the old collapse logic; prefilter_veto_test.go's vacuous test remains
(a new file now genuinely exercises the fallthrough).

## Group 5 - build gates, hooks, scripts, CI
Fixed: the `go list` guard in five hooks now classifies ok/excluded/
nested-module/missing/broken, prints a visible SKIP line, and hard-fails on a
broken package; pre-commit-build uses -o /dev/null per downstream package and
asserts no new root artifact; the dart hook validates the INDEX blob, not the
working tree; u1000's dedupe is exact-line, not substring; the e2e script
derives its port remap from the config and hard-fails if a loopback literal
survives; gui-daemon-connect.py honors the transport.http.port alias; the dart
gate now fails (not skips) when the SDK is missing; hook docs renumbering and
counts corrected; e2e lock trap installed before acquisition with a pid+lstart
recycle check; hook shebangs made portable.
Reproduced old-vs-new in a /tmp fixture for every gate (each old gate passed on
bad input; each new gate exits 1), plus the full 17-step chain.
Not wired: graphs-check / docsCheck in CI - both are RED on this tree, so wiring
them first would break CI. Corrected the false "(CI)" claims instead, and added
a `docs-check` Makefile target. Parent must run, in order: `make graphs` then
`make docs-generate`, commit the generated artifacts, and only then add the
steps to ci.yml.
New finding (reported, not fixed): the downstream-importer set is always EMPTY
because :111 compares full import paths against repo-relative dirs; fixing it
adds 23-25s per commit.

## Group 6 - eval/measurement integrity + observability
Fixed: the veto row's verdict is UNVALIDATED (the only committed producer
computes the double-confidence policy and writes FAIL) with a CORRECTIONS block
in the report, the FINAL-REPORT and the tfidf_veto.go comment; m4_gold_acceptance.py
now runs both policies and writes write-once per-policy artifacts; a
corpus<->replay disjointness guard (exact + cosine>0.95) refuses to score a
leaked case (proven: the shared h18-planexec-001 case is detected, exit 2); a
MIN_ROUTED=20 coverage floor returns INSUFFICIENT_COVERAGE instead of a
verdict; the withdrawn 92.8% iter-16 claim is struck and the corpus count
corrected to 389 (139+250) from the committed loader; 8 byte-identical live
rows relabelled live-session and the "verbatim text never enters git" invariant
corrected; the unreachable 'suppressed' bucket removed from the harvest views;
the destroyed silver artifact recovered from git (44c44255) into a
pre-edit-recovered file; p50_ms no longer reported as a measurement when there
are no cache-miss samples; HashInput's salt comment corrected; the in-sample
caveat added to the wave-5 "gate is at optimum" claim.
Disproved: the v_fallback_trend sub-claim (see the audit report's CORRECTIONS).
Not fixed (not owned): persisting the Door-1 verdict needs dispatcher.go (the
column would be always-empty otherwise); the input_summary consumer is
cmd/meept/session.go.
New discrepancy (reported): iter-16-corrected/report.md:44's own formula
evaluates to 83.44%, not the 91.44% it is labelled with.

## Group 7 - Flutter GUI (ui/flutter_ui/**)
Fixed: the exit guard now covers the orchestrator editor and the connection
form; the desktop window close and browser Back are guarded (window-close seam
plus GoRoute.onExit on /settings and all 9 /tools/* routes); a stale whole-file
meept.json5 save prompts reload/overwrite instead of silently reverting a
concurrent block save; the sidebar's leader/palette navigation routes through
the shared guard; the prompts panel renders inside ToolPanelShell; a refused tab
switch no longer leaves callers' side effects armed.
Verified: `flutter test --no-pub` whole suite +487 passing, `dart analyze lib`
clean, `dart format` 0 changed. NOT run: pub get / build (forbidden installs),
so the real browser Back is verified at the router level (GoRouterDelegate.popRoute).

## Parent duties still open
1. Full gate matrix on the landed wave (in progress).
2. Concern-split commits with explicit paths (sibling sessions share the tree).
3. Regression re-audit of the fix commits (the wave's own new bugs).
