package main

// CLI subcommand names used across cobra command definitions.
const (
	cmdList   = "list"
	cmdStatus = "status"
)

// RPC parameter keys used in map[string]string / map[string]any calls.
const (
	paramName      = "name"
	paramSessionID = "session_id"
)
