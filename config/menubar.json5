// Menubar app configuration
{
  // Connection to meept daemon (always uses HTTPS)
  "daemon": {
    "http_url": "https://localhost:8081",
    // Default dev API key (matches daemon). For production, generate a new key.
    "api_token": "meept_dev_default_key_CHANGE_ME"
  },

  // UI preferences
  "ui": {
    // Show panel in menu bar
    "show_in_menu_bar": true,
    // Start at login
    "start_at_login": false,
    // Menu bar icon style: "icon", "text", "both"
    "icon_style": "icon",
  },

  // Notifications
  "notifications": {
    "enabled": true,
    // Notify on: "all", "errors_only", "none"
    "level": "errors_only",
  },
}
