# Makefile and AGENTS wiring - Implementation Leaf

> **For the implementing agent:** You are the implementer for this leaf.
> Implement ALL tasks below using TDD. Do NOT commit — the orchestrator
> handles all git operations after review. Do NOT use read_file on existing
> source files — explore with search_files or terminal cat. After writing
> a file, do NOT read it back to verify — write once and stop. After
> completing, report what you built, what files you touched, and any
> deviations from the spec.

## Meta

- **Parent:** master.md
- **Scope:** Make targets, AGENTS.md script table, generated markdown present.
- **Dependencies:** 02-script.md
- **Estimated Context:** 40K
- **Concurrency Group:** C

## Goal

Users run `make research-harness` and `make research-harness-check`. CI (`make lint-ci` via `audit-scripts`) regressively fails if catalog evidence disappears or markdown is stale. AGENTS.md lists the script.

## Context

`Makefile` `audit-scripts` target currently runs dart-enum-name-shadow and utf8-byte-arithmetic only (near line 435). `AGENTS.md` Audit scripts table is near line 443.

Key files:

- `Makefile` — add `.PHONY` targets next to `graphs` / `audit-scripts`
- `AGENTS.md` — Build commands + Audit scripts table
- `docs/research/harness-techniques.md` — generated; do not hand-write

## Interface Contracts (From Parent)

### What This Leaf Exposes

Make targets and AGENTS.md rows per Contract 4.

### What This Leaf Consumes

`scripts/research-harness-lit.py` CLI from Contract 2.

## Tasks

### Task 1: Make targets

**Objective:** Add research-harness and research-harness-check.

**Files:**

- Modify: `Makefile` (after graphs-check / inside audit-scripts)

**Step 1:** Confirm old state: `grep research-harness Makefile` empty.

**Step 2:** Add:

```
.PHONY: research-harness research-harness-check
research-harness:
	@python3 scripts/research-harness-lit.py

research-harness-check:
	@python3 scripts/research-harness-lit.py --check
```

Hook `research-harness-check` into `audit-scripts` after the existing python audits (same `if [ -f scripts/research-harness-lit.py ]` pattern).

**Step 3:** Run `make research-harness-check`. Expected: exit 0.

### Task 2: AGENTS.md

**Objective:** Document the command and the script.

**Files:**

- Modify: `AGENTS.md`

**Step 1:** In the Build & Development Commands bash block, add:

```
make research-harness         # verify catalog evidence + regenerate techniques md
make research-harness-check   # CI: fail if evidence missing or md stale
```

Place near the graphs commands.

**Step 2:** In the Audit scripts table add a row:

`research-harness-lit.py` | Verifies harness-technique catalog evidence and markdown freshness (`make research-harness-check`)

**Step 3:** Confirm the Feature Documentation locations bullet still accurate. Optional: add `docs/research/` as a location for living research maps. Do not invent new invariants.

### Task 3: Ensure generated markdown exists

**Objective:** `docs/research/harness-techniques.md` is on disk from the generator, not hand-edited.

**Files:**

- Create/update via generator only: `docs/research/harness-techniques.md`

**Step 1:** Run `python3 scripts/research-harness-lit.py`

**Step 2:** Confirm first line contains `generated by scripts/research-harness-lit.py`

**Step 3:** Do not edit the md by hand.

## Self-Verification Checklist

- [ ] All tasks implemented and tests passing
- [ ] Interface contracts (above) satisfied exactly
- [ ] All files at exact specified paths
- [ ] No deviations from spec (or deviations documented below)
- [ ] No scope creep - only what the tasks specify
- [ ] `make research-harness-check` exits 0
- [ ] AGENTS.md table row present

**DO NOT COMMIT.** The orchestrator handles all git operations after review.

**Deviations from spec:** [none / list any with rationale]

## Review Checklist

- [ ] Every Task above is implemented
- [ ] Every test in the task is present and passing
- [ ] Interface contracts match exactly (signatures, types, file paths)
- [ ] Code follows project conventions (naming, error handling, structure)
- [ ] No bugs, no security issues
- [ ] No scope creep beyond specified tasks

Output: APPROVED or list of specific gaps with file + line references.

## Notes

Sibling sessions edit Makefile. Touch only the new targets and the audit-scripts hook. Do not reformat unrelated Makefile regions.
