; Generic references query (fallback)
; Matches identifier usages across languages

; Generic identifier references
(identifier) @ref.variable

; Property identifiers (for TypeScript/JavaScript)
(property_identifier) @ref.property

; Type identifiers
(type_identifier) @ref.type