{"time":"2026-08-30T20:43:04.426962Z","action":"improve_skill","skill_name":"subagent-orchestrator-race-on-nested-completion","diff":"# Subagent Orchestrator — Race on Nested Completion\n\n## Purpose\nWhen executing a plan by delegating steps to subagents, do not pause between steps. Every time a nested agent completes, immediately race ahead: ingest its result, dispatch every step whose dependencies are now satisfied, and keep going until the plan is fully resolved — all within a single exchange.\n\n## When this applies\n- You receive a task decomposed into steps (your own plan or an upstream planner's JSON plan with `description`, `tool_hint`, `depends_on`).\n- You are the coordinator spawning or invoking subagents for individual steps.\n- A subagent returns a completion payload (job_id, response, evidence).\n\n## Core rules\n\n1. **Single-exchange resolution.** Resolve the entire delegated plan before returning to the user. Never end your turn with \"step 1 done, awaiting continuation\" — that fragments the workflow and hands control back prematurely.\n\n2. **Race on every nested completion.** The moment a subagent result arrives:\n   - Parse its status and evidence (claims, file evidence, hashes).\n   - Mark the step done or failed.\n   - Recompute the dependency graph and immediately dispatch all newly unblocked steps — in parallel where their `depends_on` allows.\n\n3. **Dispatch early.** At the start of the exchange, fire every step with empty `depends_on` in parallel rather than sequentially.\n\n4. **Verify state-mutating steps.** For steps that write files, commit, or otherwise mutate state, ensure the result is verified (existence, size, content, hash) — either by the executor or a dedicated follow-up verification step. If verification fails, re-run the step once with the failure evidence before reporting failure.\n\n5. **Propagate context.** When dispatching a dependent step, include the results/evidence of its dependencies in the step prompt (\"Results from Prior Steps\"). Subagents have no shared memory of prior jobs unless you provide it.\n\n6. **Map hints to agents.** Translate the plan's `tool_hint` to the correct available agent using the dynamic agent list when provided (code→coder, debug→debugger, analyze→analyst, git→committer, research→researcher, plan→decompose further, chat→self).\n\n7. **Terminal report only at the end.** Emit the structured completion JSON (status, accomplished, not_done, issues, observations, evidence) once — after all steps have completed or irrecoverably failed. Include per-step outcomes and concrete evidence (file paths, sizes, hashes, content).\n\n## Failure handling\n- A failed step: retry once with clarified instructions that include the failure evidence.\n- An irrecoverably failed dependency: mark downstream steps as blocked, report `status: \"partial\"` with issues, and populate `suggested_next_agent`.\n- Never silently drop a step: every step in the plan must end as done, failed, or blocked in the final report.\n\n## Anti-patterns (known causes of prior negative outcomes)\n- Returning to the user after the first subagent completes instead of continuing the plan.\n- Dispatching independent steps sequentially instead of in parallel.\n- Omitting prior-step results when prompting a dependent subagent.\n- Ending the exchange after a write with no verification of the artifact.\n- Producing the terminal JSON report before all steps are resolved.\n","score":0.875,"accepted":true,"reason":"grounded_in_evidence: Content is derived from concrete trace data — successful traces supply the 'race on nested completion' pattern, and each listed anti-pattern corresponds to an observed negative outcome. Slightly below maximum because the causal link for the anti-patterns is hedged ('likely caused') and the injection counts cited are internally inconsistent (5 vs. 1 positive + 2 negative).; preserves_existing_value: The current skill body is empty, so nothing of value is discarded. The candidate converts value that only existed implicitly in successful traces into explicit, reusable guidance, directly addressing the 0.20 effectiveness baseline.; specificity_and_reusability: Rules are concrete and actionable (dependency recomputation, parallel dispatch of unblocked steps, context propagation in prompts, bounded retry-once, terminal report schema, no silently dropped steps). The core pattern generalizes across orchestration contexts, though the fixed tool_hint-to-agent mapping (rule 6) and the rigid single-exchange mandate lean toward one specific environment; both are mildly hedged ('when provided') which helps.; safe_to_publish: No harmful, unstable, or dangerous instructions. State-mutating steps explicitly require verification, retries are bounded to one attempt, failure reporting is structured, and the only mild consideration — firing all unblocked steps in parallel — is an operational throughput concern, not a safety risk."}
{"time":"2026-08-30T20:54:40.666166Z","action":"improve_skill","skill_name":"hujson-unquoted-keys-fix","diff":"# HJSON Unquoted Keys Fix — Strict JSON Emission and Repair\n\n## Purpose\nAny time JSON is required — a planner schema, a structured completion report, or repairing an HJSON/HuJSON file — emit strict, machine-parseable JSON: every key and string value double-quoted, no comments, no trailing commas, no markdown fences, no surrounding prose. Unquoted keys are the canonical failure this skill exists to prevent and fix.\n\n## When this applies\n- A prompt demands output of ONLY valid JSON in an exact schema (e.g., a steps array with description / tool_hint / depends_on fields).\n- You are returning a structured completion or evidence report.\n- You are converting or repairing an HJSON-style document (unquoted keys, // comments, trailing commas) into valid JSON.\n- You are writing a JSON (or any) file to disk as part of the task.\n\n## Core rules\n\n1. **Quote keys and string values.** Every object key and every string value gets double quotes. Numbers, true, false, and null stay bare. Escape embedded quotes and newlines rather than emitting them raw.\n\n2. **Strip everything strict JSON forbids.** No // or /* */ comments, no trailing commas, no single-quoted strings, no bare multi-word strings. When repairing an HJSON document, quoting bare keys before the colon is the first and most common fix.\n\n3. **Match the requested schema exactly.** Use the exact field names given in the prompt, keep depends_on indices within range of the steps array, and honor stated limits (e.g., maximum step count).\n\n4. **Output only the JSON when told to.** If the prompt says ONLY valid JSON with no markdown and no explanation, the entire response is the JSON document — no code fences, no preface, no trailing commentary.\n\n5. **Validate before sending.** Re-parse your own output before responding: balanced braces and brackets, every string closed, every comma separating exactly two elements. One stray token invalidates the whole payload for a machine consumer.\n\n6. **Verify file writes on disk.** When a task writes a file (e.g., file_write with direct: true so bytes hit disk immediately), confirm the result afterwards: existence, size, exact content, and hash. If verification fails, re-run the write once with the failure evidence in hand before reporting failure.\n\n7. **Report in strict JSON with evidence.** Completion reports use the structured schema (status, accomplished, not_done, issues, observations, suggested_next_agent, user_decision_needed) and attach concrete evidence objects (file_exists, file_hash, file_read_content) — all strictly quoted.\n\n## Repairing an existing HJSON file\n1. Read the file and locate every unquoted key (a bare token before a colon), plus comments and trailing commas.\n2. Rewrite in place: quote keys and bare string values; remove comments and trailing commas.\n3. Write back with a direct write, then read the file again and validate it (parse it, or compare content and hash against what was intended).\n4. Report success only with verification evidence; otherwise retry once, then report failure with specifics.\n\n## Anti-patterns (known causes of negative outcomes)\n- Emitting HJSON-style output (unquoted keys) when a strict JSON schema was demanded.\n- Wrapping required JSON in markdown fences or explanatory prose after an ONLY-JSON instruction.\n- Declaring a write complete without reading the artifact back.\n- Reporting a completion status with no concrete evidence (paths, sizes, hashes, content).\n","score":0.875,"accepted":true,"reason":"grounded_in_evidence: The proposal cites concrete metrics (5 injections of an empty body, effectiveness 0.00 with 0 positive / 3 negative outcomes) and derives each rule from specific observed behaviors in successful traces (strict schema-matched JSON, direct writes with hash/content verification). Minor deductions: the causal claim that filling the skill prevents recurrence is inferential, and a few heuristics (e.g., 'first and most common fix') are asserted rather than trace-derived.; preserves_existing_value: The current skill body is empty, so nothing of value is discarded; the proposal retains the failure-mode lesson encoded in the name and makes it explicit, strictly adding value. Slight deduction because the content broadens well beyond the name's narrow HJSON-key scope (general JSON emission, file-write verification, reporting schemas), which leaves the skill name underselling its coverage rather than enhancing the original framing.; specificity_and_reusability: Rules are concretely actionable (quote all keys/strings, strip comments and trailing commas, re-parse before sending, verify writes with existence/size/content/hash, bounded single retry) and apply across planner output, completion reports, HJSON repair, and file writes. The exact field lists (steps schema, report schema) are framed as examples, keeping it generalizable, though a few sections lean on task-specific schemas that may not match every deployment.; safe_to_publish: The content contains only benign formatting, validation, and verification guidance. Retries are explicitly bounded to one, no destructive or risky operations are instructed, and verification-before-reporting is a conservative practice. Clearly safe."}
{"time":"2026-08-30T20:55:56.344166Z","action":"improve_skill","skill_name":"parallel-subagent-code-review","diff":"# Parallel Subagent Code Review\n\n## Purpose\nWhen code changes (or delegated code steps) need to be checked before a task is declared done, fan the review out to multiple subagents in parallel, verify every state-mutating claim against the actual artifacts, and synthesize one structured report. Do not review sequentially what can be reviewed concurrently, and do not accept a subagent's claim without file-level evidence.\n\nThis skill governs the review/verification fan-out and final reporting. General orchestration behavior (never pausing between subagent completions) still applies alongside it.\n\n## When this applies\n- Subagents (or you) have produced code changes — files written, edits made, commits created — and completion must be verified.\n- You are coordinating subagents and multiple independent checks could run at once.\n- A completion payload asserts work is done and needs confirmation before the final report.\n\n## Core rules\n\n1. **Fan out independent checks in parallel.** Dispatch every check with no upstream dependency in the same turn. Map checks to the dynamic agent list when provided (e.g., `explore` for read-only existence/location confirmation, `skeptic` to stress-test claims and edge cases, `doc-keeper` for documentation invalidated by the change); fall back to whatever read-only reviewers are available.\n\n2. **Verify state-mutating claims with artifact evidence.** \"File written\" is unverified until you hold:\n   - `file_exists` with path and size,\n   - `file_hash` (sha256), and ideally\n   - `file_read_content` with the exact bytes read back.\n\n   Working example from a verified run: a 3-byte `answer.txt` confirmed by size, sha256, and read-back of `42\\n` before any completion report was emitted.\n\n3. **Match content exactly, not approximately.** Check against the specification literally — exact string, exact trailing newline, exact line count. \"Contains 42\" does not satisfy \"exactly '42' on a single line\".\n\n4. **Sequence dependent checks, propagate context.** A check that consumes another step's output waits for it; dispatch it the moment the upstream result lands, and include those results in its prompt — subagents have no shared memory of prior jobs.\n\n5. **Retry once with evidence.** If verification fails, re-run the offending step once with the failure evidence embedded in the prompt; only then report failure.\n\n6. **One terminal report, after everything resolves.** Emit the structured completion JSON a single time — `status` (`completed`/`partial`), `accomplished`, `not_done`, `issues`, `observations`, `suggested_next_agent`, `user_decision_needed` — plus an evidence block (`claims` + typed evidence entries). Record environment anomalies under `observations` instead of silently ignoring them (e.g., \"project_info reported 'no project bound' while the working directory resolved correctly\").\n\n## Failure handling\n- A failed verification: one evidence-informed retry, then mark the step failed with the evidence attached.\n- A reviewer finding that arrives late: wait for it — the report is emitted only after all dispatched checks resolve.\n- Every dispatched check must appear in the final report as passed, failed, or blocked. Nothing is silently dropped.\n\n## Anti-patterns\n- Declaring success on a subagent's assertion without reading the artifact back.\n- Running independent reviews one at a time instead of in parallel.\n- Emitting the completion JSON while verification steps are still outstanding.\n- Dropping a late-arriving finding instead of folding it into `issues`.\n- Loose content checks where the specification is exact.\n","score":0.875,"accepted":true,"reason":"Concrete usage data supports the change: an empty skill body injected 6 times with 2 negative outcomes and 0.00 effectiveness is a clear signal that guidance was needed and absent. The proposed content is extracted from successful traces and cites specific verifiable details (size + sha256 + read-back of a 3-byte file, an environment anomaly recorded under observations). Minor deduction: the flagship worked example (answer.txt containing '42\\n') appears drawn from a single sandbox-style task, so parts risk being overfit to one run, though the proposal generalizes with fallback language.; There is no existing skill content to preserve (the body is empty), so the main preservation risk is conflict with the adjacent accepted skill. The proposal handles this well by explicitly scoping itself: it governs the review/verification fan-out and reporting discipline while stating the race-on-nested-completion orchestration behavior still applies alongside it — complementing rather than duplicating or contradicting established value.; Well-balanced overall: it specifies concrete, reusable mechanisms (typed evidence fields like file_exists/file_hash/file_read_content, a defined terminal JSON schema, a retry-once-with-evidence policy, anti-patterns) that generalize across verification tasks. Slight deductions for environment-specific agent names (explore/skeptic/doc-keeper, though hedged with a read-only fallback) and a platform-specific report schema that could become brittle if the surrounding contract changes.; Clearly safe: the content is verification and reporting discipline with no destructive or risky instructions. Retries are bounded to one, every dispatched check must resolve to passed/failed/blocked (preventing silent hangs or dropped work), and no unstable or harmful behavior is introduced."}
{"time":"2026-08-30T20:57:31.188424Z","action":"archive_skill","skill_name":"subagent-orchestrator-race-on-nested-completion","score":0.5,"accepted":false,"reason":"heuristic fallback: LLM unavailable, using neutral 0.5 scores; overall score 0.50 is below minimum 0.75"}
{"time":"2026-08-30T20:57:33.461958Z","action":"archive_skill","skill_name":"subagent-orchestrator-race-on-nested-completion","score":0.5,"accepted":false,"reason":"heuristic fallback: LLM unavailable, using neutral 0.5 scores; overall score 0.50 is below minimum 0.75"}
{"time":"2026-08-30T21:00:58.019885Z","action":"improve_skill","skill_name":"git-blame-contributor-analysis","diff":"# Git Blame Contributor Analysis\n\n## Purpose\nWhen a request asks who wrote or last changed code — per-line attribution (`git blame`), contributor rankings, file ownership, or \"when was this line introduced and by whom\" — run the real git commands against the bound repository, parse their output precisely, and report per-contributor findings backed by commit hashes. Never state attribution from memory or inference: every author, date, and line claim must trace to command output you actually read.\n\n## When this applies\n- \"Who wrote this file / this function / these lines?\"\n- \"Which contributors changed X the most?\" — per-author churn or ownership statistics\n- \"When was this line introduced, and by whom?\"\n- Identifying the right author or commit to consult before modifying code\n- Coordinating subagents for attribution work (git operations → committer, synthesis → analyst)\n\n## Core rules\n\n1. **Run blame on the exact target.** Scope precisely: `git blame -- \u003cpath\u003e` for a file, `git blame -L \u003cstart\u003e,\u003cend\u003e -- \u003cpath\u003e` for a line range (function-level questions get the function's line range, not the whole file). For programmatic parsing use `git blame --porcelain -- \u003cpath\u003e` and group the `author` / `author-mail` / `author-time` / `summary` records by commit hash.\n\n2. **Handle renames and moves before trusting attribution.** Copied or moved code blames to whoever performed the move unless you ask git to follow it: add `-C` to detect copies, `-M` for moves/renames, and `-w` to ignore whitespace-only changes. For history across file renames use `git log --follow -- \u003cpath\u003e`. State which flags you used — attribution computed without them can be wrong.\n\n3. **Match the statistic to the question.** Line-level attribution comes from blame output. Commit-count rankings come from `git shortlog -sn` (add `--all` only if explicitly asked). Per-author line totals come from aggregating blame lines by author. Never substitute one for the other — \"most commits\" and \"most lines\" are different answers.\n\n4. **Verify before reporting.** Confirm the repository/working directory is bound and the target path exists and is tracked before blaming; wrong or untracked paths yield empty or misleading output. After analysis, re-check that every contributor named in the report actually appears in the command output. If the environment reports an anomaly (e.g., \"no project bound\" while the working directory resolves correctly), record it under `observations` instead of silently proceeding or failing.\n\n5. **Cite commit hashes as evidence.** Every attribution claim carries its evidence: commit hash, author, date, and the file/line range it covers. Aggregated tables must state the total lines examined so \"author A: 120 lines\" is checkable against \"160 lines total\".\n\n6. **Route through the dynamic agent list when delegating.** Git operations → the git specialist (e.g., `committer`), read-only codebase lookup → `explore`, synthesis and ranking → `analyst`. Subagents have no shared memory of prior jobs: include the target path, line range, and required flags in the step prompt, and ingest the raw command output they return before synthesizing.\n\n7. **Single-exchange resolution, one terminal report.** Complete the blame run, any delegated lookups, and the synthesis before responding — never pause mid-analysis to hand control back. Emit the structured completion report once (status, accomplished, not_done, issues, observations, suggested_next_agent) with an evidence block whose entries are the actual commands and their output, or per-commit attribution records. If JSON output is demanded, emit strict JSON (double-quoted keys, no comments, no fences, no prose) per the strict-JSON skill; general orchestration behavior (no pausing between subagent completions) still applies alongside this skill.\n\n## Failure handling\n- Empty blame output: confirm the file is tracked (`git ls-files -- \u003cpath\u003e`) and you are on the intended ref. If the path is wrong, report that","score":0.8575,"accepted":true,"reason":"The need for the skill is well-evidenced (empty body, 5 injections, 3 negative outcomes, 0.00 effectiveness), and the behavioral rules (verify-before-report, observations, single-exchange, structured report) are trace-grounded. However, the domain-specific content (blame flags, porcelain parsing, shortlog vs. blame distinctions) derives from the skill's name and general git knowledge, not from observed blame-analysis failures — the evidence summary itself concedes no traces show blame work directly. Intervention is justified; the specifics are plausible but not trace-validated.; The current skill is empty, so no existing value is at risk. The proposal also explicitly complements rather than duplicates the accepted orchestration, code-review, and strict-JSON skills by reference, which is the correct behavior when introducing adjacent guidance.; Strong balance: exact commands and flags (blame -L/--porcelain, -C/-M/-w, --follow, shortlog -sn, ls-files), concrete failure handling, and anti-patterns make it actionable, while the triggers and rules generalize across any attribution question. Subagent names are given as examples against a dynamic agent list, keeping it portable rather than hard-coupled to one environment.; Entirely read-only git operations (blame, log, shortlog, ls-files); explicitly forbids history rewriting (rebase, filter-branch, amend) and fabrication of authors/dates; retries are bounded and failures route to explicit partial reporting. Clearly safe."}
